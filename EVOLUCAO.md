# Evolução do Traço — objetivo integral e evidência

Contrato operacional de desenvolvimento da visão vigente. Atualizar por evidência, sem transformar o incremento concluído em redefinição da finalidade. “Perfeição” não é um teste finito; conclusão do trabalho exige confrontar toda a visão e declarar lacunas reais.

Atualização em execução, 08/09/2026 — [cinco itens](prova/cinco-itens.md):
conta Grok conectada pelo usuário; 16 operações medidas três vezes com conta e
sem conta pela política do app. Jornada de QA com dados fictícios já preservou
exercício, tentativa errada, feedback, relato parcial e adaptação após reabertura.
Atalhos e histórico foram observados no simulador. Regras v3 corrigem resumo de
tempo duplicado e preservam restrições ao adaptar. Suíte de 891 testes passou.
Qualidade semântica permanece em correção: variantes `medium` tiveram 15/27
atendimentos integrais; `high` sozinho não corrigiu a preparação. Provas negativas
permanecem no denominador. Isso não demonstra aprendizagem nem resultado pessoal
do usuário, nem encerra a visão integral.

| Capacidade da visão | Estado atual | Prova ainda necessária |
|---|---|---|
| Qualidade efetiva transversal da IA | ADR07a e QUALIDADE-IA.md: contratos estruturados, evidência por IDs, correções de contexto/transformação e Combinar em avaliação. 137 testes selecionados passaram em 07/09 às 14:16 UTC no candidato daquele momento; base viva encontrou falhas semânticas e de atribuição. **08/09 (ADR 08q, volta Q): a lacuna "nenhuma operação medida com Grok" FECHOU** — 16 operações × 6 casos × 3 execuções no aparelho do dono com a conta ligada. `conferir`, `padroes` e `conferirTentativa` atenderam 6 de 6; sete rotas foram cortadas por qualidade — cinco sem substituto medido (`ecos`, `calibragem`, `recordar`, `instigar`, `contrapor`) e duas com conserto nomeado (`responder`, `responderNasNotas`); **20** falhas de transporte (não 12: 11 + 6 + 3), todas nas quatro rotas de `grok-4.6`, pelo teto de 90 s — que a ADR 2026-09-08r mediu e substituiu. Três casos de `responderNasNotas` mediram rota que a produção não usa e foram anulados: defeito do instrumento, não do provedor. **08/09 (ADR 08r, volta Q-B): a lacuna de DISPONIBILIDADE fechou** — o teto virou um valor medido (`Grok.tetoTrabalho`, 240 s), e a remedição dos nove casos que carregavam as 20 falhas devolveu **0 falhas de transporte em 30 chamadas**, no aparelho do dono com a conta ligada (`prova/qb-teto-avaliacoes.jsonl`). O candidato da 08q foi corrigido para `acdfcb4`. Continua aberto, e agora nomeado: 3 de 15 execuções de `prepararPratica` voltam HTTP 200 com conteúdo completo e são recusadas pelo NOSSO contrato (`PraticaTrabalho.validar`) — defeito de conteúdo, com estado honesto por pedido na tela, sem executor novo a medir. **08/09 (ADR 08p, volta Q-C): a recusa do nosso parser deixou de ser um `nil`.** `PraticaTrabalho.Recusa` redige a guarda que reprovou — categoria, campo e medida, sem bruto nem credencial — e a sonda a grava em `recusasDaPreparacao`. Remedidos os dois casos no aparelho do dono com a conta ligada: **18 execuções, 18 HTTP 200 completos de `grok-4.6`, 5 recusas e as CINCO na mesma guarda** — `Prova.vaza(criterio, alvo: exemplo)` (`prova/qc-recusa-avaliacoes.jsonl`). **O defeito é NOSSO:** a régua veio do Recordar, onde o `alvo` é a resposta; aqui o `alvo` é o exemplo, que por contrato é outro caso, e ela reprova o vocabulário estrutural da própria tarefa — quadrigrama medido “a dependencia ainda aberta”, palavras que estão na instrução do autor antes de estarem no exemplo. O parser NÃO foi alargado nesta volta, por decisão: **fica no RUMO**, com a régua nos dois sentidos a escrever antes do conserto. **08/09 (volta Q-D, correções do re-G3 na mesma ADR, agora `08p`): o registro da recusa deixou de carregar o trecho** — guarda posição (qual critério, qual palavra do exemplo, de quantas) e ORIGEM (quantas das palavras do trecho o autor já tinha escrito no pedido), conferível pela `entrada` que a sonda já grava. Remedição no aparelho do dono com a conta ligada (`1B7E0E63`): **12 execuções, 12 HTTP 200 completos de `grok-4.6`, 4 recusas — 3 por vazamento, todas com as QUATRO palavras já escritas pelo autor, e 1 por limite** (`enunciado tem 1582 caracteres e o teto é 1500`), que derruba o "as onze outras guardas não dispararam" da redação anterior (`prova/qd-origem-avaliacoes.jsonl`). Somando as duas remedições: 30 execuções, 9 recusas, 8 por vazamento e 1 por limite. As **quatro** recusas da primeira corrida continuam SEM evidência individual e ficam indeterminadas. O estado honesto por pedido (`praticaIndisponivel`) e a tabela `Politica` seguem sem mudança. **08/09 (ADR 08w, volta Q-H): a recusa nomeada e o contrato do ajuste da V17 passam a viver na mesma função.** `lerPreparacao` conhece `comMudanca`, e a chave `mudanca` — que num ajuste é do contrato (ADR 08j) — recusa por nome em vez de `nil`: ausente vira `chavesForaDoContrato(faltando: ["mudanca"])`, fora do tipo `campoNaoTexto`, vazia `campoVazio`, acima de 400 `campoAcimaDoTeto`, e o vazamento ganha caso PRÓPRIO (`mudancaVazaOExemplo`) porque chamar de "critério" um campo que não é critério seria a recusa mentindo sobre qual guarda reprovou. A regra da V17 fica inteira (qualquer dessas quedas derruba a preparação toda) e a rota remota passa a usar o `Grok.tetoTrabalho` medido na 08r, não o `90` suposto. Build sem aviso e **957 testes, 956 passados, 0 falhos, 1 pulado** na árvore MESCLADA e **958 / 957 / 0 / 1** depois de trazer também o `main` que a V13 avançou durante o trabalho (`34CC3F94`) — os testes da V17 sobre `mudanca` e os da Q sobre `Recusa` passam juntos, que é o que ninguém tinha medido (`ferramentas/orca/q-h-reconciliacao.md`). | Matriz Q1–Q8 inteira, atendimento >=9 em cada dimensão e três inferências novas por caso; geração espanhola, feedback observável, fontes, todas as rotas e provedores, privacidade após espera e jornada integrada. Meta integral aberta; regras/testes não substituem essas provas. | **10/09 (ADR 2026-09-10b, volta RESPONDER): a alavanca do PROMPT foi medida em `responder` e NÃO fecha a rota.** Duas reescritas de `sistemaResponder` correram contra o texto vigente no MESMO binário — 20 casos × 3 cada braço, `TRACO_AVALIAR_PEDIDO` como única variável, 240 saídas lidas uma a uma, mais 6 de uma pergunta REAL do aparelho da conta — e as duas ficaram PIORES: base **14 e 15 de 20**, candidatos **12 e 12**. O defeito é simétrico: mandar ajudar traz de volta a estrutura inventada do documento; mandar não inventar faz o modelo parar em "não consta X" sem o próximo ato. `responder` FICA cortada e o `conserto` saiu da linha do Perfil, porque foi tentado. **Fecharam quatro lacunas de ROTA, com vermelho e verde:** a resposta podia publicar na pergunta que já não era a atual (identidade de requisição), a página era lida depois do `await`, as fontes não eram revalidadas antes de publicar, e a divulgação nomeava vizinhas que o corte de 5.000 nunca deixou viajar. O corte silencioso aos 900 saiu do parser e o RETORNO BRUTO passa a ser preservado no portão (`Grok.Diagnostico.bruto`), nas dezesseis rotas. Continua aberto: a alavanca seguinte é o CONTEXTO — `prova/10b/`, `prova/10b2/` e `ferramentas/orca/responder.md`.
| Preservar escrita, importação e restauração | ADR05h revisado (513 testes); ADR05s: auditoria rota a rota da `Sessao` — `trancarESair` ignorava a recusa e apagava a página, `salvar` gravava widget/versão/índice antes do `save`, `apagar`/`desfazerApagar` salvavam sem rollback, `importarCorpus` não projetava; tudo passa a commit→projeção, recusa deixa a linha fixa "Não consegui guardar agora. O texto continua aqui."; projeções com relógio lógico (`Geracao`): escrita atrasada não ressuscita nota selada nem apaga `.md` novo; pasta espelhada indisponível ou bookmark morto viram linha no Perfil; 17 testes focados e suíte 639/0 em 05/09/2026. **Volta A1 (ADR 2026-09-08s): o arranque honesto.** `TracoApp.swift:13` era `try! DiscoTraco.abrir(emTeste:)` e a versão anterior, quando o disco recusava, caía num contentor EM MEMÓRIA com o app inteiro de pé sobre um caderno vazio — e `Corpus.escrever` apaga do espelho em Arquivos todo `.md` fora da lista que recebe. O arranque não apagava nada sozinho: o estrago exigia UM GESTO seguinte da pessoa dentro do caderno falso (selar, queimar, apagar, importar — qualquer rota que projete o mundo), e é essa distância de um gesto que torna o risco grave em vez de teórico. Agora a recusa é estado (`DiscoTraco.Resultado`), NADA se abre no lugar (sem container não há `RaizView`, sem `RaizView` nenhuma rota do selo toca o espelho, e `Ferias`/`Revisoes`/reconciliações da Ilha não correm), e `ArranqueFalhouView` diz o que houve, onde o conteúdo está (contagem MEDIDA dos `.md` do espelho) e o próximo ato ("Tentar abrir de novo", 44,0 pt medidos na árvore de AX; se recusar de novo, diz isso). Portão do `try!` irmão do portão do movimento, lista congelada MEDIDA e reproduzível (`grep -rn 'try!' Traco/ TracoWidget/ | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//'` → 9 em `main`, 8 aqui; o `grep` cru conta comentário e não serve de prova) com julgamento caso a caso e prova do vermelho. Suíte 934/0 em 152 suítes e build sem aviso em 08/09/2026 no `6033B043`; tela fotografada com o `default.store` do App Group trocado por um diretório (erro real `loadIssueModelContainer`), e a nota reapareceu intacta ao restaurar **Volta M1 (ADR 2026-09-09f): a porta que a A1 tinha fechado, aberta.** A R1-C mediu o mesmo store pré-08u em três builds e achou o `main` parando no arranque honesto; a causa medida no aparelho é `NSCocoaErrorDomain 134504` ("Cannot use staged migration with an unknown model version"), porque `TracoSchemaV2/V3/V4` apontavam para a CLASSE VIVA e o checksum da V4 andou junto com o `origemRaw` da 08u — o caderno do autor guarda o checksum do dia em que foi gravado e deixou de casar com o plano. V2/V3/V4 passam a declarar cópias CONGELADAS, a V5 é a única com as classes vivas, e o estágio V4→V5 é leve. Provado no `1A46B6D3`: com o caderno pré-08u plantado no App Group, o `main` mostra "O Traço não abriu o seu caderno" (`aparelho-main-recusa.png`) e o build desta volta abre a lista com as SETE notas — `caderno antigo 1,2,4,6,7`, a Expressiva queimada com o sentido "linha 5" e a Expressiva trancada (`aparelho-conserto-notas.png`); o store ficou byte a byte idêntico depois da recusa (`cmp` limpo), a rede da A1 não destruiu nada. **Lacuna da R1-C fechada** e portão novo: `CadernoAntigoAbreTests` abre um store REAL de cada versão (`TracoTests/Fixtures/`) — o que os testes de `DiscoTraco`, que injetam closures, nunca fizeram; o `caderno-v5-origem` fica vermelho se alguém mudar a classe viva sem abrir a V6, provado com um atributo de sonda. **Volta M1-B (adendo da mesma ADR): "cada versão" passou a ter cada versão — e apareceu uma 1.0.0 que ninguém tinha visto.** O checksum de uma versão depende só da FORMA das entidades, não da plataforma nem do módulo: medido, a cópia congelada V4 deste repo gravada num executável macOS produz `ImY8W7hR8jJH+xS4hddeRW+haXr9qO3ZDVx/J9RS3sI=`, **o mesmo checksum byte a byte** do `caderno-v4-pre08u` que o build iOS `8d9ce62` gravou — instrumento calibrado contra um caso conhecido, e por isso as formas antigas da `Nota`, extraídas do `git` POR SCRIPT (não transcritas), gravam cadernos genuínos de cada versão. Com eles apareceu o achado: a `1.0.0` teve DUAS formas — `b7fbc3e` (31/08 08:27) e `fea00dd` (31/08 16:31), que pôs `queimada`/`queimadaEm`/`minutosEscritos`/`sentido` na `Nota` **sem abrir versão**, o mesmo pecado da 08u cometido oito horas depois de o schema nascer (`ZaCSxtyZ…` virou `c2qnFksOJhh…`). O conserto da M1 congelou só a segunda e deixou a primeira ÓRFÃ: o caderno daquela manhã continuava recusando abrir no build já consertado. `TracoSchemaV0` (rótulo `0.9.0`, porque o CoreData casa por checksum e não por número) e o estágio `V0→V1` fecham isso. O portão passa a ter SEIS cadernos, um por versão do plano, com contagens distintas (3, 4, 5, 6, 7, 7) e conferência de texto, selo, queima, sentido e origem `.autor`; `oPortaoTemUmCadernoPorVersao` compara a lista com `TracoMigracao.schemas.count`, de modo que quem abrir a V6 sem gravar o caderno dela vê vermelho antes de a mudança chegar ao aparelho do autor — provado tirando o `TracoSchemaV0` do plano (contador 6 contra 5 e `loadIssueModelContainer` no `caderno-v0-b7fbc3e`). A `3.0.0` NUNCA esteve na mão de ninguém — nasceu e foi superada dentro de `9ad639e` — e o nome do arquivo (`caderno-v3-degrau`) diz isso. Suíte integral 977/0 em 158 suítes, 0 warning, na árvore MESCLADA com o `main` de 09/09. | Captura da linha do espelho no aparelho (sem rota `traco://` para o Perfil; pede maestro); avisos/haptics fora do contrato de commit; garantia física do queimar; sincronização contínua; da A1: as quatro dívidas de `try!` com dado de fora foram FECHADAS pela volta B1 (ADR 2026-09-09o), e o achado é que nenhuma delas podia explodir: `Corpus` codifica um `String` e `Sessao` um `[String]`, sempre JSON e sempre UTF-8 válidos, com o `!` do dicionário coberto pelo filtro três linhas acima — provado com NUL, U+FFFF, emoji, U+2028/2029 e aspas atravessando os quatro caminhos e voltando idênticos; e em `FonteNotas`/`PraticaTrabalho` o `try!` guardava a porta errada, porque `JSONSerialization.data(withJSONObject:)` com objeto inválido NÃO lança, levanta `NSInvalidArgumentException` e mata o processo por baixo de `try!`, `try?` e `do/catch` igualmente (medido: com o `try!` de volta o teste derruba o runner sem sequer aparecer como falha) — o guarda passa a ser `isValidJSONObject`, a lista congelada do portão desce de 8 para 6, e `ConferenciaTrabalho.regex(_:)`, literal só enquanto todo chamador for literal, ganhou portão próprio que fica vermelho no commit que criar a dívida; suíte 996/0 em 161 suítes no `34CC3F94`; e a recuperação oferecida é só tentar de novo: o Traço não repara arquivo que não conseguiu ler, e importar o espelho de volta como caminho de recuperação é volta própria; o pior caso da frase do meio (espelho VAZIO) foi fotografado em `a1-08-espelho-vazio.png`, mas a leitura FALADA do VoiceOver não foi exercitada e não será: voz e VoiceOver são proibidos no Traço (o áudio do simulador sai pelo Mac do autor) — a acessibilidade desta tela se prova por árvore de AX e captura |
| Qualidade efetiva transversal da IA | ADR07a e QUALIDADE-IA.md: contratos estruturados, evidência por IDs, correções de contexto/transformação e Combinar em avaliação. 137 testes selecionados passaram em 07/09 às 14:16 UTC no candidato daquele momento; base viva encontrou falhas semânticas e de atribuição. **08/09 (ADR 08q, volta Q): a lacuna "nenhuma operação medida com Grok" FECHOU** — 16 operações × 6 casos × 3 execuções no aparelho do dono com a conta ligada. `conferir`, `padroes` e `conferirTentativa` atenderam 6 de 6; sete rotas foram cortadas por qualidade — cinco sem substituto medido (`ecos`, `calibragem`, `recordar`, `instigar`, `contrapor`) e duas com conserto nomeado (`responder`, `responderNasNotas`); **20** falhas de transporte (não 12: 11 + 6 + 3), todas nas quatro rotas de `grok-4.6`, pelo teto de 90 s — que a ADR 2026-09-08r mediu e substituiu. Três casos de `responderNasNotas` mediram rota que a produção não usa e foram anulados: defeito do instrumento, não do provedor. **08/09 (ADR 08r, volta Q-B): a lacuna de DISPONIBILIDADE fechou** — o teto virou um valor medido (`Grok.tetoTrabalho`, 240 s), e a remedição dos nove casos que carregavam as 20 falhas devolveu **0 falhas de transporte em 30 chamadas**, no aparelho do dono com a conta ligada (`prova/qb-teto-avaliacoes.jsonl`). O candidato da 08q foi corrigido para `acdfcb4`. Continua aberto, e agora nomeado: 3 de 15 execuções de `prepararPratica` voltam HTTP 200 com conteúdo completo e são recusadas pelo NOSSO contrato (`PraticaTrabalho.validar`) — defeito de conteúdo, com estado honesto por pedido na tela, sem executor novo a medir. **08/09 (ADR 08p, volta Q-C): a recusa do nosso parser deixou de ser um `nil`.** `PraticaTrabalho.Recusa` redige a guarda que reprovou — categoria, campo e medida, sem bruto nem credencial — e a sonda a grava em `recusasDaPreparacao`. Remedidos os dois casos no aparelho do dono com a conta ligada: **18 execuções, 18 HTTP 200 completos de `grok-4.6`, 5 recusas e as CINCO na mesma guarda** — `Prova.vaza(criterio, alvo: exemplo)` (`prova/qc-recusa-avaliacoes.jsonl`). **O defeito é NOSSO:** a régua veio do Recordar, onde o `alvo` é a resposta; aqui o `alvo` é o exemplo, que por contrato é outro caso, e ela reprova o vocabulário estrutural da própria tarefa — quadrigrama medido “a dependencia ainda aberta”, palavras que estão na instrução do autor antes de estarem no exemplo. O parser NÃO foi alargado nesta volta, por decisão: **fica no RUMO**, com a régua nos dois sentidos a escrever antes do conserto. **08/09 (volta Q-D, correções do re-G3 na mesma ADR, agora `08p`): o registro da recusa deixou de carregar o trecho** — guarda posição (qual critério, qual palavra do exemplo, de quantas) e ORIGEM (quantas das palavras do trecho o autor já tinha escrito no pedido), conferível pela `entrada` que a sonda já grava. Remedição no aparelho do dono com a conta ligada (`1B7E0E63`): **12 execuções, 12 HTTP 200 completos de `grok-4.6`, 4 recusas — 3 por vazamento, todas com as QUATRO palavras já escritas pelo autor, e 1 por limite** (`enunciado tem 1582 caracteres e o teto é 1500`), que derruba o "as onze outras guardas não dispararam" da redação anterior (`prova/qd-origem-avaliacoes.jsonl`). Somando as duas remedições: 30 execuções, 9 recusas, 8 por vazamento e 1 por limite. As **quatro** recusas da primeira corrida continuam SEM evidência individual e ficam indeterminadas. O estado honesto por pedido (`praticaIndisponivel`) e a tabela `Politica` seguem sem mudança. **08/09 (ADR 08w, volta Q-H): a recusa nomeada e o contrato do ajuste da V17 passam a viver na mesma função.** `lerPreparacao` conhece `comMudanca`, e a chave `mudanca` — que num ajuste é do contrato (ADR 08j) — recusa por nome em vez de `nil`: ausente vira `chavesForaDoContrato(faltando: ["mudanca"])`, fora do tipo `campoNaoTexto`, vazia `campoVazio`, acima de 400 `campoAcimaDoTeto`, e o vazamento ganha caso PRÓPRIO (`mudancaVazaOExemplo`) porque chamar de "critério" um campo que não é critério seria a recusa mentindo sobre qual guarda reprovou. A regra da V17 fica inteira (qualquer dessas quedas derruba a preparação toda) e a rota remota passa a usar o `Grok.tetoTrabalho` medido na 08r, não o `90` suposto. Build sem aviso e **957 testes, 956 passados, 0 falhos, 1 pulado** na árvore MESCLADA e **958 / 957 / 0 / 1** depois de trazer também o `main` que a V13 avançou durante o trabalho (`34CC3F94`) — os testes da V17 sobre `mudanca` e os da Q sobre `Recusa` passam juntos, que é o que ninguém tinha medido (`ferramentas/orca/q-h-reconciliacao.md`). **09–10/09 (ADR 09t, volta MERGE-Q34): as três medições em lote e a tela entraram em `main` juntas.** LOTE-1 (76 execuções), LOTE-2 (114, as mesmas fixtures em `4.5` e `4.6`) e LOTE-3 (114 numa janela só, 00:38:42Z–00:54:55Z de 10/09 UTC, uma instalação, `4.3` e `4.5`) estavam presos em worktree, com os dois relatórios de revisão citando `../lote-ia-09c/prova/…`; agora a prova mora no repositório (`prova/lote09*.jsonl`, `prova/lote09*-janela.log`, `prova/q3-*.json`, `prova/q4-*.json`). O LOTE-3 mediu os consertos da Q3-B e da Q4-B e os dois re-G3 **reprovaram de novo**: `responderNasNotas`, `instigar` e `contrapor` seguem `indisponivelPorQualidade` — o que mudou foi o MOTIVO na tela do autor, que acusava defeito já derrubado. A meia-recusa da conversa acabou (**6 de 6** calculam R$ 3.354, contra 0 de 3 antes) e o que sobra é a conta pela metade (**6 de 6** omitem os R$ 2.646); o `instigar` devolveu o método e o degrau do AUTOR (**3/3** nos dois modelos) e o degrau 4 parou de repetir o degrau 0 (**3/3**), e o que sobra é a pergunta vaga no texto curto; o `contrapor` não fabrica mais evidência, e o que sobra é renda que a nota não declara e contraponto que não chega onde a nota dá matéria. `medidaEm` das três passa a 10/09/2026, e o teste do Perfil passa a ler a linha pelo caminho REAL da tela em vez de contar os dois grupos. Lacuna que segue ABERTA para `instigar` e `contrapor`. **10/09 (ADR 09v, volta Q3-C): `responderNasNotas` VOLTOU, e as cortadas caem de sete para seis.** O LOTE-09d correu a MESMA fixture nos dois modelos, na mesma janela (02:27:24Z–02:33:03Z), com UMA instalação e a conta conferida ligada nas três fumaças: **`grok-4.5` 21 de 21, `grok-4.3` 12 de 21** — o padrão global calcula os R$ 3.354 e não diz os R$ 2.646 em 3 de 3, e expõe os 18 inscritos contra a sala de 15 sem o próximo ato em 3 de 3. A linha de base ficou intacta nos DOIS (conversa, prazo, sem lastro e instrução hostil, 6 de 6 cada) e `escreveuRotuloInterno` é false nas 42 saídas, lidas inteiras. A decisão que isto abre vale para as próximas: **o modelo se escolhe POR OPERAÇÃO** (`Grok.modelo(daRota:)`, `Sabia.modeloMedido = "grok-4.5"`), com o padrão global INTOCADO em `grok-4.3` — porque no LOTE-3 o 4.5 foi PIOR no `contrapor` (renda inventada 3 de 3 contra 1 de 3), e um vencedor único consertaria uma rota e estragaria outra. A precedência é `sonda → rota → padrão global`, guardada por portão que falha fechado, para que `TRACO_AVALIAR_MODELO` continue medindo esta rota. **A operação voltou a estar disponível às 08h20min37s de 10/09/2026** (11:20:37Z, o install único), e o primeiro cartão com resposta real na tela do `B91C8DEF` é de **11:22:2xZ**: *"…520 × 6,45 = R$ 3.354 no total em reais… Você reservou R$ 6.000 para a viagem; sobram R$ 2.646 em relação a esse teto"* (`ferramentas/orca/q3c-01-cartao-com-a-sobra.png`), com o Perfil listando "responder nas Notas" entre as que a conta Grok atende (`q3c-03-perfil-saiu-da-lista.png`). Conta ligada às 11:20:25Z, 11:20:38Z e 11:23:17Z, 12 modelos nas três. Achado do instrumento que quase invalidava a medida: a árvore carregava um prompt EDITADO ÀS 23:34:35, depois de a janela fechar às 23:33:06 — o texto comitado é o extraído do binário medido (`Traco.debug.dylib` `57d02df3…`), conferido byte a byte. Lacuna que segue ABERTA: `instigar` e `contrapor` não voltaram, e o `grok-4.3` continua reprovando esta rota — se o padrão global voltar a ela por descuido, o autor recebe a conta pela metade. **10/09 (ADR 09w, volta Q3-D): a resposta que voltou parou de cortar calada.** A captura que fechou a 09v terminava a frase em `"(A nota"` — parêntese aberto — e nada dizia que havia mais; o `ScrollView` do cartão sempre rolou, faltava o AVISO. A escolha foi por MEDIDA e não por gosto: a resposta real na tela tem **419 grafemas**, as 18 corridas da mesma pergunta deram **203–568** (mediana 384), os 220 pt do cartão cabem ~330 em `medium` — e em AX5 os 568 medem **3.120,7 pt** de conteúdo na mesma janela (15 páginas), onde só caberiam **40 grafemas**. Logo nem subir o teto nem descer o teto de 900 do prompt calam o corte em AX5 (e descer o de 900 anularia a medida que a 09v acabou de fazer): o teto FICA em 220 e entra o **sinal de sobra** — degradê no pé enquanto há sobra, mais a palavra *continua*, que some sozinha quando a pessoa chega ao fim. O padrão não é novo: estava copiado à mão no `CartaoAnaliseView` desde o G4 da V8, e virou `Traco/Componentes/SinalDeSobra.swift`, usado nos DOIS `ScrollView` com teto do cartão. A invariante fica guardada onde todos passam (`todoTetoDoCartaoTemSinalDeSobra`: todo `.frame(maxHeight:)` de `NotasView` tem um `.sinalDeSobra(`). Provado na MESMA pergunta, no mesmo aparelho da conta, com UMA instalação e `contaGrokLigada=true` nas quatro fumaças de 12:47:24Z a 12:59:37Z (`ferramentas/orca/q3d-04-cartao-medium.png`, `q3d-05-cartao-ax5.png`, `prova/q3d-fumaca-*.jsonl`); 1022/1022 e a UI verde em duas passadas no `34CC3F94`. Lacunas ABERTAS que esta volta MEDIU e não consertou: em AX5 o cartão inteiro transborda a tela pela linha *"Foram junto:"*, que não tem teto; e em AX5 a topbar da Página fica em y=-371 pt e não volta com rolagem, deixando "Notas" e "Concluir" inalcançáveis a quem usa letra grande. **10/09 (ADR 09z, volta MERGE-Q3D): `responderNasNotas` chegou a `main` e a lista de indisponíveis do Perfil caiu de sete para SEIS** — a primeira das sete a voltar, por medida pareada (ADR 09v). Na mesma mescla o cartão CONTA e o aviso `Politica.semProvedor` passaram a falar a língua do autor: sem data, sem "medida" e sem o nosso plano de obra, guardados por dois portões que leem o texto inteiro pelo caminho da tela e caem por mutação. A lacuna que fica é a mesma: **seis** operações continuam sem executor, e o que as tira da lista é medida nova, não texto novo. **10/09 (ADR 10c, volta INSTIGAR): a lacuna do `instigar` deixou de ser o PEDIDO e passou a ser o MODELO e a SUPERFÍCIE.** A Q4-C tinha escrito o conserto e não o aplicado, para o binário comitado não divergir do medido; esta volta o aplicou e o mediu com os DOIS braços no MESMO dylib (`sistemaInstigarBase` por ambiente, `pedidoInstigarSHA256` em toda linha do JSONL), quatro braços por janela, UMA instalação, conta conferida ligada nas quatro fumaças de cada uma. **A cobrança condicionada à matéria dá o ganho do LOTE-5 sem o preço do LOTE-5:** o texto magro pede o quando em **6 de 6** (2 modelos × 3) contra **4 de 12** da base nas duas janelas juntas, Fisher p = 0,011, e as perguntas ancoradas na nota ficam na faixa da PRÓPRIA base (**88 a 93%**), onde a promoção incondicional as tinha derrubado a 76% e 89%. **Contra a base o ganho de 1 ponto nas ancoradas é RUÍDO** (p = 0,54, e a base contra ela mesma dá 0,37, medido rodando a 2ª corrida da base que estava comitada e não fora lida) e a contagem ABSOLUTA de ancoradas cai, 55 para 52 no grok-4.3 — o que se afirma é que o preço do LOTE-5 não voltou, não que o candidato ancore mais. O que sobra é limite MEDIDO do `grok-4.3`, achado pelo **caso cego escrito nesta volta**: numa nota que NEGA por escrito o quando e o "dar certo", o 4.3 pergunta os dois assim mesmo em 3 de 3 e o `grok-4.5` não pergunta em 3 de 3. A 2ª redação, que mandava a perna só entrar se a nota a tivesse deixado em aberto, foi **medida e descartada**: consertou o cego do 4.3 (6 falhas → 1) e quebrou o controle (texto magro 3/3 → 1/3, ancoradas 93% → 74%) — as duas falhas são simétricas e cada uma esconde a outra. `instigar` **continua `indisponivelPorQualidade`**, agora por outro motivo e com a frase da tela dizendo o defeito de hoje; pelas §14/§15 nenhuma operação volta sem a superfície aprovada no G4. Junto veio uma guarda que serve às dezesseis: `Grok.Diagnostico.bruto` no portão por onde todas as rotas passam, para que a pergunta que o nosso parser derruba não some sem rastro (`prova/instigar-lote/`, `ferramentas/orca/instigar.md`). **10/09 (ADR 09s + emenda à 09i, volta Q4-C): o LOTE-5 mediu o conserto do `semRetorno` e os dois de prompt, e a rota que calava com HTTP 200 parou de calar.** Mesma fixture do LOTE-3 (`q4-instigar-contrapor-casos.json`, SHA `ed9267c1…`), 12 casos × 3 repetições em `grok-4.3` e `grok-4.5`, **72 execuções numa janela só** (10:55:25Z–11:10:47Z no `B91C8DEF`, uma instalação por cima, `ContaGrok.ligada` true antes, depois do install e no fim). **`Falha.semRetorno` com HTTP 200 e conteúdo completo: 2 → 0.** A causa era NOSSA e estava no `limpo(_:)` do `parseContraparte` — a guarda que protege apagando devolvia `nil`, e `nil` era "não li"; agora `nil` é não li e a `Contraparte` VAZIA é li e a guarda não deixou nada passar, com o irmão `parsePerguntas` consertado no mesmo lugar e frase própria na tela (`Sabia.nadaPassouNaGuarda`). **Renda que a nota não declara: 4 → 0** (`"renda"` entrou em `fatoQueEleNaoDeu`, o que só ficou barato depois do desfecho novo). **Texto magro pedindo QUANDO: 1/6 → 6/6**, e as três pernas da fixture 1/6 → 6/6. O conferidor mecânico **INALTERADO** do LOTE-3 (`lote-ia-09c-guardas.py`) foi de **68/72 para 72/72**. A sonda passou a nomear a guarda que apagou (`guardasQueApagaram`), e a única execução que a acionou mostra o conserto certo: uma chave caiu, as outras duas sobreviveram e o autor recebeu contraponto em vez de silêncio. **Defeito OPOSTO comprado na mesma janela, medido e nomeado:** promover a cobrança do QUANDO fez o `grok-4.3` devolver, em 1 de 30 repetições com matéria (0 de 30 na base, nenhuma no `grok-4.5`), o gabarito nu sem nada da nota do autor — e o extremo é a ponta, não o tamanho: contando pergunta a pergunta, a cláusula promovida ACRESCENTOU em vez de substituir, e as perguntas ancoradas na nota caíram de **96% (49/51) para 76% (48/63)** no `grok-4.3` e de **97% (59/61) para 89% (63/71)** no `grok-4.5`. A alavanca seguinte é condicionar o requisito à MATÉRIA da nota, numa frase; está escrita na emenda e NÃO foi aplicada, porque o binário medido é o binário comitado. A janela é também uma comparação de UMA alavanca e, nela, o `grok-4.5` é melhor que o `grok-4.3` em `instigar` (0/15 contra 1/15 de repetição genérica; 89% contra 76% de ancoradas) — insumo pareado para a escolha de modelo por operação. Lacuna que segue ABERTA: as três continuam `indisponivelPorQualidade` — falta a leitura de mérito de quem não escreveu os casos — e a captura de cartão com resposta real segue sem existir. **10/09 (emenda à 09s, volta Q4-D): a mesma guarda estava viva uma função adiante, e agora não está.** `Sabia.vestir` — `.grokDepoisBordo`, rota que o autor toca hoje em "Vestir tudo", ao contrário das três cortadas — devolvia `nil` tanto para o provedor mudo quanto para o cru que chegou inteiro e o NOSSO contrato recusou (mapa ilegível, lista vazia, dois títulos, ou menos rótulos que os blocos pendentes), e `Sessao.vestirTudo` escrevia "a sábia não respondeu. o texto ficou como estava." sobre um HTTP 200. Agora `[]` é o terceiro desfecho e a tela diz `Sabia.nadaVestiu` — frase própria porque `vestir` MEMOIZA e "peça de novo" seria falso ali. A sonda passou a nomear qual das duas quedas foi (`vestir · mapa fora do contrato` / `· mapa menor que os blocos pendentes`), que é justamente o que os **6** `Falha.semRetorno` de `vestir` em 07/09 (`prova/qualidade-ia-contexto-vestir-20260907.jsonl`) não sabiam dizer. Vermelho antes: 5 issues em 2 dos 9 testes; verde depois: 1028 testes em 164 suítes, build LIMPO. Os 12 parsers do app foram relidos: a espécie tem 5, dois já consertados na 09s, este agora, e os dois que sobram não produzem frase de tela. Lacuna ABERTA e com dono: `RespostaNotas.interpretar` tem o mesmo defeito e `NotasView` escreveria a mesma mentira — só não escreve porque `responderNasNotas` está cortada; o conserto é da volta que a descortinar. Lacuna ABERTA: `nadaVestiu` não tem captura — a rota exige `Sabia.disponivel`, falso no aparelho de trabalho, e o da conta estava com outra volta. **10/09 (ADR 09x, volta Q4-E): o caso cego do `contrapor` foi escrito, medido e REPROVOU — e a lacuna que fecha é outra.** A lacuna "falta a leitura de mérito de quem não escreveu os casos" FECHOU: dois casos cegos de espécie nova — material que NEGA o que se pediria, medindo os dois polos simétricos (recusa covarde e invenção) — entraram em `prova/q4c-contrapor-cego-casos.json` com os seis do LOTE-5 byte a byte, e a janela `lote09f` (10/09 13:39:13Z–13:49:54Z no `B91C8DEF`, UMA instalação, `ContaGrok.ligada` true nas três leituras, 48 execuções, zero erro de transporte) correu os dois modelos. **Os dois reprovam, em lados opostos:** no `grok-4.3`, que é o modelo do app, `q4-contrapor-tudo-ou-nada` rep. 2 devolveu os TRÊS campos vazios sobre HTTP 200 com `guardasQueApagaram` vazio — a recusa é do modelo, e aquela fixture escreve que os três vazios reprovam —, mais dois `contra` vazios contra ZERO no LOTE-5; no `grok-4.5` a base saiu perfeita (18/18, 0 campos vazios em 54) e o caso cego reprovou **3 de 3**, com os três `foraDaLista` propondo o ensaio que a nota fecha por escrito, um deles dizendo "cópia restaurada dos dados reais" contra "não tenho ambiente de teste com os dados reais". **A escolha de modelo por operação (09v) não salva esta rota:** cada modelo falha de um lado. `contrapor` fica `indisponivelPorQualidade` com o motivo de 10/09 e o conserto renomeado — o pedido tem de dizer que a restrição escrita fecha também a ALTERNATIVA, não só o argumento. **A CAPTURA DO CARTÃO REAL EXISTE** (`ferramentas/orca/q4e-02-cartao-real-na-conta.png`, 13:49:54Z): a rota funciona de ponta a ponta, os três campos desenham — e o cartão que o autor veria carrega o defeito, com "a partir de backup completo" afirmando um backup que a nota não dá. **A lição de instrumento é a maior:** o `9 de 54` e o `contra 0` do LOTE-5 eram UMA amostra, não uma propriedade — mesmo prompt e mesmo parser conferidos byte a byte contra a árvore medida, e a remedida devolveu `10 de 54` com três `contra` vazios. Um número que caiu a zero em 18 execuções não é zero: é "não apareceu em 18". Lacuna que segue ABERTA: `contrapor` sem executor, agora com o conserto nomeado e a régua escrita — duas janelas, não uma. **10/09 (ADR 09/10c, volta Q4-F): as DUAS tentativas do regime, numa alavanca só, e a rota NÃO volta.** Mesma fixture do LOTE-6 byte a byte (SHA `da012e21…`), 8 casos × 3 × 2 modelos, no aparelho da conta `34CC3F94`, uma janela e uma instalação por tentativa, `cmp` do `.debug.dylib` nos dois lados, conta ligada nas quatro fumaças de cada janela. A regra nova diz que **o que ela já descartou, recusou ou disse não ter é DADO** — não volta como proposta, nem no `foraDaLista`, nem como etapa antes — e que **quanto mais saídas ela fecha, mais o contraponto se aperta no que SOBRA**, que é o antídoto da mudez que a Q4-C comprou. O que ela derrubou, medido: os **três campos vazios sobre HTTP 200 do `grok-4.3` foram a ZERO** e lá ficaram em 96 execuções — e a leitura de `guardasQueApagaram` confirma que a rota já herdara o conserto da 09s, logo o silêncio era do modelo, não da nossa guarda; o `grok-4.5` subiu de 0/3 para 2/3 no caso cego que o derrubava. O que ela NÃO derrubou: **nenhum dos dois passa os DOIS cegos em 3 de 3**, e a escolha por operação (09v) não salva — o melhor de cada um falha no cego do outro. O defeito que sobra tem nome: para o recurso que a nota diz NÃO ter, o modelo oferece um **substituto** (ensaio sintético, cópia mascarada, recorte representativo). Junto veio o portão de instrumento que faltava — **`Grok.Diagnostico.retornoBruto`**: sem ele, "os três vazios são do modelo" era inferência, não prova. A linha do Perfil perdeu a metade que a medida não sustenta mais ("e às vezes não devolve nada": 1 em 24, depois 0 em 48 e 0 em 48) e foi fotografada em `large` às 16:52:20Z. Lacuna que segue ABERTA: `contrapor` e `instigar` continuam sem executor, e a régua agora pede os DOIS cegos em 3 de 3 — prova/lote09g/, prova/lote09h/, prova/q4f/q4e-04-cartao.png e ferramentas/orca/q4f-contrapor.md. **10/09 (ADR 10d, volta Q4-F/LOTE-9 + G3): a alavanca não era o PEDIDO, era o ESQUEMA DA SAÍDA — e quem reprovava a operação éramos nós.** Depois de as duas tentativas de redação do pedido falharem (LOTE-7 e LOTE-8), a terceira alavanca não pede nada de novo: o `corpoContrapor` é BYTE A BYTE o mesmo nos dois braços e o que muda é a FORMA que a API aplica — `fechadas` e `dependeDe`, duas chaves de FATO, com `fechadas` PRIMEIRO, porque o modelo escreve da esquerda para a direita e a ordem é propriedade do gerador, não súplica. Os dois braços no MESMO dylib (o antigo por ambiente), pedido por SHA em toda linha, `prova/lote09i/`. **A guarda que decidia por cima dela — `Sabia.dependeDoQueElaFechou` — foi medida junto e RETIRADA: 5 disparos, 1 acerto, 4 erros, e os quatro são a mesma espécie** — a proposta usava de outro jeito um recurso que a autora JÁ TEM ('pausar a matrícula' da academia que ela tem). Casar palavra não separa *ela não tem X* de *ela tem X e a proposta usa X de outro jeito*, e ler a relação é juízo, que devolveria a autocertificação. A função fica sem chamador com o teste que prova por que ela erra, e a dívida está nomeada: **falta um campo que diga se o recurso vem DE FORA do que ela tem, e que seja FATO**. **O G3 conferiu a recontagem e cortou uma seta:** o `bruto` guarda o que o modelo escreveu antes da guarda, o pedido nunca menciona o join e os `requisitos` da fixture não viajam — a recontagem sem segunda instalação é legítima e reproduz célula a célula. Mas o polo de controle **NÃO SUBIU**: o MESMO braço antigo, com o MESMO pedido (`e4b665fb…`), a MESMA fixture e o MESMO parser deu 16 de 18 no LOTE-8 e 11 de 18 no LOTE-9, e foi de 1/3 a 3/3 num cego entre as duas janelas sem que nada mudasse. **Três tiradas por caso dizem que o defeito não apareceu, não que a forma o fechou** (Fisher p = 0,40; p = 0,17 juntando as duas janelas do braço antigo) — por isso a frase da tela continua a nomear o substituto. Lacuna que segue ABERTA: `contrapor` sem executor, com a régua agora a pedir uma segunda janela antes de qualquer volta, e a segunda porta nomeada (o modelo pode ENCOLHER o `fechadas` que ele mesmo escreve) — `prova/lote09i/`, `ferramentas/orca/q4f-contrapor.md` e `ferramentas/orca/g3-contrapor-responder-ctx.md`. | Matriz Q1–Q8 inteira, atendimento >=9 em cada dimensão e três inferências novas por caso; geração espanhola, feedback observável, fontes, todas as rotas e provedores, privacidade após espera e jornada integrada. Meta integral aberta; regras/testes não substituem essas provas. | **09/09 (ADR 09n, volta Q2-E): `responder` VOLTOU, e a lacuna das sete cortadas caiu para seis.** A DIRETRIZ §10 aprovou a espera, e o "melhor" foi MEDIDO entre os dois candidatos alcançáveis, mesma fixture, mesmo binário, mesmo aparelho: `grok-4.6`/`medium` **12 de 12** casos em 38,3 s de média; `grok-4.20-0309-reasoning` **9 de 12** em 20,2 s — quase o dobro mais rápido e perde assim mesmo (regra genérica inventada como certeza no prazo em conflito, 5+5+5 que não divide, e `(487 caracteres)` vazado no texto do autor). `Grok.modelo` = `grok-4.6` para TODAS as rotas; `modeloTrabalho` e `tetoTrabalho` deixaram de existir (um modelo, um teto). **Dois defeitos que a adoção teria criado foram pegos pela medida, não pela tela:** `grok-4.6` recusa `reasoning_effort: none` (400 em 6 de 6) — e em `classificar` e `vestir` a falha seria CALADA, com o aparelho respondendo pior sem ninguém dizer; e o `timeout: 10` de `AnaliseRemota.classificar`, medido para um modelo que não raciocinava, estourou 3 de 3. Piso `Grok.esforcoMinimo = "low"` e um teto só: remedidas, 9 de 9 em HTTP 200 (`conferir` 3,1–3,6 s, `padroes` 8,5–13,6 s, `classificar` 11,1–16,3 s). A espera virou estado de tela — a palavra do Traço, o segundo que anda e "Parar de esperar" que devolve a pergunta em vez de perdê-la —, provada no aparelho da conta (`ferramentas/orca/q2e-0*.png`). `responder` voltou a responder às **14h01 de 09/09/2026**. **REVERTIDO no mesmo dia** (G3, `ferramentas/orca/revisao-q2-responder.md`): a comparação mudou DUAS alavancas (modelo e `reasoning_effort`) e a triagem dos doze excluiu por nome e posição. **09/09 (ADR 09q, volta Q2-F): a comparação foi refeita com UMA alavanca, e ela NÃO ELEGE NINGUÉM.** Os doze foram triados por frase da API — cinco `imagine` dão `Model not found`, `grok-build-0.1` e as três `grok-4.20` recusam `reasoningEffort` (o parâmetro que o Traço manda em toda chamada, e essa recusa **prova** que ele ia no corpo HTTP) —, sobraram TRÊS candidatos, e os três correram os 12 casos da 08z **mais os 6 cegos do revisor**, três vezes, com `medium` fixo, mesmo binário, mesmo aparelho e **zero instalações**: `grok-4.3` **15 de 18** casos (46 de 54 execuções), `grok-4.5` **17 de 18** (51 de 54), `grok-4.6` **16 de 18** (52 de 54). Nenhum chega a 18; as duas leituras honestas do placar não elegem o mesmo vencedor entre `4.5` e `4.6` (um descumprimento em 54 de distância) e o `4.6` custa **4,2×** a espera (39,9 s contra 9,4 s). O `12 de 12` da 09n veio de UMA corrida e não se repete. **`Grok.modelo` fica `grok-4.3` e `responder` fica cortada** — e a razão é medida: o padrão global reprova `revisor-responsavel-nao-definido`, um caso CEGO, em 3 de 3. A lacuna deixa de ser "falta a comparação pareada" e passa a ser **prompt**: `4.3` e `4.5` afirmam que o relatório é PDF, onde estão sumário e conclusões e até que há tabelas, contra o que `sistemaResponder` já proíbe em prosa (`prova/q2f-*.jsonl`, `ferramentas/orca/q2f-responder.md`). Continua devendo: casos cegos de quem não os escreveu — os seis do revisor são cegos para a 08z e a 09n, não para quem escreveu a 09q. **08/09 (ADR 08z, volta Q2): o conserto nomeado de `responder` foi MEDIDO, e não bastou** — o contrato de sustentação adaptado de `MotorTrabalho.sistema` matou a fabricação de NÚMERO (0 em 108 execuções do candidato: o total de R$ 1.008 e o horário inventado da biblioteca não voltaram), mas o melhor candidato passa **8 de 12 casos**: em 3 de 3 execuções ele supõe o destinatário e o envio que o autor nunca nomeou, e uma execução inverteu a fórmula do cálculo. **O mesmo prompt com `grok-4.6` e esforço `medium` passou 12 de 12 casos, 36 de 36 execuções** — inclusive o caso do prazo em conflito, que resistiu a três prompts com o `grok-4.3`. A hipótese que o conselho chamou de não-causal está medida no escopo desses doze casos. `responder` **fica** indisponível assim mesmo, e por dois motivos ditos: o critério exige a leitura independente de quem não escreveu os casos, e a espera passa de 1,4 s para 36,1 s (77,5 s no pior caso) num cartão da nota. A `Politica` recebe as duas medidas e o conserto renomeado; a troca de modelo NÃO foi implementada — o caminho está escrito na ADR. Provado também que apertar contra a invenção compra o defeito oposto — a primeira variante trocou fabricação por recusa seca e chegou a devolver uma pergunta ao autor, pego pelos pares que mudam só a evidência. A alavanca de modelo/esforço **não foi medida**, e a lacuna que fica é essa (`prova/q2-responder-*.jsonl`, `ferramentas/orca/q2-responder.md`).
| Preservar escrita, importação e restauração | ADR05h revisado (513 testes); ADR05s: auditoria rota a rota da `Sessao` — `trancarESair` ignorava a recusa e apagava a página, `salvar` gravava widget/versão/índice antes do `save`, `apagar`/`desfazerApagar` salvavam sem rollback, `importarCorpus` não projetava; tudo passa a commit→projeção, recusa deixa a linha fixa "Não consegui guardar agora. O texto continua aqui."; projeções com relógio lógico (`Geracao`): escrita atrasada não ressuscita nota selada nem apaga `.md` novo; pasta espelhada indisponível ou bookmark morto viram linha no Perfil; 17 testes focados e suíte 639/0 em 05/09/2026. **Volta A1 (ADR 2026-09-08s): o arranque honesto.** `TracoApp.swift:13` era `try! DiscoTraco.abrir(emTeste:)` e a versão anterior, quando o disco recusava, caía num contentor EM MEMÓRIA com o app inteiro de pé sobre um caderno vazio — e `Corpus.escrever` apaga do espelho em Arquivos todo `.md` fora da lista que recebe. O arranque não apagava nada sozinho: o estrago exigia UM GESTO seguinte da pessoa dentro do caderno falso (selar, queimar, apagar, importar — qualquer rota que projete o mundo), e é essa distância de um gesto que torna o risco grave em vez de teórico. Agora a recusa é estado (`DiscoTraco.Resultado`), NADA se abre no lugar (sem container não há `RaizView`, sem `RaizView` nenhuma rota do selo toca o espelho, e `Ferias`/`Revisoes`/reconciliações da Ilha não correm), e `ArranqueFalhouView` diz o que houve, onde o conteúdo está (contagem MEDIDA dos `.md` do espelho) e o próximo ato ("Tentar abrir de novo", 44,0 pt medidos na árvore de AX; se recusar de novo, diz isso). Portão do `try!` irmão do portão do movimento, lista congelada MEDIDA e reproduzível (`grep -rn 'try!' Traco/ TracoWidget/ | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//'` → 9 em `main`, 8 aqui; o `grep` cru conta comentário e não serve de prova) com julgamento caso a caso e prova do vermelho. Suíte 934/0 em 152 suítes e build sem aviso em 08/09/2026 no `6033B043`; tela fotografada com o `default.store` do App Group trocado por um diretório (erro real `loadIssueModelContainer`), e a nota reapareceu intacta ao restaurar **Volta M1 (ADR 2026-09-09f): a porta que a A1 tinha fechado, aberta.** A R1-C mediu o mesmo store pré-08u em três builds e achou o `main` parando no arranque honesto; a causa medida no aparelho é `NSCocoaErrorDomain 134504` ("Cannot use staged migration with an unknown model version"), porque `TracoSchemaV2/V3/V4` apontavam para a CLASSE VIVA e o checksum da V4 andou junto com o `origemRaw` da 08u — o caderno do autor guarda o checksum do dia em que foi gravado e deixou de casar com o plano. V2/V3/V4 passam a declarar cópias CONGELADAS, a V5 é a única com as classes vivas, e o estágio V4→V5 é leve. Provado no `1A46B6D3`: com o caderno pré-08u plantado no App Group, o `main` mostra "O Traço não abriu o seu caderno" (`aparelho-main-recusa.png`) e o build desta volta abre a lista com as SETE notas — `caderno antigo 1,2,4,6,7`, a Expressiva queimada com o sentido "linha 5" e a Expressiva trancada (`aparelho-conserto-notas.png`); o store ficou byte a byte idêntico depois da recusa (`cmp` limpo), a rede da A1 não destruiu nada. **Lacuna da R1-C fechada** e portão novo: `CadernoAntigoAbreTests` abre um store REAL de cada versão (`TracoTests/Fixtures/`) — o que os testes de `DiscoTraco`, que injetam closures, nunca fizeram; o `caderno-v5-origem` fica vermelho se alguém mudar a classe viva sem abrir a V6, provado com um atributo de sonda. **Volta M1-B (adendo da mesma ADR): "cada versão" passou a ter cada versão — e apareceu uma 1.0.0 que ninguém tinha visto.** O checksum de uma versão depende só da FORMA das entidades, não da plataforma nem do módulo: medido, a cópia congelada V4 deste repo gravada num executável macOS produz `ImY8W7hR8jJH+xS4hddeRW+haXr9qO3ZDVx/J9RS3sI=`, **o mesmo checksum byte a byte** do `caderno-v4-pre08u` que o build iOS `8d9ce62` gravou — instrumento calibrado contra um caso conhecido, e por isso as formas antigas da `Nota`, extraídas do `git` POR SCRIPT (não transcritas), gravam cadernos genuínos de cada versão. Com eles apareceu o achado: a `1.0.0` teve DUAS formas — `b7fbc3e` (31/08 08:27) e `fea00dd` (31/08 16:31), que pôs `queimada`/`queimadaEm`/`minutosEscritos`/`sentido` na `Nota` **sem abrir versão**, o mesmo pecado da 08u cometido oito horas depois de o schema nascer (`ZaCSxtyZ…` virou `c2qnFksOJhh…`). O conserto da M1 congelou só a segunda e deixou a primeira ÓRFÃ: o caderno daquela manhã continuava recusando abrir no build já consertado. `TracoSchemaV0` (rótulo `0.9.0`, porque o CoreData casa por checksum e não por número) e o estágio `V0→V1` fecham isso. O portão passa a ter SEIS cadernos, um por versão do plano, com contagens distintas (3, 4, 5, 6, 7, 7) e conferência de texto, selo, queima, sentido e origem `.autor`; `oPortaoTemUmCadernoPorVersao` compara a lista com `TracoMigracao.schemas.count`, de modo que quem abrir a V6 sem gravar o caderno dela vê vermelho antes de a mudança chegar ao aparelho do autor — provado tirando o `TracoSchemaV0` do plano (contador 6 contra 5 e `loadIssueModelContainer` no `caderno-v0-b7fbc3e`). A `3.0.0` NUNCA esteve na mão de ninguém — nasceu e foi superada dentro de `9ad639e` — e o nome do arquivo (`caderno-v3-degrau`) diz isso. Suíte integral 977/0 em 158 suítes, 0 warning, na árvore MESCLADA com o `main` de 09/09. | Captura da linha do espelho no aparelho (sem rota `traco://` para o Perfil; pede maestro); avisos/haptics fora do contrato de commit; garantia física do queimar; sincronização contínua; da A1: as quatro dívidas de `try!` com dado de fora (`FonteNotas`, `PraticaTrabalho`, `Corpus:144`, `Sessao:599`) ficam congeladas e vão ao RUMO — `Analise` é da volta Q e `Trabalho` é da E1, as duas vivas; e a recuperação oferecida é só tentar de novo: o Traço não repara arquivo que não conseguiu ler, e importar o espelho de volta como caminho de recuperação é volta própria; o pior caso da frase do meio (espelho VAZIO) foi fotografado em `a1-08-espelho-vazio.png`, mas a leitura FALADA do VoiceOver não foi exercitada e não será: voz e VoiceOver são proibidos no Traço (o áudio do simulador sai pelo Mac do autor) — a acessibilidade desta tela se prova por árvore de AX e captura |
| Intenção→artefato delegado→ação→evidência→ajuste | Trabalho persistente V4: revisões, origem, ações, relatos e hipóteses; jornada local, calendário e retorno de Markdown exercitados no simulador. ADR 08m: o resultado observado é eixo próprio do relato (`funcionou`/`parcial`/`naoFuncionou`, `nil` = não observado), separado de executar; `cancelada` tem gesto; a orientação seguinte muda pelo resultado com a causa guardada em `Pedido.ajuste(gatilho: .resultadoInformado)`. ADR 08n: cancelar exige os DOIS eixos (`pendente` E não observado) no agregado, nas duas ordens, com `validar()` fechando a rota de importação; na tela o gesto some e diz por quê. ADR 08o: a orientação nomeia a ação de que o resultado veio (`acaoObservada`), e a premissa vem antes do botão. Jornada dos três resultados fotografada no A1DF082C em `large` e AX5, e a orientação nomeando a ação certa no 64F7B8B4 com três ações e três resultados | Versão nascida do relato com provedor real (sem conta Grok no aparelho de teste: o pedido nasce com a causa e falha); qualidade do artefato ajustado; retomada visual de todos os estados |
| Preservar escrita, importação e restauração | ADR05h revisado (513 testes); ADR05s: auditoria rota a rota da `Sessao` — `trancarESair` ignorava a recusa e apagava a página, `salvar` gravava widget/versão/índice antes do `save`, `apagar`/`desfazerApagar` salvavam sem rollback, `importarCorpus` não projetava; tudo passa a commit→projeção, recusa deixa a linha fixa "Não consegui guardar agora. O texto continua aqui."; projeções com relógio lógico (`Geracao`): escrita atrasada não ressuscita nota selada nem apaga `.md` novo; pasta espelhada indisponível ou bookmark morto viram linha no Perfil; 17 testes focados e suíte 639/0 em 05/09/2026. **Volta A1 (ADR 2026-09-08s): o arranque honesto.** `TracoApp.swift:13` era `try! DiscoTraco.abrir(emTeste:)` e a versão anterior, quando o disco recusava, caía num contentor EM MEMÓRIA com o app inteiro de pé sobre um caderno vazio — e `Corpus.escrever` apaga do espelho em Arquivos todo `.md` fora da lista que recebe. O arranque não apagava nada sozinho: o estrago exigia UM GESTO seguinte da pessoa dentro do caderno falso (selar, queimar, apagar, importar — qualquer rota que projete o mundo), e é essa distância de um gesto que torna o risco grave em vez de teórico. Agora a recusa é estado (`DiscoTraco.Resultado`), NADA se abre no lugar (sem container não há `RaizView`, sem `RaizView` nenhuma rota do selo toca o espelho, e `Ferias`/`Revisoes`/reconciliações da Ilha não correm), e `ArranqueFalhouView` diz o que houve, onde o conteúdo está (contagem MEDIDA dos `.md` do espelho) e o próximo ato ("Tentar abrir de novo", 44,0 pt medidos na árvore de AX; se recusar de novo, diz isso). Portão do `try!` irmão do portão do movimento, lista congelada MEDIDA e reproduzível (`grep -rn 'try!' Traco/ TracoWidget/ | grep -vE '^[^:]+:[0-9]+:[[:space:]]*//'` → 9 em `main`, 8 aqui; o `grep` cru conta comentário e não serve de prova) com julgamento caso a caso e prova do vermelho. Suíte 934/0 em 152 suítes e build sem aviso em 08/09/2026 no `6033B043`; tela fotografada com o `default.store` do App Group trocado por um diretório (erro real `loadIssueModelContainer`), e a nota reapareceu intacta ao restaurar  **ADR09y (P0-CRLF, 10/09): a porta de entrada apagava o arquivo do autor sem ter lido o que havia nele.** `Corpus.importarComEstado` devolvia `contemProtegida` e o coletor remontava o portão do lado de fora (`podeRetirar = !contemProtegida`) antes do `FileManager.removeItem`. Seis formas de `.md` mediam APAGAR em `main`, refeitas em harness com as linhas verbatim dos dois lados: tudo em CRLF (o regex do cabeçalho exige `\n` e dá zero casamentos — o arquivo inteiro virava UMA nota `origem: autor` e o corpo SELADO entrava), `\r` só na linha do estado (0 de 88 caracteres lidos), `\r` só na linha da origem (`origem: modelo` virava AUTOR, porque `"\r\n"` é UM `Character` e `split(separator: "\n")` não separa), arquivo misto (o corpo selado absorvido pela nota sã), e — **sem um único `\r`** — **prosa do autor antes do primeiro cabeçalho: 12 de 104 caracteres lidos, o resto sumia calado e o arquivo era apagado**, que é o caso que renomeou a volta e não precisa de Windows nenhum. A invariante passou a ser COBERTURA DE DELIMITAÇÃO e ela é um NÚMERO: `importarComEstado` devolve `(itens, podeRetirar, consumido)`, onde `consumido` é a fração dos caracteres com tinta que caiu DENTRO de um bloco importado — não a que virou nota — e `podeRetirar` é `lidos == tinta`; **qualquer `continue`** deixa a conta curta sozinho, por `lidos += comTinta(bloco)` ser a última instrução do laço, e o portão sai de quem chama. **O G3 derrubou a alegação mais forte:** a conta NÃO fecha para descarte que consome sem delimitar — o teto de 140 grafemas da ADR 08h, sem um `continue` novo, importou 140 de 699 caracteres com `consumido = 1,00` e apagou o arquivo, e `dominio`/`recordada`, que o app escreve e o importador nunca lê, somem com a conta dizendo 100% (a tinta do cabeçalho é creditada sem virar nota: 659 de 699 na mesma nota). Portão que não enxerga falha fechado: `Corpus.cabecalhos(_:)` conta a mesma forma por `\.isNewline` e, quando a contagem difere da do regex, nada entra como do autor e nada se apaga — mas quando AS DUAS contagens são cegas ao mesmo cabeçalho malformado o arquivo cai no ramo `hits.isEmpty`, que é fail-open e já era assim em `main` (dívida P0-SELO-CEGO, com recomendação medida de uma linha). 2 testes novos (a tabela A–G e a irmã que NÃO acusa) | Captura da linha do espelho no aparelho (sem rota `traco://` para o Perfil; pede maestro); avisos/haptics fora do contrato de commit; garantia física do queimar; sincronização contínua; da A1: as quatro dívidas de `try!` com dado de fora foram FECHADAS pela volta B1 (ADR 2026-09-09o), e o achado é que nenhuma delas podia explodir: `Corpus` codifica um `String` e `Sessao` um `[String]`, sempre JSON e sempre UTF-8 válidos, com o `!` do dicionário coberto pelo filtro três linhas acima — provado com NUL, U+FFFF, emoji, U+2028/2029 e aspas atravessando os quatro caminhos e voltando idênticos; e em `FonteNotas`/`PraticaTrabalho` o `try!` guardava a porta errada, porque `JSONSerialization.data(withJSONObject:)` com objeto inválido NÃO lança, levanta `NSInvalidArgumentException` e mata o processo por baixo de `try!`, `try?` e `do/catch` igualmente (medido: com o `try!` de volta o teste derruba o runner sem sequer aparecer como falha) — o guarda passa a ser `isValidJSONObject`, a lista congelada do portão desce de 8 para 6, e `ConferenciaTrabalho.regex(_:)`, literal só enquanto todo chamador for literal, ganhou portão próprio que fica vermelho no commit que criar a dívida; suíte 996/0 em 161 suítes no `34CC3F94`; e a recuperação oferecida é só tentar de novo: o Traço não repara arquivo que não conseguiu ler, e importar o espelho de volta como caminho de recuperação é volta própria; o pior caso da frase do meio (espelho VAZIO) foi fotografado em `a1-08-espelho-vazio.png`, mas a leitura FALADA do VoiceOver não foi exercitada e não será: voz e VoiceOver são proibidos no Traço (o áudio do simulador sai pelo Mac do autor) — a acessibilidade desta tela se prova por árvore de AX e captura. **ABERTO E GRAVE, medido pela R1-C em 09/09/2026:** caderno gravado ANTES da ADR 08u não abre mais. O `Nota.origemRaw` entrou como migração leve dentro do `TracoSchemaV4`, mas o `TracoSchemaV4` aponta para a classe viva e o checksum da V4 mudou junto — o store carrega o carimbo antigo, nenhum estágio do plano casa e o CoreData recusa com `loadIssueModelContainer`. Medido no MESMO `default.store` e no MESMO aparelho (`34CC3F94`) em três builds: `e72dd85` abre a Página; `main` sozinho e a árvore mesclada da R1 mostram "O Traço não abriu o seu caderno". A A1 faz o certo (para em vez de abrir vazio por cima), e é ela que torna o defeito visível em vez de silencioso. Consertar a migração é volta própria |
| Intenção→artefato delegado→ação→evidência→ajuste | Trabalho persistente V4: revisões, origem, ações, relatos e hipóteses; jornada local, calendário e retorno de Markdown exercitados no simulador. ADR 08m: o resultado observado é eixo próprio do relato (`funcionou`/`parcial`/`naoFuncionou`, `nil` = não observado), separado de executar; `cancelada` tem gesto; a orientação seguinte muda pelo resultado com a causa guardada em `Pedido.ajuste(gatilho: .resultadoInformado)`. ADR 08n: cancelar exige os DOIS eixos (`pendente` E não observado) no agregado, nas duas ordens, com `validar()` fechando a rota de importação; na tela o gesto some e diz por quê. ADR 08o: a orientação nomeia a ação de que o resultado veio (`acaoObservada`), e a premissa vem antes do botão. Jornada dos três resultados fotografada no A1DF082C em `large` e AX5, e a orientação nomeando a ação certa no 64F7B8B4 com três ações e três resultados. ADR 08y (volta R1): quem volta depois de um dia lê **na primeira tela** o que houve desde a visita anterior — versão preparada, ato marcado como realizado, resultado informado, decisão de apoio e dificuldade, escritos pelo APP a partir dos vínculos e nunca por modelo; o "Último retorno" ganhou data e o resultado. Medido no 34CC3F94, mesmo estado plantado e MESMO GESTO nos dois builds, com a tarefa executada pelo XCUITest (volta R1-B): **3 toques + 5 arrastos + 6 paradas de leitura** para saber as sete coisas viraram **3 toques + 0 arrastos + 1 parada**, com os sete fatos na primeira tela; as alturas de AX (1,57 / 2,41 / 2,45 / 3,76 / 4,22 → 0,48–0,74 telas) ficam como medida complementar de distância, não de esforço | Versão nascida do relato com provedor real (sem conta Grok no aparelho de teste: o pedido nasce com a causa e falha); qualidade do artefato ajustado; retomada visual de todos os estados; **e o item 4 da fila só fecha quando o dono efetivamente volta e continua — a demonstração técnica não o fecha** |
| IA produz trabalho delegado com origem | ADR05p: cada versão produzida guarda uma conferência local presa ao pedido — idioma por trecho (`NLLanguageRecognizer`) e distribuição de tempo, com trecho literal do pedido, e tudo que a gramática não reconhece contado como não avaliado na linha da versão. ADR05q: "Conferir com IA" e "Pedir ajuste" sob demanda — segunda `Conferencia` ao lado da local, com o provedor EFETIVO (`Sabia.chamarComProveniencia`) e "· revisão assistida"; parser estrito recusa chave fora do contrato, enum inválido e JSON quebrado (→ `indisponivel`) e citação não literal (→ `inconclusivo`); nada automático; as divergências viram o texto do próximo pedido num toque, com a marca da conferência de origem no texto, editável pelo autor. Volta 5: a revisão assistida só é OFERECIDA com conta Grok — sem ela a tela mostra a linha "precisa da conta Grok; o modelo do aparelho não devolveu revisão válida" no lugar do botão; nenhuma linha soa favorável sem nada confirmado ("nada confirmado · N inconclusivos"); versão importada ou à mão não oferece conferência contra pedido alheio; 44 testes focados e suíte 622/0 em 05/09/2026 | Qualidade ainda insuficiente no caso real de 05/09/2026 (prova/4.md): o roteiro saiu sem nenhuma tradução, com 9 minutos em vez de 15 e portunhol dentro das falas. A checagem apontou o tempo e DEIXOU PASSAR a ausência de traduções — idioma por bloco não vê papel; ela declara esse limite, não o corrige. A revisão assistida NÃO corrigiu isso no caso real de 05/09/2026 (prova/5.md): pela rota do app ela nem rodou no primeiro caso — a montagem deu 3.977 caracteres contra o teto de 3.500 e o estado foi `indisponivel` por limite, sem enviar nada. No SEGUNDO caso real, pela tela (prova/5.md §9), ela RODA com artefato curto (2.997 ≤ 3.500) e não produz revisão utilizável: voltou JSON válido com três critérios nomeados com o enum e citações não literais, tudo `inconclusivo`, sem ver a única divergência real; forçada fora do contrato, a Apple Intelligence devolveu JSON inválido nas DUAS amostras (aspas não escapadas, `justificativa` ausente, citações que não são literais do artefato) e não apontou a ausência de traduções. Segunda passada do mesmo provedor é crítica assistida, não independência. Falta: adequação semântica e ao destinatário com um provedor que consiga produzi-la, revisão pelo Grok (nenhuma conta em nenhum simulador — é justamente o único provedor que a tela agora oferece), e comparar capacidade com/sem contexto |
| Ambiente Markdown compartilhado | ADR05l: Trabalho exporta `.md` e importa edição externa com prévia, base e autoria externa; Files confirmou extensão e retorno v3→v4; bytes, ancestral, ação pendente e relato conferidos no banco; suite integral 536/0. ADR06a (voltas 11 e 11-B): o CONFLITO tem tela E só existe quando há mesmo dois lados — a versão local precisa ter andado desde a base do arquivo e o conteúdo ainda não estar guardado; sem isso a tela dizia "mudou dos dois lados" numa rota de três toques (exportar, guardar intenção, importar o mesmo arquivo) e pedia uma escolha sem efeito. Havendo conflito, a prévia mostra as DUAS versões lado a lado com a consequência escrita antes da escolha e duas saídas nomeadas e de pesos distintos (guardar o arquivo como versão nova, em âmbar / manter só a versão atual, secundária); nenhuma sobrescreve. `IntercambioTrabalho.jaGuardado` é a regra única de "isto já está aqui": a mutação a usa para não fabricar versão e a tela para não oferecer decisão, dizendo "não há nada para decidir". `RecusaDoCommit` separa as duas recusas de `guardar()`: `.aguardandoCommit` (disco) oferece "Tentar guardar de novo", que confirma a MESMA versão sem reimportar; `.precisaReabrir` (mudou em outra abertura) não oferece botão nenhum, porque repetir bateria na mesma guarda, e manda reabrir o trabalho com o arquivo preservado. O selo da origem recolhe o intercâmbio em curso por `verificarAcesso()`, inclusive durante a leitura, e a tela protegida diz o que recolheu. 5 testes focados, suíte 720/0 em 125 suítes em 06/09/2026, build sem aviso; jornada real reexecutável SOZINHA (`maestro/intercambio-conflito.sh`: o roteiro edita o `.md` no disco entre as duas partes; 2 de 2 verdes) e capturas `ferramentas/orca/v11b-*.png` (as duas versões, as duas escolhas, AX5, o arquivo sem novidade sem botão, o importar bloqueado). Volta 11-C (correção do G4): a comparação mostrava DUAS VEZES O MESMO TEXTO sempre que a diferença estava depois do corte — a truncagem mostra o começo e a edição de ida-e-volta acontece no fim; não era limite de AX5, aparecia nas doze linhas do corpo normal em documento longo. `recorteDaDiferenca` ancora os dois cartões na PRIMEIRA divergência, com um fio de contexto recuado até linha ou palavra, e a tela diz onde começou (“As duas começam iguais até a linha N”); o contexto é parâmetro porque a janela muda (48 no corpo normal, 12 em AX5, onde 48 preenchia sozinho as quatro linhas). `Desfecho.mantida` dá retorno às duas saídas que fechavam a revisão em silêncio; a chegada do conflito é trazida à tela por `ScrollViewReader` e anunciada, entrada e recolhimento passam por `Tema.movimento(.deslocamento, Mola.camada)` e a linha de desfecho por `.opacidade`, em `cartao(.campo)` com tinta cheia e `AccessibilityNotification.Announcement` — antes era a terceira frase cinza idêntica às instruções fixas. As três ações seguem o movimento da volta 18: nada de `.disabled()` (o `AcaoTrabalhoStyle`, que nunca leu `isEnabled`, deixa de existir lá) — a ação bloqueada continua cápsula, com o motivo ao lado e no `accessibilityHint`. Suíte 722/0 em 125 suítes em 06/09/2026; jornada real dirigida à mão no iPhone 17e e conferida por `simctl io screenshot` (maestro não isola com vários simuladores ligados), capturas `ferramentas/orca/g4c-v11-*.png` e vídeos `g4c-v11-normal.mp4`/`-reduzido.mp4` ADR08u (MAC-1): **quem escreve na pasta tem nome.** `traco_escrever` ganhou `origem` (`autor`|`grokbot`|`pesquisa`), `motivo` e `fontes`; origem diferente de `autor` sem motivo é RECUSADA e a recusa diz o que falta, `pesquisa` sem `fontes` idem, e nada é gravado na recusa. `Nota.origemRaw` atravessa import, export e tela: `Pilula(.etiqueta)` — a mesma cápsula do gesto, sem cor nem componente novo — diz “feito pelo bot” na linha da lista (antes de abrir) e na página aberta ACIMA do texto (antes de ler). `Retrato.NotaLida.doAutor` e `Trajetoria.NotaLida.doAutor` cortam a nota do bot no mesmo filtro do selo, nem como contagem. `agenda.md` é o quarto arquivo solto da pasta (compromissos do `calendario.json`, decisões vencidas por `Volta.campoDevido`, Recordar devido por `FatiaCorpus.recordarEm`); `traco_agenda(dias)` e `traco_decisoes(dias)` (esperava × aconteceu × sem resposta) o leem. Ao escrever `traco_decisoes` apareceu que `traco_semana` lia os campos da forma do CABEÇALHO e eles vivem no CORPO (ADR 05h): a revisão da semana devolvia decisões vazias desde sempre — corrigido, com a fixture do autoteste que sustentava o engano. Instruções coláveis dos casos 1, 2 (leitura), 3, 8, 9 e 11 em `ferramentas/grokbot/casos/`. Autoteste do servidor verde com as três recusas; 5 testes novos; suíte 953/0 em 153 suítes em 08/09/2026, 0 warning. ADR09b (MAC-1-B, correção do G3): **a origem acompanha todo consumidor.** O corte da 08u estava no LEITOR e o CHAMADOR de produção o esquecia — `Sessao.responderNasNotas` montava `Retrato.NotaLida` sem o argumento e o padrão `= true` mandava a nota `grokbot` à IA. Regra do dono: nenhum consumidor que declare voz, retrato, trajetória ou mapa do autor lê texto que não é dele, nem para inferir domínio, nem para contar. O campo virou `vozDoAutor` e PERDEU O PADRÃO em `Retrato`, `Trajetoria`, `RevisaoSemanal` (nova) e `Rede` (nova): quem escrever o próximo chamador não compila sem declarar; as seis conversões espalhadas viraram `Nota.paraRetrato/paraTrajetoria/paraSemana/paraRede`. `Nota.vozDoAutor` devolve vazio quando a nota não é do autor (a busca pede `textoDeQualquerOrigem`), o que calou o léxico e o classificador de bordo; `Nota.dominio` cala o rótulo já gravado, sem migração; a fonte citada leva a etiqueta no título. `traco_contrato` passou a ser gerado de `Catalogo.todos` com campos e PERGUNTA de cada método — o caso 8 exercitado num cliente MCP de verdade. Os dois vermelhos que faltavam ficaram reexecutáveis (leitor antigo de `traco_semana` no autoteste; replay da V5 por `touch /tmp/traco-replay-v5`, que corrigiu a 08u: o checksum só reclama quando um estágio RODA, e é `NSException`, não `Error`). 8 testes novos, todos do chamador e todos vistos vermelhos; suíte 963/0 em 155 suítes em 09/09/2026, 0 warning; tela em `ferramentas/orca/mac1b-*.png`. Falta ainda: os Trabalhos na pasta e a porta de volta deles (MAC-2); pesquisa com citação obrigatória (MAC-3); nenhuma sincronização automática, por desenho. | A linha do recolhimento, o `.precisaReabrir` e o `ProgressView` da leitura não têm captura: o selo só produz a linha se cair com a mesma `Oficina` viva e nenhuma rota do app sela a origem com a folha do Trabalho aberta (o seletor de Arquivos é modal); a base divergente exige duas `Oficina`s do mesmo Trabalho vivas, e não há rota de duas folhas; a leitura de um `.md` de 400 bytes não dura um quadro. Os três estão provados por teste ou declarados. Em AX5 os dois cartões continuam não cabendo INTEIROS no mesmo olhar — o que a volta 11-C garantiu é que o pedaço visível dos dois é diferente, não que os dois caibam. Segue em aberto: o `recado` não é zerado por atos não relacionados; tocar uma ação bloqueada nomeia o obstáculo em vez de rolar até ele, porque ele mora na folha do Trabalho. Falta ainda: continuidade entre ferramentas com sincronização contínua; cópia já entregue externamente não é revogável |
| Artefato que se transforma (Markdown primeiro; HTML como faixa estreita) | Mecanismo implementado em 08/09/2026 (ADR 08j, volta V17): a causa do ajuste virou dado — `Pedido.ajuste` com gatilho fechado, motivo e referência à evidência; `Artefato.pedidoID` liga a versão à causa no lugar da inferência. O ato "Conferir e adaptar o exercício" lê a tentativa, guarda a leitura antes de pedir a versão e só reescreve quando a leitura sustenta; a seção "Nesta versão" diz no próprio documento o que mudou e por quê; "Não foi isso que eu errei" tira a leitura contestada dos ajustes seguintes sem apagá-la. Núcleo causal fora do trecho descartável, com `ajusteIndisponivel` dito na tela. Volta V17-B (ADR 08k), depois da revisão independente: as duas garantias que viviam na TELA viraram invariante do documento — a mesma leitura não sustenta dois ajustes e a leitura citada tem de estar concluída, não contestada e divergente no critério apontado; e o documento não troca debaixo de quem edita (a folha bloqueia a entrada em edição durante preparação/adaptação e `guardarVersaoHumana` recusa guardar sobre uma base que já não está na tela). O pedido escrito pelo autor passou a registrar a própria causa, e a cápsula enlatada saiu. 912 testes verdes; jornada observada em documento plantado no simulador, com a oferta simulada por argumento de Debug porque a conta Grok vive noutro aparelho. Estado anterior: não implementado. Decisão do dono em 07/09/2026 (DIRETRIZ §4): a primeira prova é em Markdown, sobre o Trabalho e o Caderno que já existem — versões com origem, tentativa como evidência e recipientes `:::slug` já cobrem a base; HTML só para protótipo, simulação ou visualização com interação própria, em sandbox, quando um caso real pedir. Zero WebKit no repositório; `FormatoArtefato.html` existe no modelo e nunca é produzido | O item 1 da fila do dono NÃO fecha aqui: falta o dono usar de verdade, com a conta ligada, num caso real de espanhol de ponta a ponta — a versão adaptada tem de ser lida inteira contra o pedido, e a qualidade semântica do exercício continua sendo prova da frente Q, não desta. Falta também a jornada com o provedor real (esta volta plantou o documento e simulou a oferta em Debug, porque a conta vive noutro aparelho) e a representação do exercício como bloco do Caderno |
| Calendário ligado à realização | Ação pendente projeta horário E aviso do Trabalho (ADR 05n): seletor na folha, promessa em hora real, estado lido do centro ao abrir, cancelamento em executar/cancelar/retirar/mudar e ao proteger a origem; suíte 576/0 e fluxo maestro em 05/09/2026 | Estado "sem permissão" na folha reaberta só provado por teste (simulador não nega); duração explícita; re-armar ao liberar origem; widget/Ilha da ação; horário não prova realização |
| Modelo revisável do autor | Retrato agregado e hipóteses locais corrigíveis no Trabalho; contestação integra pedido e invalida geração; ADR05m: a sábia local preserva a carga ou cala e a conferência recusa veredito sobre evidência cortada ou JSON fora do contrato (17 testes focados, suíte 565/0 em 05/09/2026); ADR05o: no aparelho, rótulo e conteúdo são indivisíveis e os ecos exigem duas candidatas inteiras ou calam. ADR 06j (volta L1): a LATÊNCIA DA DESCOBERTA — quanto tempo entre afirmar e saber que errou — vira grandeza e SÉRIE no Perfil, sem nenhum campo novo: `Hipotese.data`→`avaliadaEm` já dava a distância, e a data em que a Decisão foi respondida saiu do histórico `Versoes` (a versão mais recente com "o que aconteceu" ainda vazio), porque `editadaEm` desliza a cada retoque. Quatro estados distintos e nenhum é falha (afirmado, devido, descoberto, abandonado); hipótese avaliada antes da 05r fica "tempo desconhecido" e ninguém deduz a data; os abertos aparecem ao lado dos fechados para a série não virar viés de sobrevivência; sem meta, sequência, seta ou ação, e a cobrança segue só na lista de Notas. Volta L1-B (a correção do G3): a leitura passa por `AcessoTrabalho.permitido` como as outras onze superfícies (hipótese de Trabalho com origem selada não chega ao Perfil); a nota trancada ou queimada não entra NEM COMO CONTAGEM, na regra do retrato logo acima; o corte da lista é POR ESTADO (2 devidos, 4 abertos no total, 2 sem data, 4 descobertos, 2 abandonados) para que dezessete abertos não apaguem todos os fechados e a tela não vire fila de dívida; e `propostaPor` viaja junto, com "proposta por X" e "autoria desconhecida" quando a hipótese não é do autor (05r). Volta L1-C: a BARRA do mês SAI e ficam só as palavras — ela normalizava pela série, então o pior mês era sempre 100 % (300 dias e 1 dia desenhariam a mesma pista), e a única escala honesta seria absoluta, que em dias não cabe na largura nem informa; sem barra, o que separa os meses é o vão, que vira `@ScaledMetric` para não sumir dentro da entrelinha em AX5. **Volta L2 (ADR 08h, a correção do G4 da L1)**: a lista de meses ganha o TETO de doze com o horizonte dito na copy — era o único pedaço da seção sem corte, e com vinte meses semeados o cartão cai de 6.148 pt para 5.340 pt em AX5 (−13%); `Tema.miudo` sai de dentro do app (a ADR 05u o reserva a fora do app e a L1 era o primeiro uso dele em todo o produto, justo na linha da medida), e o cartão passa a ter UMA regra de degrau — linha que carrega medida é `Tema.meta` + `tintaSuave`, prosa de apoio é `.footnote` + `tintaFraca` —, o que separa o registro que antes lia como massa só; a frase-resumo quebra em duas (a medida sozinha como manchete, a composição mais quieta embaixo) sem tirar um número, por duas leituras da MESMA `emPalavras`; e um mês com uma descoberta só diz "1 descoberta · levou 5 dias" em vez de chamar de "tempo do meio" o único valor que existe. Custo medido e declarado: onde a série é curta o cartão engorda 6,6% (5.200→5.541 pt em AX5), porque a linha da medida subiu de 12 para 15 pt em nove registros; 887 testes em 08/09/2026; L2-B: a ADR ganha a letra reservada (08h), todas as capturas e medidas são refeitas no binário do commit — com o vazio DEPOIS (241 pt / 1.485 pt em AX5) — e a compactação dos registros pedida pelo G3 é medida e recusada: num fluxo só ela recupera 180 dos 341 pt em AX5 e piora a leitura em `large`, porque a altura em AX5 mora no parágrafo (833 pt) e no embrulho de cada registro | Continuidade entre trabalhos e intervenções verificáveis por contexto/capacidade; conferência semântica do modelo sem prova; vestir/calibragem/Padrões calam no aparelho acima do teto. Da 06j: a série foi provada em simulador de teste com registros semeados no formato do próprio app (o simulador não viaja no tempo) — a série do aparelho do dono continua sem captura; decisão respondida sem histórico de versões fica sem data; e o estado DEVIDO ancora o "espero" em `criadaEm` enquanto `Volta.devida` o relê a partir de hoje (divergência declarada, não resolvida). Da L1-B: o ciclo escrever→avaliar→ler está provado pelo app (`maestro/latencia-ida-e-volta.yaml`), a SÉRIE de meses não; e o gate de acesso continua sendo chamado por cada leitor, então fechar a classe pede `Trabalho.ler()` com `ModelContext`, e são só três chamadores. Da L1-C (registrado, não consertado): a Decisão ficou SEM porta para `abandonado` — a que o autor nunca vai conferir fica devida para sempre e só sai da tela se a nota for trancada, que é o gesto que a L1-B declarou não ser abandono; falta um "não vou conferir esta" sobre a decisão, e é volta própria. Da L2: em AX5 a camada do arquivo do Perfil transborda na horizontal em algumas sessões (cartão e barra de abas cortados dos dois lados) — reproduz em HEAD sem a mudança da volta e o app Ajustes no mesmo aparelho não transborda, então é do `Camadas`/`RaizView` e fica para a volta do Perfil; a captura do estado VAZIO depois da mudança não saiu (o helper do `orca emulator` é um só para a máquina e outra sessão o tomava), e do estado vazio só há a leitura da árvore de acessibilidade |
| Desenvolvimento de capacidades relevantes | Recordar/métodos existentes. ADR05r: `Apoio.praticar` deixa de ser uma frase do prompt — a IA prepara um EXERCÍCIO estruturado (enunciado, exemplo resolvido de outro caso, critérios com identidade) por geração guiada por schema tipado, a tentativa da pessoa é guardada como EVIDÊNCIA da ação ligada ao material (`Evidencia.tentativa`, origem pessoa, apoio utilizado obrigatório, revisão ligada à anterior) e nunca como versão, e "Conferir minha tentativa" responde por critério com trecho literal e observação sem solução. Cobertura incompleta é `naoAvaliado`; citação não literal é `inconclusivo`; reavaliar não cria outra demonstração; guardar não marca ação executada; confirmar hipótese é concordância contextual e não declara aprendizagem. Gargalo corrigível com autoria explícita (`propostaPor`, `avaliadaEm`, `motivoAvaliacao`). Volta 6 (revisão): exercício e feedback pela IA só com conta Grok (decisão (b), como 05q); preparação que não sai deixa o pedido `praticaIndisponivel` e NUNCA cai na produção delegada; a tentativa existe sem exercício e sem conta (ação "Praticar por conta própria"); em `delegar` a seção "Dificuldade" mantém propor/confirmar/contestar (05i) e as tentativas já escritas, só leitura. 43 testes focados e suíte 688/0 em 06/09/2026 | **A prática textual e o feedback contextual NÃO estão demonstrados** (prova/6.md, 05/09/2026). Em quatro amostras no aparelho a preparação e o feedback estruturados SAÍRAM em formato válido — a geração guiada removeu a classe de falha que derrubou a V5 —, mas o conteúdo não serviu: o exercício ignorou "3 blocos de 5 minutos" e reduziu 15 minutos a uma frase; 3 de 6 critérios cobravam pronúncia e repetição, ilegíveis num texto; e nas 12 citações o modelo não copiou a tentativa literalmente (escreveu `vivo em Sao Paulo` onde ela dizia `vivo en São Paulo`), então ZERO critérios foram avaliados nas duas amostras. Sem a regra da citação literal, uma amostra teria exibido seis critérios "atendidos" sobre um texto transcrito errado. A jornada pela tela (revisão independente, aparelho do dono, `ferramentas/orca/v6-*.png`) confirmou: 3/3 preparações e 3/3 feedbacks do aparelho sem utilidade, e uma preparação não validada que entregou o roteiro pronto — corrigido: sem conta Grok não há botão de IA, e a preparação que não sai nunca vira entrega. Falta: exercício e feedback úteis pelo Grok (sem conta em nenhum simulador; o único provedor oferecido está sem prova real), e aprendizagem duradoura, pronúncia e transferência, que continuam inteiramente pendentes |
| Direção visual e uso simples | Acesso Notas com teclado, Trabalhos, renderer por blocos e inspeção AX5; parser não trava com marcador sem fecho. ADR05t (volta 8): movimento reduzido numa lei só (`Tema.animacao`/`transicao`) nas jornadas Página, Notas, Calendário, Recordar e Rede; arrasto do calendário e toque longo em Analisar com par no rotor; anúncios da sábia e do cartão na página e nas Notas; AX5: régua, cabeçalho do Recordar e chrome das Notas param em xxxLarge, pergunta do Recordar não é mais comprimida; suíte 623/0 em 05/09/2026. ADR05v (volta 10): `Traco/Componentes` com dez componentes e preview por estado, tokens de duração/mola/raio/sombra em `Tema` e a lei de movimento por classe; Notas, ficha do calendário e Recordar migradas sem mudar desenho: diff de pixels 0 px em vazio, mês, ficha topo AX5 e Recordar nas três fases; onde não deu 0, é caret (ficha 0,008 %, escrever 0,08 %), rolagem clampada (ficha AX5 0,07 %), a pergunta que a sábia inventa (escrever AX5 3,7 %) e anti-aliasing de texto no campo de busca (218–636 px) — relatórios `ferramentas/orca/relatorio-v10-b.md` e `revisao-v10-fundacao.md`; suíte 713/0 em 125 suítes em 06/09/2026 na árvore final (G4: queima da expressiva na classe `.opacidade`; sob Reduzir Movimento ela já cortava antes da cena) . ADR06b (voltas 18 e 18-B): o Trabalho entra na família do mundo claro — `CabecalhoDeFolha` no lugar da barra do sistema nas duas telas, rótulo de seção, campo em névoa e cartão de papel, `AcaoTrabalhoStyle` apagado, oito `DisclosureGroup` em cinco e nenhum aninhado; a ordem de leitura passa a ser a do ciclo (intenção → apoio → preparar → versão → ato → o que aconteceu → dificuldade), com a dificuldade saindo da frente do caminho principal; a decisão delegar/praticar/combinar sai do disclosure e vira trilho de pílulas no caminho, com o padrão já marcado (0 toques a mais) e empilhado em AX5, onde a linha estourava a largura da folha e sangrava o documento pelos dois lados (defeito herdado da V9, reproduzido e fechado); ação principal não vira fantasma por campo vazio — leva o foco ao campo e diz o motivo; a promessa do aviso passa a distinguir concedido, não perguntado e negado (`PromessaDoAviso`), fechando o defeito 6 da §6, o mesmo 2 da ficha do calendário. Volta 18-B (correção do G3): a jornada intenção → versão preparada CAI de 6 toques e 2 digitações para 5 e 2 — a folha recém-criada abre com o cursor no pedido, e o toque que saiu só existia para revelar o passo seguinte (medido por `maestro/trabalho-curva-zero.yaml`, que falha se alguém devolver o toque); `PromessaDoAviso` ganha o quinto caso `jaPassou` com `instante:`/`agora:`, e um ato sem horário passa a nascer meia hora à frente em vez de `.now` cru, então a folha deixa de prometer alarme para hora já passada (10 testes); a regra do foco vira lei única para as sete ações — nenhum `.disabled()` sobrou em `TrabalhoView`/`AgendamentoAcaoView`, a ação bloqueada continua cápsula com o motivo ao lado e tocar leva ao campo vazio, à edição pendente, à saída do erro ou ao progresso da IA; o trilho do apoio anuncia `selected` ao VoiceOver; `maestro/trabalho-acao-aviso.yaml` volta a passar (os quatro `swipe` fixos viraram `scrollUntilVisible`). Volta 18-C (correção do re-G3): a folha passa a CUMPRIR a regra que enuncia — com a intenção editada e não guardada, o toque em "Preparar com IA" deixa de disparar a IA e leva o foco ao campo em edição, provado na tela em cinco estados (`v18c-regra-cumprida.png`); a causa era duas listas de guardas copiadas que divergiram, e as duas rotas que chamam a IA passam a compartilhar `levouAoQueFalta`, na mesma ordem em que a mensagem fala; `AccessibilityNotification.Announcement` diz o motivo nos ramos que antes só rolavam a tela, fechando (para quem usa VoiceOver) a perda do `isEnabled = false` que fazia Controle Assistivo e Acesso Total por Teclado pularem o controle; `PromessaDoAviso` fica pronto para a ficha do Calendário — `jaPassou` guardado por `!repete`, porque `Aviso.instante` conta a partir do início da série e o tipo diria "ficou sem alarme" do compromisso que toca toda semana, e `instante:`/`repete:` sem valor padrão para ninguém ligar as pontas em silêncio (13 testes); suíte 728/126 em 06/09/2026. Volta 18-D (correção do G4): o estado preso da folha — depois de guardar a PRÓPRIA versão ela afirmava para sempre uma edição pendente inexistente, imprimia a versão duas vezes, travava "Preparar nova versão com IA" e a importação, e sobrevivia a fechar, reabrir, descartar e reiniciar o aparelho — é fechado na causa e nas DUAS metades dela: `campoEmEdicao` julgava a versão por não-vazio em vez de por diferença, e o `TextField` que sai da tela devolvia o `padrao` ao binding depois do `limpar` do salvamento (medido: `"versao" => ""` e `"pedido" => ""` no plist do app). Edição pendente passa a ser rascunho DIFERENTE do guardado, rascunho em branco não é edição em campo nenhum, e o `set` do campo não grava o que não mudou; a regra vira `static` e é lida nos três lugares que divergiam (guarda do bloqueio, reabertura do campo dentro do cartão da versão, rodapé do descarte), então o aparelho já preso sai do estado sozinho na primeira leitura. A linha do trilho passa a falar da opção SELECIONADA (dizia "Delegar não exige…" com Praticar e Combinar marcados). 7 testes em `RascunhoTrabalhoTests`; suíte 735/127 em 06/09/2026   ADR06k (volta 19): o Recordar sai de 6,2 com um PÉ só para escrever e para revelar — fio no topo, a ação principal como `Pilula(forma: .larga)` cheia (o único objeto escuro da folha) e as saídas honestas em meta abaixo dela; antes "Revelar" e "hoje não" eram dois textos centrados soltos em ~1 100 px de papel, sem cara de botão e sem peso diferente entre o caminho e a saída. O `VStack` raiz vira `.leading` (o conteúdo estava na margem e "serviu/não serviu" e as ações flutuavam no centro: três eixos numa tela só), e o julgamento da pergunta desce para DEPOIS do campo, continuando visível em todo corpo de texto. Em AX5 fecham dois defeitos herdados da V9 e reproduzidos hoje: o revelar insistia em duas colunas por um teste cego ao corpo do texto (`largura >= 360`) e o SwiftUI hifenizava — "obstá-culo", "MEMÓ-RIA", "per-gunta" — com a nota cortada no meio de uma letra e a ação por cima do corte; e a pergunta com `fixedSize` tomava sete linhas e deixava ao autor um campo de linha e meia. A regra vira `RecordarView.comparaLadoALado(largura:tamanho:)`, nomeada e testada fora da tela (lição da F4), e `GridItem(alignment: .topLeading)` põe "DE MEMÓRIA" e "A NOTA" na mesma linha de base (estavam 57 pt / ~280 pt fora). Movimento: a frase "Leia uma última vez — a nota vai se esconder." passa a sair no MESMO driver da nota, em vez de ficar 1,2 s em opacidade cheia sobre uma tela já vazia prometendo o que já aconteceu; e a pergunta da sábia que chega depois da primeira letra NÃO entra mais — o comentário no código já dizia que nada muda embaixo do autor, o vídeo mostrou o enunciado sendo trocado ~4 s depois com ele escrevendo. TRÊS dos quatro defeitos da V9 que a volta deu por caídos já tinham mesmo caído em V8/V10-B, e foram conferidos na tela viva antes de tocar em qualquer linha, não relidos do relatório (estilo local, cross-fade do §21, hifenização do cabeçalho) — o revisor do G3 confirmou os três com `git show` e com a tira de quadros. O QUARTO ERA FALSO e a volta 19-B o corrige aqui: o toast do "hoje não" NÃO caiu e continua nascendo em cima de "Trabalhar nisto" e da régua Analisar · Recordar · Anexar · Lente — é o defeito 15 da V9, reproduzido duas vezes pelo revisor com a nota escrita (`v19-rev-01-toast-tapa-a-barra.png`); a prova que a volta 19 deu era de uma página VAZIA, onde os controles que o toast tapa nem existem, e o `padding(.bottom, 88)` de `PaginaView.swift:303` está lá desde 31/08, antes da auditoria. O defeito 15 fica ABERTO com dono nomeado: quem toca `Traco/Pagina/PaginaView.swift`, e o conserto é o toast medir a altura real da barra em vez de chutar 88. Regra que fica: conferir a auditoria na tela viva antes de tocar continua certo e poupou esta volta de reconsertar três coisas, mas só vale quando a tela viva reproduz o estado que a auditoria acusou — quando o defeito é "A tapa B", a prova tem de mostrar B na tela. Volta 19-B (correção do G3): o teto da pergunta virava COTA — o `.frame(maxHeight:)` estava fora do `ViewThatFits` e cobrava metade do vão mesmo com a pergunta em duas linhas, abrindo ~198 pt de papel morto em `large` e pondo o autor a escrever no meio da folha (caret a 46,2 %, contra 23,6 % antes da volta); o teto desceu para dentro do ramo que rola e o caret voltou a 22,0 %, medido na tela viva (`v19b-01-escrever-sem-vao-morto.png`). A máscara de gradiente saiu: ela apagava a última linha da pergunta também depois de já se ter rolado até o fim. A regra da pergunta tardia vira `RecordarView.aceitaPergunta(jaTem:memoria:)`, testada fora da tela (era o comportamento novo mais importante da volta e o único sem teste), espaço em branco não conta como escrita, e com `.task(id:)` a primeira letra CANCELA a chamada em voo em vez de pagar por uma resposta que vai ao lixo. O rótulo de VoiceOver "Próxima", perdido na troca do botão pelo pé, volta. FICA ABERTO, medido e com dono fora do Recordar: a ação principal desabilitada mede **1,53:1** (`Tema.tintaMorta` #C7C7CC sobre `Tema.fundo` #F4F4F2) no estado em que a tela SEMPRE abre, e a saída "hoje não" (6,73:1) grita quatro vezes mais alto que o caminho — `von-restorff-effect` invertido. A causa não é do Recordar: `Pilula.swift:52` devolve `tintaMorta` e a 57 devolve fundo `.clear` para TODOS os chamadores, ou seja a `Pilula` não tem estado desabilitado no sistema. Dono: `Traco/Componentes`. Suíte 786/132 e build sem aviso em 06/09/2026. A curva-zero não muda: 1 toque + escrever + 1 toque; adiar e pular em 1 toque. Suíte 782/131 e build sem aviso em 06/09/2026; capturas `ferramentas/orca/v19-antes-*.png` e `v19-depois-*.png` (sete estados cada, `large` e AX5, mesmo roteiro) e vídeos `v19-antes-movimento.mp4`, `v19-depois-movimento.mp4`, `v19-depois-movimento-reduzido.mp4`, onde a tira a 30 fps mostra a instrução e a nota apagando juntas e nenhum quadro com dois textos legíveis, com e sem Reduzir Movimento **10/09 (ADR 10f, SISTEMA-IA/conversa): a conversa com a sábia é uma folha do Traço** — gesto "perguntar" separado da busca, resposta inteira na área da lista (sem teto nem dobra), continuação sem recomeçar, falha junto da pergunta com recuperação, um Fechar; `CartaoDeResposta` é a superfície única (Notas, Página, Lente) com os três estados. |  . ADR05y (volta 12): a Página e o Caderno até 9 — o cartão da análise, o aviso e a linha "lendo…" saem do rodapé e passam a viver ACIMA da régua e das ações, que nunca mais somem sob o dedo (V9: o toque em "Todas" caía no cartão); as saídas do cartão ficam no pé dele em todo tamanho, com degradê quando o texto passa da dobra (G4 V8); em AX o pé tem "Trabalhar nisto" como BOTÃO e as outras quatro no menu "Mais ações da nota" (as cinco dentro do menu não cabiam: ele passava da tela e a quinta só existia depois de rolar, sem afordância — G3 da V12, M3); `.primario`/`.compacto` sem opacidade no press (ADR 02h, G4 V10); célula nova com a classe que declara (G4 V10); `Camadas` devolve a posição quando o binding recusa (re-G3 V7) e a aba do arquivo tem 44 pt de alvo; Página e Caderno citam Componentes (`.rotulo`, `.cartao`, `Pilula`, `CabecalhoDeFolha`, `.discreto`) e o diff é 0 px onde não devia mudar (arquivo pela borda em large e AX5, página vazia) — capturas `ferramentas/orca/v12-*.png`, vídeos com e sem Reduzir Movimento; vestir a forma deixou de derrubar o foco: `paginaUna` tinha dois ramos, o ramo trocava quando os campos nasciam, o editor era recriado, o teclado descia e o encaixe inteiro era redesenhado noutra geometria — o pé do cartão e o pé da página dissolvidos sobre o texto do cartão, ~165 ms, com E sem Reduzir Movimento (Re-G3, corrigido na V12-C: `v12c-pe-quadros-sem-rm.png` e `-com-rm.png`). Correção do G4 (V12-D): com o TECLADO DE PÉ o cartão comia o papel — 413 pt de encaixe (47% da tela) para 33 pt de página, e 37 caracteres digitados depois de o cartão chegar não apareciam; o teto do cartão deixa de ser absoluto e o encaixe leva o que SOBRA depois do pé medido e do piso do papel (três linhas de corpo, nunca mais de metade do que sobra), e com o teclado de pé o cartão vale UMA LINHA com as saídas à vista (em AX, num menu da própria linha; o aviso, o "sem conta", o "pensando…" e a resposta da sábia nunca recolhem). Medido na tela no mesmo estado do G4: papel 141 pt contra 33, encaixe 35% contra 47%, a frase inteira e o caret à vista; em AX5 a pilha para de transbordar nas duas pontas e "Mais ações da nota" — a única porta das quatro ações em AX — fica acima do teclado. E a LEI DO MOVIMENTO REDUZIDO, que é do app inteiro, deixa de ser ambígua: a 05v dizia "deslocamento → fade OU corte" e a implementação entregava `fadeReduzido` (0,15 s), que é duração curta e não corte — a geometria seguia interpolada e dois textos legíveis podiam ocupar as mesmas linhas, o que fez a quinta ocorrência da classe A1 durar ~370 ms COM Reduzir Movimento contra ~215 ms sem. Agora sob reduzido só a OPACIDADE anima; deslocamento, escala e laço cortam, `fadeReduzido` deixa de existir e `Tema.animacao`/`gaveta`/`CalendarioTema.morph` devolvem `Animation?`. Provado em quadros nativos nos dois lugares: a Página ("Abrir os campos" e a linha que abre) e o Calendário de outra volta (Dia→Semana e Semana→Mês em UM quadro sob RM); suíte 760/0 em 128 suítes em 06/09/2026; a volta fecha em +323 linhas líquidas de Swift do app (V12 −55, V12-B +108, V12-C +9, V12-D +261), portanto NÃO cumpre a regra de shortstat líquido-negativo da 05v — a exceção foi aceita pelo orquestrador e o motivo está declarado: os dois testes que o G3 pediu exigiram tirar uma decisão de dentro de um gesto e outra de dentro de um `body`, e a identidade por caso do cartão é um `switch` sobre um enum de outra área | VoiceOver ligado em aparelho real (humano); `maestro hierarchy` do revisor contra a árvore documentada; capturas AX5 feitas no simulador da volta, não no iPhone do dono (em uso); ano/grade do calendário seguem presos em large/xxLarge e a régua em xxxLarge. Da V12-D: nota MAIS ALTA que o papel visível ainda mostra as PRIMEIRAS linhas enquanto se escreve no fim — o `TextEditor` da página não rola sozinho e o `ScrollView` de fora não segue caret; medido e filmado, a correção mexe na estrutura de `paginaUna` que a V12-C acabou de estabilizar. E, ao apresentar a folha dos campos, o rótulo "Todas" fica no lugar por alguns quadros enquanto o pé desce (fotografia do `.sheet`, sobre o fundo da barra e nunca sobre texto). ADR 06h (volta A4, voz do app sobre si mesmo): "O que o Traço aprendeu de você" virou "O que o Traço registrou — contagem, não conclusão" (embaixo é contagem de sinais), "Muletas" virou "Palavras de apoio" na Lente e no apontamento de versão (a lista inclui os hedges que o próprio catálogo ensina) e o aviso do plano diz "pode atrapalhar" em vez de "costuma". Correções do G3 (05/09): alvo = frame + contentShape (`View.alvo()`), chips do calendário lidos uma vez, ano um elemento por mês, cartão da análise com ações no pé em AX  . ADR06b (voltas 18 e 18-B): o Trabalho entra na família do mundo claro — `CabecalhoDeFolha` no lugar da barra do sistema nas duas telas, rótulo de seção, campo em névoa e cartão de papel, `AcaoTrabalhoStyle` apagado, oito `DisclosureGroup` em cinco e nenhum aninhado; a ordem de leitura passa a ser a do ciclo (intenção → apoio → preparar → versão → ato → o que aconteceu → dificuldade), com a dificuldade saindo da frente do caminho principal; a decisão delegar/praticar/combinar sai do disclosure e vira trilho de pílulas no caminho, com o padrão já marcado (0 toques a mais) e empilhado em AX5, onde a linha estourava a largura da folha e sangrava o documento pelos dois lados (defeito herdado da V9, reproduzido e fechado); ação principal não vira fantasma por campo vazio — leva o foco ao campo e diz o motivo; a promessa do aviso passa a distinguir concedido, não perguntado e negado (`PromessaDoAviso`), fechando o defeito 6 da §6, o mesmo 2 da ficha do calendário. Volta 18-B (correção do G3): a jornada intenção → versão preparada CAI de 6 toques e 2 digitações para 5 e 2 — a folha recém-criada abre com o cursor no pedido, e o toque que saiu só existia para revelar o passo seguinte (medido por `maestro/trabalho-curva-zero.yaml`, que falha se alguém devolver o toque); `PromessaDoAviso` ganha o quinto caso `jaPassou` com `instante:`/`agora:`, e um ato sem horário passa a nascer meia hora à frente em vez de `.now` cru, então a folha deixa de prometer alarme para hora já passada (10 testes); a regra do foco vira lei única para as sete ações — nenhum `.disabled()` sobrou em `TrabalhoView`/`AgendamentoAcaoView`, a ação bloqueada continua cápsula com o motivo ao lado e tocar leva ao campo vazio, à edição pendente, à saída do erro ou ao progresso da IA; o trilho do apoio anuncia `selected` ao VoiceOver; `maestro/trabalho-acao-aviso.yaml` volta a passar (os quatro `swipe` fixos viraram `scrollUntilVisible`). Volta 18-C (correção do re-G3): a folha passa a CUMPRIR a regra que enuncia — com a intenção editada e não guardada, o toque em "Preparar com IA" deixa de disparar a IA e leva o foco ao campo em edição, provado na tela em cinco estados (`v18c-regra-cumprida.png`); a causa era duas listas de guardas copiadas que divergiram, e as duas rotas que chamam a IA passam a compartilhar `levouAoQueFalta`, na mesma ordem em que a mensagem fala; `AccessibilityNotification.Announcement` diz o motivo nos ramos que antes só rolavam a tela, fechando (para quem usa VoiceOver) a perda do `isEnabled = false` que fazia Controle Assistivo e Acesso Total por Teclado pularem o controle; `PromessaDoAviso` fica pronto para a ficha do Calendário — `jaPassou` guardado por `!repete`, porque `Aviso.instante` conta a partir do início da série e o tipo diria "ficou sem alarme" do compromisso que toca toda semana, e `instante:`/`repete:` sem valor padrão para ninguém ligar as pontas em silêncio (13 testes); suíte 728/126 em 06/09/2026. Volta 18-D (correção do G4): o estado preso da folha — depois de guardar a PRÓPRIA versão ela afirmava para sempre uma edição pendente inexistente, imprimia a versão duas vezes, travava "Preparar nova versão com IA" e a importação, e sobrevivia a fechar, reabrir, descartar e reiniciar o aparelho — é fechado na causa e nas DUAS metades dela: `campoEmEdicao` julgava a versão por não-vazio em vez de por diferença, e o `TextField` que sai da tela devolvia o `padrao` ao binding depois do `limpar` do salvamento (medido: `"versao" => ""` e `"pedido" => ""` no plist do app). Edição pendente passa a ser rascunho DIFERENTE do guardado, rascunho em branco não é edição em campo nenhum, e o `set` do campo não grava o que não mudou; a regra vira `static` e é lida nos três lugares que divergiam (guarda do bloqueio, reabertura do campo dentro do cartão da versão, rodapé do descarte), então o aparelho já preso sai do estado sozinho na primeira leitura. A linha do trilho passa a falar da opção SELECIONADA (dizia "Delegar não exige…" com Praticar e Combinar marcados). 7 testes em `RascunhoTrabalhoTests`; suíte 735/127 em 06/09/2026  ADR 08p (volta V13, 08/09/2026): a lista das Notas não afirma o que não há — Trabalhos vira linha da lista com seta (destino, não link âmbar), a régua perde a máscara que apagava o chip seguinte e a contagem nomeia o filtro ("3 notas · WOOP"), o cartão da sábia em falha perde o vão de ~100 pt (teto de ScrollView com fixedSize) e "Repetir pergunta" quebra linha em AX5, a página só com marcador de seção não vira linha em branco (raiz em Sessao.paginaVazia/Nota.temVoz nomeada para o arquiteto), título sem teto de linhas em AX, Versões e Ligações com Pilula(.acao); suíte 947/948 em 153 suítes no 17e em 08/09/2026 (o vermelho é o caret do caderno em AX XXXL, pré-existente no 17e); capturas antes/depois em ferramentas/orca/v13/. ADR 09d (volta C1, 08/09/2026): esse vermelho fecha — a invariante da escrita visível passa a valer TAMBÉM no iPhone 17e em AX XXXL. Duas causas medidas por sonda antes de editar: o contêiner concedia ao papel menos de uma linha (o pé toma 274,67 dos 413,67 pt e a regra `min(piso, sobra/2)` deixava 69,5 pt — depois 43,5 — para uma linha de 67), e `EscritaVisivel.seguirCaret` perseguia o `caretRect` (45 pt) em vez da linha visual (67 pt), pedindo a folga inteira num papel que não a comporta. Agora o piso do papel nunca desce abaixo de uma linha (`piso/3`), o seguidor mede a linha pelo TextKit 2 (`EscritaVisivel.linhaDoCaret`) e a folga cede antes da letra. Nenhuma asserção afrouxada e nenhum aparelho tirado da medida: 18 issues → 0, AX5 31/31 e large 44/44, suíte integral 949/949 em 153 suítes no 17e, build sem aviso; capturas em ferramentas/orca/c1/ e relatório em ferramentas/orca/c1-caret.md. Resíduo declarado, não corrigido: durante a gaveta do cartão a chegar há ~0,11 s em que a linha ativa aparece cortada pela borda do cartão (`c1-04-residuo-gaveta-cartao.png`); e fica a dívida da barra de baixo, que em AX XXXL toma 275 dos 414 pt do 17e. ADR 08x (volta C1-B, 09/09/2026): esse resíduo fecha, e a 08f volta a valer como está escrita — em cada quadro. A invariante passa a ser MEDIDA por quadro (`CADisplayLink` sobre as camadas de apresentação, com o instante de cada quadro), e a medida corrige duas coisas que a 09d tinha dito: são 0,098 s em AX5 e 0,100 s em `large` — 6 quadros cada —, e o resíduo NÃO era só de AX XXXL. Causa: a altura do papel é animada pela gaveta e desce até 21 pt por quadro, e o seguidor, que é observador dela, fica sempre um passo atrás — provado por ablação com três seguidores diferentes, que deram os mesmos offsets ao ponto: de fora do layout do SwiftUI não há corrida a ganhar. Decisão: NENHUMA GAVETA CORRE SOBRE A LINHA DO AUTOR — com o foco na Página a altura do encaixe muda por CORTE (a mesma lei que a 08f já aplicara à régua e à abertura dos campos), e o seguidor corre no MESMO quadro, com a altura que o `onScrollGeometryChange` anuncia em vez da que o `bounds` ainda tem. As duas metades são necessárias: só o corte deixa 1 quadro com 115 pt de linha cortada; só o seguidor síncrono deixa os 6 quadros. Juntas, 0 fora nas seis cenas, nos dois tamanhos e nos DOIS aparelhos — 17e `C7341E64` e Pro Max `6033B043`, teclado de software real (308 e 318 pt); `LinhaDoCaretTests` cobre as bordas do TextKit 2 (vazio, linha vazia, quebra por palavra, fim do documento, `NSMaxRange` em todos os offsets) e mede que num `UITextView` nu o `caretRect` JÁ é a caixa da linha visual em 0 de 61 offsets — a distância de 45 para 67 pt é do editor da Página, não do TextKit 2; `TemaTests.ondeHaviaFolgaSobrandoA09dNaoMudaNada` varre 3.025 combinações e prova que, onde meia sobra já dava uma linha, a 09d devolve o mesmo número da 05y. Suíte integral 956/956 em 154 suítes no 17e, `grep -c warning:` 0; quadros carimbados em `ferramentas/orca/c1/c1b-quadros-{vermelho,verde}.txt`, relatório `ferramentas/orca/c1b-gaveta.md`. ADR 08x, correção do re-G3 (volta C1-C, 09/09/2026): a sonda passa a medir a 08f INTEIRA por quadro — `E ⊆ P` e `P ∩ O = ∅`, contadas e relatadas separadas, lendo a árvore de camadas pela apresentação e ignorando camada sem `presentation()` (que não pinta naquele quadro); o teste planta uma camada adversarial sobre a linha ativa e exige que os quadros a acusem (35 de ~51), para que zero intruso seja prova e não silêncio; e `linhaAtiva` deixa de devolver só o `caretRect` no fim do documento (recuo de um caractere), com o que `E` volta a ser a linha de letras — 322 pt em AX5, 55 em `large`, contra 2 pt antes. Com a sonda inteira, o pai `4898703` fica vermelho nas DUAS metades: AX5 7 quadros fora (0,113 s) e 2 cobertos (0,050 s, `ColorShapeLayer`/`CGDrawingLayer` do próprio cartão sobre a linha), `large` 5 e 5 (0,083 s cada); o candidato dá 0 e 0 nas seis cenas. O MP4 `c1b-gaveta-consertada.mp4` continha 44 s da Tela Inicial e foi REMOVIDO; no lugar, `c1/c1c-pagina-na-sonda.mp4` (36 s, gravado por `simctl recordVideo` na corrida verde e assistido antes de versionar). Suíte integral 956/956 em 154 suítes no 17e `C7341E64`, `grep -c warning:` 0; quadros em `c1/c1c-quadros-{vermelho,verde}.txt`, relatório `ferramentas/orca/c1c-sonda-inteira.md`. Pro Max `6033B043` NÃO reexecutado (aparelho reservado, `ask` expirado): essa parte fica herdada da C1-B. As três dívidas foram ao RUMO: a barra de baixo em AX, o oráculo de pixels e o limite do seguidor contra uma altura animada. | VoiceOver ligado em aparelho real (humano); `maestro hierarchy` do revisor contra a árvore documentada; capturas AX5 feitas no simulador da volta, não no iPhone do dono (em uso); ano/grade do calendário seguem presos em large/xxLarge e a régua em xxxLarge. ADR 06h (volta A4, voz do app sobre si mesmo): "O que o Traço aprendeu de você" virou "O que o Traço registrou — contagem, não conclusão" (embaixo é contagem de sinais), "Muletas" virou "Palavras de apoio" na Lente e no apontamento de versão (a lista inclui os hedges que o próprio catálogo ensina) e o aviso do plano diz "pode atrapalhar" em vez de "costuma". Correções do G3 (05/09): alvo = frame + contentShape (`View.alvo()`), chips do calendário lidos uma vez, ano um elemento por mês, cartão da análise com ações no pé em AX. Acabamentos M-2 e M-3 do re-G3 (volta A-5): o diálogo destrutivo do Perfil dizia "Esquecer tudo o que o Traço APRENDEU DE VOCÊ?" — a frase que a A4 já tinha trocado no rótulo acima e esqueceu no diálogo — e passa a "Esquecer tudo o que o Traço registrou?"; a dica de VoiceOver do botão da Lente dizia "MULETAS, frases feitas…" para abrir uma tela cujas seções se chamam "Palavras de apoio" e "Frases de outro", e passa a nomear o que a tela mostra. As seis fases do `design-router` citadas na ADR 06i. **O M-2 está capturado** (`ferramentas/orca/a5b-m2-esquecer-dialogo.png`, volta A-5-B): o diálogo diz "Esquecer tudo o que o Traço registrou?" sobre "Os sinais somem do aparelho. As notas ficam.", com o rótulo "O que o Traço registrou — contagem, não conclusão" na mesma tela; a alegação anterior de que o instrumento não permitia a captura estava errada e o revisor a derrubou — a lei da ESTEIRA tira o maestro quando há vários simuladores ligados e no mesmo parágrafo nomeia o substituto, `xcrun simctl io <UDID> screenshot`. O M-3 continua sem foto porque VoiceOver não fotografa ADR08f (volta V12-B): o encaixe da Página cola no pé — `.frame(maxHeight:tetoDoEncaixe, alignment: .bottom)` — e com isso caem juntos o fantasma do `.sheet` (o cartão saltava ~176 pt e ficava em duas geometrias ao abrir os campos; medido em quadros nativos com e sem Reduzir Movimento, `v12b-fantasma-antes.png` contra `v12b-fantasma-depois-{sem,com}-rm.png`) e o vão de 86,7 pt entre o aviso e a barra, que virou os 12,7 pt do próprio padding; `Pilula` desabilitada sai de 1,53:1 para 5,04:1 no papel (`tintaFraca`) e guarda a cápsula por hairline, com o contorno à mão da `RecordarView` apagado (hairline dobrada, RGB 211 → 227) e portão em `PilulaContrasteTests`; itens 2 e 4 do re-G4 da V12 refotografados no estado exato (AX5 com cartão e teclado passa sem tocar em código: `v12b-antes-ax5-vestida.png`); suíte 890/0 em 144 suítes em 08/09/2026. V12-D (correção do re-G3): a letra `08c` colidia com `main` e vira `08f` em toda referência; o quadro longo da rolagem (92–176 ms em 3 de 6 rodadas da V12-C) foi isolado com sonda por quadro no `CadernoHitchesTests` — NÃO era o aviso da análise (o encaixe fica em 192 → 192 pt e sob teste o aviso nem entra) nem o app: era a captura do próprio teste (`drawHierarchy` + PNG de 1,3 MB, 127–162 ms na main thread cronometrados) a cair a +0,15 s da fase seguinte; tirada das janelas medidas, 6 de 6 rodadas fecham com 0 quadros perdidos na espera, na primeira descida e na rolagem em regime (2843 quadros). V12-E (segunda recusa do G4, conselho Astra): a invariante da escrita visível entra na 08f — "em cada quadro em que a Página recebe escrita, a linha ativa inteira e o caret pertencem à área livre do papel; nenhuma outra superfície desenha nessa área". A lei mora no contêiner: `CadernoView` troca o `.safeAreaInset` (o papel corria por baixo do encaixe: 0 pixels de caret em 11 amostras do G4) por uma pilha de irmãos sem pixel em comum, e `EscritaVisivel.seguirCaret` rola o papel de verdade até a linha do caret (texto, foco e janela do ScrollView; só com foco). `EscritaVisivelTests` mede na Página real, hospedada, com teclado de software, em `large` e AX5, encaixe vazio/cartão/aviso, fim e meio: AX5 31/31 e large 44/44; vermelho com camada intrusa plantada (59) e com superfície do encaixe sobre o papel (59). Na tela: caret a 466 pixels acima do cartão em `large` (quatro amostras no ciclo do piscar), no fim e no meio; suíte 892/0 em 146 suítes, build sem aviso, 08/09/2026. Limite escrito: sob xctest o teclado nasce fora da tela e o desvio é reposto por `additionalSafeAreaInsets`; a prova do fantasma de "Abrir os campos" é amostrada por quadros nativos, não universal — o oráculo de pixels vai ao RUMO. V12-F (correção do re-G3): o `testLargeComCartao` passava verde sem cartão e sem tocar "Abrir os campos"; cartão, botão e folha viram pré-condições que falham com o motivo (planta vermelha colada), `-autoAnalise <true/>` tira o estado do aparelho da prova (a forma `1` é string e não chega ao `as? Bool`), e a refilmagem pelo caminho certo dá 0 quadros com par legível em 69, sem e com Reduzir Movimento (`v12f-abrir-{sem,com}-rm.mp4`). V12-G (correção do re-G4, B1): o pé do encaixe voltou a ser rígido — como irmão na pilha ele aceitava a proposta e o corpo se dividia a meio, e em AX5 com cartão "Mais ações da nota" entrava 52,7 pt sob o teclado (sonda de geometria colada); agora 8 pt de ar sobre a barra preditiva, sem e com RM (`v12g-ax5-cartao-pe-inteiro{,-rm}.png`), o condutor mede o pé contra o `inputView` e falha se ele entrar, os dois casos "encaixe vazio" rodaram verdes pela primeira vez, AX5 com RM refilmado (o único par que resta é o menu do sistema a dissolver, ~60 ms, fora do encaixe), suíte 891/892 (1 pulado). Custo dito: no iPhone de 874 pt o cartão recolhido perde 15 pt do topo pela regra da metade da 05y. **ADR 09e (volta C1-D, 09/09/2026): a C1 fundida com o `main`** — 981 testes em 157 suítes verdes na árvore MESCLADA no 17e, 0 avisos (os 25 testes que o `main` trouxe e os da C1 passam juntos), e a prova por quadro REEXECUTADA no Pro Max `6033B043` com teclado real de 318 pt nos dois tamanhos — a ressalva de prova herdada sai. Achado da fusão: a etiqueta de origem do `main` (08u/09b) fica ACIMA do editor e toma 30 pt do papel em AX5 (25 em `large`); a invariante 08f passa a MEDIR esse estado, com portão que reprova se a cápsula não desenhar. Relato em ferramentas/orca/c1d-reconciliacao.md. |
| Privacidade e autoria em todas rotas | Origem protegida/ausente restringe Trabalho, busca e produção; intercâmbio Markdown revalida acesso e identifica autoria externa; recuperação local preservada; ADR05s: matriz do selo provada por célula — expressiva (em curso e fechada), selada e queimada × `.md` do corpus, índice/PELO SENTIDO, Spotlight, widget, contexto da sábia, export completo, import — e a ordenação backups/Spotlight/índice fechada por geração (teste com ordem forçada). ADR 06h (voltas A3 e A-B): a escrita pessoal FICA DO AUTOR — não vira método nenhum, venha o método antes ou depois da Expressiva no catálogo, e o rodapé do Destaque obedece o mesmo cálculo. O revisor da M3 mediu 22 desabafos chegando VESTIDOS de método (14 linhas curtas pelo teto de 120 que pulava a Expressiva, 8 longos e factuais pelo léxico de dez palavras dela), com "Me arrependi e chorei" recebendo "que defeito você conteve?" do Exame da noite; o revisor da A-B mediu mais 15 de 20 ainda vestidos porque a primeira guarda só valia para a CAUDA do catálogo. `AnaliseLocal.eEscritaPessoal` é a guarda, em código e não no `Metodos.json` (a pasta do autor reescreve o catálogo, e alargar a Expressiva por dado deixaria os dois métodos novos inalcançáveis — 287 sondas), com CINCO famílias e não uma lista de frases: o estado por radical, o juízo sobre si, o funcionamento básico negado, o que eu fiz a alguém (qualquer tamanho) e o que eu deixei de fazer (só acima do teto). E a proteção vence o modelo: `Sessao.escolher` ganhou a quarta linha — escrita pessoal reconhecida pelo algoritmo CALA o modelo —, sem a qual a guarda era NULA com Grok ou Apple Intelligence ligados, porque `.silencio` tem precedência zero. Régua: 57 frases (22 da M3 + 15 do G3 + 20 minhas, uma por porta do catálogo, cada uma provada batendo na regex que declara), e sem a guarda o teste acusa as 22 uma a uma; custo REMEDIDO depois do conserto: 14 dos 287 ramos fecham para texto longo (3 da Coluna, 11 do Exame) e nenhum método fica sem porta. **ADR 06i (volta A-5): a guarda calava o que não devia, e agora reconhece o SENTIMENTO COMO ASSUNTO.** O revisor do re-G3 mediu o outro lado e achou a regressão que a 06h introduziu em main: de dez notas comuns de trabalho, NOVE mudavam de destino e OITO eram regressão limpa, porque `vazio`, `sozinho`, `cansado`, `pesa`, `ansioso` e `medo` são palavras de trabalho tanto quanto de desabafo — "Quero correr de manhã, mas o MEDO de me machucar me trava" deixou de receber WOOP num app cujo campo do WOOP se chama "OBSTÁCULO INTERNO (O SEU HÁBITO/MEDO)", e "estado vazio, carregando e falha" calava. A família 1 se parte em duas: 1a, o sentimento de uma vida só, dispara sozinho (e `senti` ganhou borda de palavra — sem ela "o sentido dele" e "o sentimento do cliente" calavam a nota); 1b, a palavra de DUPLA VIDA, só decide com companhia — o autor no meio (primeira pessoa + verbo de estado com complemento pronome, "estou sozinho nisso", nunca "estou cansado desse módulo"; `medo` PREDICADO e não NOMEADO; `vazio` substantivo e não adjetivo de tela; `pesa` sem carga nomeada), densidade (duas de dupla vida na mesma nota) ou a omissão ao lado em qualquer tamanho. E a régua que faltava, que é o coração da volta: **a direção inversa**, 47 frases com no mínimo DUAS POR PORTA das 21 formas de main (as dez do revisor incluídas), com `todaPortaDeMainTemPeloMenosDuasFrases` cobrando a cobertura contra o catálogo. As duas réguas correm JUNTAS em `asDuasReguasValemAoMesmoTempo`, nenhum caso precisou de arbitragem. **Volta A-5-B (o preço, medido pelo revisor e pago): o estreitamento não foi de graça, e a causa era a CAUDA e não o radical.** Ele replicou a guarda em Swift lendo os literais direto do `AnaliseLocal.swift`, escreveu 20 desabafos novos que usam só vocabulário 1b MAIS um gancho de roteamento — o recorte que NENHUMA das duas réguas exercitava — e mediu: em `main` 19 dos 20 caíam no silêncio; na A-5, **18 dos 20 passavam a ser VESTIDOS** em quatro campos ("Quero sumir uns dias, ando muito cansado ultimamente" → WOOP; "Percebi que estou sozinha faz meses" → Nota permanente). Só 7 das 57 protegidas exercitam a família 1b, e nenhuma tem gancho: o ramo mais novo do código era o menos coberto pela régua. O conserto alarga a CAUDA (`caudaDoSentimento`, constante própria) e não o radical, porque foi o radical largo que causou a regressão da 06h: intensificador posposto ("cansado demais"), advérbio de tempo ("sozinha faz meses", "exausto por dois dias", "ansioso desde que…"), `acordo cansado`/`fico com um medo`/`bate um medo`/`morrendo de medo`, e os substantivos `ansiedade`/`cansaço`. **Os dez casos que o revisor citou por extenso ficam calados, 10 de 10.** `dá medo` foi RECUSADO com o motivo escrito na ADR: a régua inversa dele mesmo tem "o que me dá medo é ninguém avisar a tempo" esperando Pré-mortem — primeiro caso de arbitragem, e o lado é o do trabalho. E `vazi[oa]` virou `vazi`: era o único radical de `lexicoDeDuplaVida` que capturava a própria flexão, então "a lista vazia e o estado vazio da tela" contava como duas palavras e disparava a densidade sozinha, contra o comentário do próprio código e contra a ADR. **A régua ganhou um TERCEIRO bloco, e é a lição: `comGancho`, dez desabafos com gancho de roteamento, um por gancho** — as três correm juntas, 57 protegidas + 10 com gancho + 6 legítimas + 48 de trabalho, suíte 752/0 em 128 suítes em 06/09/2026. **Volta A-5-C (ADR 06i-C): a forma da cauda, e a borda de palavra varrida até o fim.** O re-G3 pôs três dimensões em 8 e as três desciam da MESMA raiz: a cauda da 06i-B foi escrita como TERMINADOR, e o intensificador posposto dentro dela curto-circuitava o teste do OBJETO — "estou cansado demais desse módulo para reescrever a função" virava pessoal, sendo o exemplo canônico do lado trabalho DESTA ADR (uma palavra a mais troca o lado da frase). O intensificador vira TRANSPARENTE (`intensificadorPosposto`, entre o adjetivo e a cauda), e `pra isso` entra na cauda como complemento pronominal: "cansado demais pra isso" é desabafo, "cansado demais desse módulo" é trabalho. **E a classe do defeito foi fechada por varredura:** o padrão da volta inteira era um só — radical sem `\b` casando dentro de outra palavra (`senti` na 06i, `vazi[oa]` na 06i-B, `ando ` agora) — e a guarda foi conferida por completo, **os 8 léxicos e as 82 alternativas, uma por uma: 11 pontos precisaram de `\b` (as três listas de verbos contam três) e o resto estava limpo.** Cada um com frase real medida antes e depois: `\b` nas três listas de verbos (`ando ` casava dentro do gerúndio, "trabalhando cansado demais"; `bate ` dentro de "combate um medo"), `\bcansad` ("o time está descansado e a fila vazia"), `\bmedo`, `\bcansaço`, `\bsou (o|um|uma)` ("ele pen**sou o** problema todo"), `\bme (odi|culp|…)` ("o fil**me odi**ado pela crítica"), `\bculpad` ("o erro foi des**culpad**o") e `\bbriguei` ("me a**briguei** da chuva"). Três suspeitas foram medidas e estão limpas: `exaust` em "busca exaustiva", `vazi` em "esvazia" e `\bpesa` em "pesa demais". **E `dá medo` deixa de ser arbitragem:** a 06i-B nomeou o discriminador PREDICAR/NOMEAR na própria frase da recusa sem implementá-lo, e agora ele existe — `\bd[áa] (um |uma )medo|\bd[áa] medo de \w+r\b|(^|[.!?]\s*)d[áa] medo`: PREDICAR leva artigo, pede infinitivo ou abre a frase; NOMEAR ("o que me dá medo é ninguém avisar") não faz nenhum dos três e continua indo ao Pré-mortem. O `de` + INFINITIVO é correção medida sobre a proposta do revisor, que largo comia "o que dá medo de verdade nesse plano". **A ADR volta a não ter nenhum caso de arbitragem, e é melhor assim.** Mais dois buracos do mesmo ramo: `bate|bateu|dá|deu` entram no ramo de `ansiedade|cansaço` (a 06i-B pôs `bate` só no ramo do `medo`) e `por dentro` na lista de sujeitos do `pesa`. As 121 frases das réguas existentes foram medidas antes e depois num binário que copia as linhas 111–238 do `AnaliseLocal.swift` VERBATIM (o método do revisor): **0 mudanças**; os 12 casos que mudaram de lado são exatamente os 12 alvo. As três réguas passam a 57 protegidas + 13 com gancho + 6 legítimas + 52 de trabalho, suíte 752/0 em 128 suítes em 06/09/2026. **Volta A-5-D (ADR 06i-D): as duas bordas que faltavam, e duas frases de honestidade.** O revisor reconstruiu o binário verbatim sobre a A-5-C e mediu 122 frases das réguas com 0 problemas; sobraram dois pontos da MESMA classe de borda que a varredura não alcançou — `\btratei mal` (hoje casava dentro de "**contratei** mal" e "**retratei** mal") e `me d[áa]|me deu` no lugar do `d[áa]|deu` largo, que comia a ansiedade DO USUÁRIO ("a fila dá ansiedade no usuário", "esse fluxo dá cansaço"); o segundo é correção sobre a minha implementação, não sobre a proposta: a sugestão foi do revisor e eu a implementei mais larga do que ele mediu. Seis frases novas na régua inversa (58 de trabalho), as seis medidas VERMELHAS antes do conserto e verdes depois. **E duas frases da ADR foram corrigidas por desonestidade, que é o motivo de o revisor ter baixado Estado honesto de 9 para 8:** (1) o braço `\bd[áa] medo de \w+r\b` NÃO é discriminador, é ARBITRAGEM declarada — ele come Pré-mortem que usa a mesma forma ("dá medo de perder o cliente se o deploy falhar"); as três saídas estão medidas na ADR e a escolhida vence pela ASSIMETRIA (0 carimbos contra 2 silêncios: calar é reversível pelo autor, carimbar não), então a 06i TEM um caso de arbitragem e é este; (2) "a classe está fechada" vira "a classe foi VARRIDA" — as alternativas foram lidas uma a uma e as sem borda à esquerda foram medidas contra os candidatos que soubemos nomear, o que não prova que não há mais nada, e esta volta achou mais dois exatamente aí; o resíduo sem conserto grátis é a família do adjetivo sem cópula ("dia vazio", "gente cansada"), que exige análise sintática. Suíte 752/0 em 128  | Validar revogar/liberar com seletor/exportador aberto e durabilidade de recuperação; cópia já entregue externamente não é revogável; iCloud que aceita e descarta depois não é visto . **ADR 06i (volta A-5): a guarda calava o que não devia, e agora reconhece o SENTIMENTO COMO ASSUNTO.** O revisor do re-G3 mediu o outro lado e achou a regressão que a 06h introduziu em main: de dez notas comuns de trabalho, NOVE mudavam de destino e OITO eram regressão limpa, porque `vazio`, `sozinho`, `cansado`, `pesa`, `ansioso` e `medo` são palavras de trabalho tanto quanto de desabafo — "Quero correr de manhã, mas o MEDO de me machucar me trava" deixou de receber WOOP num app cujo campo do WOOP se chama "OBSTÁCULO INTERNO (O SEU HÁBITO/MEDO)", e "estado vazio, carregando e falha" calava. A família 1 se parte em duas: 1a, o sentimento de uma vida só, dispara sozinho (e `senti` ganhou borda de palavra — sem ela "o sentido dele" e "o sentimento do cliente" calavam a nota); 1b, a palavra de DUPLA VIDA, só decide com companhia — o autor no meio (primeira pessoa + verbo de estado com complemento pronome, "estou sozinho nisso", nunca "estou cansado desse módulo"; `medo` PREDICADO e não NOMEADO; `vazio` substantivo e não adjetivo de tela; `pesa` sem carga nomeada), densidade (duas de dupla vida na mesma nota) ou a omissão ao lado em qualquer tamanho. E a régua que faltava, que é o coração da volta: **a direção inversa**, 47 frases com no mínimo DUAS POR PORTA das 21 formas de main (as dez do revisor incluídas), com `todaPortaDeMainTemPeloMenosDuasFrases` cobrando a cobertura contra o catálogo. As duas réguas correm JUNTAS em `asDuasReguasValemAoMesmoTempo`, nenhum caso precisou de arbitragem. **Volta A-5-B (o preço, medido pelo revisor e pago): o estreitamento não foi de graça, e a causa era a CAUDA e não o radical.** Ele replicou a guarda em Swift lendo os literais direto do `AnaliseLocal.swift`, escreveu 20 desabafos novos que usam só vocabulário 1b MAIS um gancho de roteamento — o recorte que NENHUMA das duas réguas exercitava — e mediu: em `main` 19 dos 20 caíam no silêncio; na A-5, **18 dos 20 passavam a ser VESTIDOS** em quatro campos ("Quero sumir uns dias, ando muito cansado ultimamente" → WOOP; "Percebi que estou sozinha faz meses" → Nota permanente). Só 7 das 57 protegidas exercitam a família 1b, e nenhuma tem gancho: o ramo mais novo do código era o menos coberto pela régua. O conserto alarga a CAUDA (`caudaDoSentimento`, constante própria) e não o radical, porque foi o radical largo que causou a regressão da 06h: intensificador posposto ("cansado demais"), advérbio de tempo ("sozinha faz meses", "exausto por dois dias", "ansioso desde que…"), `acordo cansado`/`fico com um medo`/`bate um medo`/`morrendo de medo`, e os substantivos `ansiedade`/`cansaço`. **Os dez casos que o revisor citou por extenso ficam calados, 10 de 10.** `dá medo` foi RECUSADO com o motivo escrito na ADR: a régua inversa dele mesmo tem "o que me dá medo é ninguém avisar a tempo" esperando Pré-mortem — primeiro caso de arbitragem, e o lado é o do trabalho. E `vazi[oa]` virou `vazi`: era o único radical de `lexicoDeDuplaVida` que capturava a própria flexão, então "a lista vazia e o estado vazio da tela" contava como duas palavras e disparava a densidade sozinha, contra o comentário do próprio código e contra a ADR. **A régua ganhou um TERCEIRO bloco, e é a lição: `comGancho`, dez desabafos com gancho de roteamento, um por gancho** — as três correm juntas, 57 protegidas + 10 com gancho + 6 legítimas + 48 de trabalho, suíte 752/0 em 128 suítes em 06/09/2026. **Volta A-5-C (ADR 06i-C): a forma da cauda, e a borda de palavra varrida até o fim.** O re-G3 pôs três dimensões em 8 e as três desciam da MESMA raiz: a cauda da 06i-B foi escrita como TERMINADOR, e o intensificador posposto dentro dela curto-circuitava o teste do OBJETO — "estou cansado demais desse módulo para reescrever a função" virava pessoal, sendo o exemplo canônico do lado trabalho DESTA ADR (uma palavra a mais troca o lado da frase). O intensificador vira TRANSPARENTE (`intensificadorPosposto`, entre o adjetivo e a cauda), e `pra isso` entra na cauda como complemento pronominal: "cansado demais pra isso" é desabafo, "cansado demais desse módulo" é trabalho. **E a classe do defeito foi fechada por varredura:** o padrão da volta inteira era um só — radical sem `\b` casando dentro de outra palavra (`senti` na 06i, `vazi[oa]` na 06i-B, `ando ` agora) — e a guarda foi conferida por completo, **os 8 léxicos e as 82 alternativas, uma por uma: 11 pontos precisaram de `\b` (as três listas de verbos contam três) e o resto estava limpo.** Cada um com frase real medida antes e depois: `\b` nas três listas de verbos (`ando ` casava dentro do gerúndio, "trabalhando cansado demais"; `bate ` dentro de "combate um medo"), `\bcansad` ("o time está descansado e a fila vazia"), `\bmedo`, `\bcansaço`, `\bsou (o|um|uma)` ("ele pen**sou o** problema todo"), `\bme (odi|culp|…)` ("o fil**me odi**ado pela crítica"), `\bculpad` ("o erro foi des**culpad**o") e `\bbriguei` ("me a**briguei** da chuva"). Três suspeitas foram medidas e estão limpas: `exaust` em "busca exaustiva", `vazi` em "esvazia" e `\bpesa` em "pesa demais". **E `dá medo` deixa de ser arbitragem:** a 06i-B nomeou o discriminador PREDICAR/NOMEAR na própria frase da recusa sem implementá-lo, e agora ele existe — `\bd[áa] (um |uma )medo|\bd[áa] medo de \w+r\b|(^|[.!?]\s*)d[áa] medo`: PREDICAR leva artigo, pede infinitivo ou abre a frase; NOMEAR ("o que me dá medo é ninguém avisar") não faz nenhum dos três e continua indo ao Pré-mortem. O `de` + INFINITIVO é correção medida sobre a proposta do revisor, que largo comia "o que dá medo de verdade nesse plano". **A ADR volta a não ter nenhum caso de arbitragem, e é melhor assim.** Mais dois buracos do mesmo ramo: `bate|bateu|dá|deu` entram no ramo de `ansiedade|cansaço` (a 06i-B pôs `bate` só no ramo do `medo`) e `por dentro` na lista de sujeitos do `pesa`. As 121 frases das réguas existentes foram medidas antes e depois num binário que copia as linhas 111–238 do `AnaliseLocal.swift` VERBATIM (o método do revisor): **0 mudanças**; os 12 casos que mudaram de lado são exatamente os 12 alvo. As três réguas passam a 57 protegidas + 13 com gancho + 6 legítimas + 52 de trabalho, suíte 752/0 em 128 suítes em 06/09/2026. **Volta A-5-D (ADR 06i-D): as duas bordas que faltavam, e duas frases de honestidade.** O revisor reconstruiu o binário verbatim sobre a A-5-C e mediu 122 frases das réguas com 0 problemas; sobraram dois pontos da MESMA classe de borda que a varredura não alcançou — `\btratei mal` (hoje casava dentro de "**contratei** mal" e "**retratei** mal") e `me d[áa]|me deu` no lugar do `d[áa]|deu` largo, que comia a ansiedade DO USUÁRIO ("a fila dá ansiedade no usuário", "esse fluxo dá cansaço"); o segundo é correção sobre a minha implementação, não sobre a proposta: a sugestão foi do revisor e eu a implementei mais larga do que ele mediu. Seis frases novas na régua inversa (58 de trabalho), as seis medidas VERMELHAS antes do conserto e verdes depois. **E duas frases da ADR foram corrigidas por desonestidade, que é o motivo de o revisor ter baixado Estado honesto de 9 para 8:** (1) o braço `\bd[áa] medo de \w+r\b` NÃO é discriminador, é ARBITRAGEM declarada — ele come Pré-mortem que usa a mesma forma ("dá medo de perder o cliente se o deploy falhar"); as três saídas estão medidas na ADR e a escolhida vence pela ASSIMETRIA (0 carimbos contra 2 silêncios: calar é reversível pelo autor, carimbar não), então a 06i TEM um caso de arbitragem e é este; (2) "a classe está fechada" vira "a classe foi VARRIDA" — as alternativas foram lidas uma a uma e as sem borda à esquerda foram medidas contra os candidatos que soubemos nomear, o que não prova que não há mais nada, e esta volta achou mais dois exatamente aí; o resíduo sem conserto grátis é a família do adjetivo sem cópula ("dia vazio", "gente cansada"), que exige análise sintática. Suíte 752/0 em 128 suítes em 06/09/2026. **Volta A-6 (ADR 06i-E): a confissão curta ainda chegava VESTIDA, e o buraco era de AMOSTRA, não de léxico.** O revisor da volta M3, medindo a guarda contra o catálogo de 28 métodos, provou na tela dele (`m3-reg3-01-confissao-curta-vestida.png`) que "Não devia ter reagido assim com ele." — 36 caracteres — virava Exame da noite, porque `não devia ter` morava na família 5 (só ACIMA do teto de 120) e não na família 4 (qualquer tamanho); no mesmo tamanho e no mesmo ato de fala, "fui grosso com ele hoje" e "perdi a paciência com ele hoje" eram caladas — a assimetria era arbitrária. `não devia ter` passa para a família 4, que passa a se chamar pelo que sempre foi: **confissão de conduta**; `engoli`, `fiquei calad` e `deixei passar` ficam na 5 porque curtas elas nomeiam uma conversa e a Coluna serve. **E o item que vale mais que o conserto:** as 57 protegidas carregavam um rabo de ~140 caracteres, então TODA sonda ficava acima do teto e o caso curto de cada família nunca era exercitado — foi por isso que quatro voltas de régua (06i a 06i-D) passaram por cima da assimetria. `curtasPorFamilia` põe **uma sonda ABAIXO do teto por família**, cada uma com a porta que a levaria vestida, e `cadaFamiliaTemUmaSondaAbaixoDoTeto` deixa VERMELHO quem ficar sem. Corrigida a frase da 06h/06i que era falsa abaixo do teto ("14 ramos fechados para TEXTO LONGO"): os 3 da Coluna fecham só acima do teto, os 11 do Exame fecham em qualquer tamanho (eram 7 de 11), e abaixo do teto o Exame continua alcançável pelo primeiro ramo e por "hoje eu fiz|reagi|tratei". Régua medida VERMELHA antes do conserto e verde depois; quatro réguas juntas com 64 protegidas + 13 com gancho + 6 legítimas + 58 de trabalho; suíte 780/0 em 130 suítes em 06/09/2026 | Validar revogar/liberar com seletor/exportador aberto e durabilidade de recuperação; cópia já entregue externamente não é revogável; iCloud que aceita e descarta depois não é visto. **Dívida residual (não regressão), no RUMO:** a regra de densidade cala nota de sistema com duas palavras de dupla vida ("o medo é o servidor cair" + "equipe cansada") — 15 de 20 notas de trabalho novas do revisor seguem caladas, **e as mesmas 20 eram caladas em `main` também**; oito delas chegariam a uma forma real sem a guarda. Os dois substantivos novos da A-5-B alargam essa densidade em dois termos. O critério continua sintático: mede quem é o sujeito da frase, não o que o autor sente |
| Métodos e pesquisa com proveniência | Catálogo existente; ADR05o: o corpus leva o id do método e a nota conserva gesto e campos depois que o método sai da pasta do autor (suíte 578/0 em 05/09/2026). ADR05x (volta 16): cada um dos 21 métodos traz `proveniencia` aditiva — fonte, função (prática, lente ou estudo com evidência delimitada), o que o Traço adaptou, evidência ("sem evidência específica conhecida" onde não há) e aplicabilidade; a Lente mostra "De onde vem" recolhida por padrão e o Perfil a mesma proveniência por método (do autor: "a que você escreveu" ou "não informada"); nota cujo método saiu da pasta diz isso na Lente, sem bloquear; nenhum selo de eficácia; a linha do método no Perfil é o toque (alvo 44 sem linha a mais) e as cinco linhas são um componente só (`LinhasDeProveniencia`); método ausente em `tintaSuave`, não em `aviso`; 4 testes novos, suíte 717 testes, 716 passam, 1 falha alheia (F2, permissão do simulador) em 06/09/2026. ADR 06f (volta A1): o aviso que interrompe a escrita passou pela mesma régua — a frase "Afirmação sem prova não gruda" alegava o que Wood, Perunovic e Lee (2009) NÃO mediram (o estudo mede humor logo depois de repetir uma frase dada, não fixação) e foi trocada pelo achado nos dois sentidos mais a mesma pergunta pelo fato; `AnaliseLocal.proveniencia(doAviso:)` devolve FONTE/FUNÇÃO/EVIDÊNCIA no formato da 05x (o aviso do plano sem obstáculo reusa a proveniência do WOOP no catálogo; aviso que é regra do app devolve nil, sem origem inventada); 5 testes travam a regra "nenhum aviso alega eficácia", provados contra o texto antigo; suíte 724/126 em 06/09/2026 | A proveniência é texto do catálogo, não verificação; a bibliografia dos 21 vive só em `Metodos.json`; não viaja no prompt da sábia; o autor não é avisado quando um método some (só vê ao abrir a Lente); sem edição pela tela 2026-09-06e (volta M3): sete métodos novos entram no FIM do catálogo — Subtração, Coluna da esquerda, Classe de referência, Cinco porquês (M1), A pergunta de Hamming, O que se vê e o que não se vê, Exame da noite (M2) —, preenchendo as três faculdades vazias (simplificação, direção, consequência); a POSIÇÃO no arquivo é PARTE da proteção da escrita pessoal, não toda ela — o G3 mediu que a proteção é a ordem MAIS o teto de 120 caracteres de `AnaliseLocal.detectarGesto`, que pula a Expressiva em texto curto e promove `colunaEsquerda` e `exameDaNoite` a primeiro-a-casar (14 de 14 linhas curtas com palavra de sentimento), e que acima do teto o léxico de dez palavras da Expressiva não cobre o desabafo factual (8 de 8): 22 desvios em 58 frases do revisor, reproduzidos na tela em `m3-rev-02` e `m3-rev-03`; as "0 desvio em 74 frases" do autor eram AMOSTRA dele, não propriedade do catálogo; `oDesabafoLongoContinuaExpressivo` prova só o caso fácil e `aEscritaPessoalNaoChegaVestidaDeMetodo` cobrava as 22 e FALHAVA de propósito nesta volta; na COLAGEM com main (volta M3-C) as 22 passaram sem uma linha de `Traco/Analise` tocada aqui, porque a guarda das voltas A1–A5 (ADRs 06h e 06i) chegou antes — nenhuma das 22 sobrou; conserto do `\b` no roteamento do Se–então ("sempre que" casava dentro de "sempre quebra" e "sempre queria"), com teste que fixa; guarda de dado contra botão morto (`nenhumEncadeamentoApontaParaMetodoInexistente` sobre `Catalogo.todos`); guarda de alcance de ramo (`todoRamoDeRegexAlcancaOSeuMetodo`: uma frase gerada por ramo de regex de `Catalogo.todos`, 287 sondas nos 28, cobrando que ela chegue no método que a declara — nenhum método futuro nasce com ramo morto sem alguém saber); os desvios conhecidos são DEZOITO, remedidos na colagem com a lista vazia e não herdados da 06h, e o estreitamento da A5 e das 06i-B/C/D não mudou a conta: 4 de regex larga comendo regex específica (os quatro antigos seguem vivos) e 14 da guarda da escrita pessoal calando a sonda — 3 ramos da Coluna da esquerda e 11 do Exame da noite, sendo que o SEGUNDO ramo do Exame (`não devia ter …`, `me arrependi`, `fui injusto|grosso|duro demais|ríspido`) está INTEIRO fechado, 9 de 9, e o método só continua alcançável por `exame da noite`, `passei o dia em revista` e `hoje eu (fiz|reagi|tratei)` — é a proteção funcionando e é o preço dela; duas frases desta volta morreram na colagem e ESTAVAM ERRADAS ("perdi a paciência na reunião e me arrependi" e "fui injusto com o time hoje de manhã" são confissão de conduta e o teste pedia o roubo que a 06h proíbe), trocadas por três que convocam o método sem confessar; e as sete portas novas ganharam régua — `todaPortaDeMainTemPeloMenosDuasFrases` cobrou duas notas de trabalho por porta de `Catalogo.doApp` e as sete entraram com 14 frases (58 → 72 em `EscritaPessoalTests.trabalho`), com as três réguas de main valendo com os sete colados e sem um caractere de guarda enfraquecido; três frases de `reconhecimento` que julgavam o autor voltaram à voz da casa (Coluna da esquerda, Hamming e Exame da noite: "o seu dia pedindo julgamento — dos seus atos" servido a quem escreveu que chorou virou "o seu dia em revista — e uma regra para amanhã"), mitigação e não cura; e três frases de honestidade nos 21 antigos, onde o grau de origem estava declarado acima do real — Decisão (prática atribuída a Kahneman, sem texto dele que a descreva), Primeiros princípios (Aristóteles não propõe o exercício) e Inversão (discursos sem transcrição localizada, Jacobi como atribuição). Suíte integral 785/0 em 130 suítes na colagem com main (06/09) e 723/0 em 125 suítes no fecho da primeira volta, target do app compilando limpo e 4 avisos (2 únicos) em `ConferenciaTrabalhoTests.swift:381` pré-existentes de `73b1ebc` ("build sem aviso" era falso), e o Perfil dizendo "28 do app" com a proveniência dos novos abrindo no simulador, em 06/09/2026. ADR 06f (volta A1): o aviso que interrompe a escrita passou pela mesma régua — a frase "Afirmação sem prova não gruda" alegava o que Wood, Perunovic e Lee (2009) NÃO mediram (o estudo mede humor logo depois de repetir uma frase dada, não fixação) e foi trocada pelo achado nos dois sentidos mais a mesma pergunta pelo fato; `AnaliseLocal.proveniencia(doAviso:)` devolve FONTE/FUNÇÃO/EVIDÊNCIA no formato da 05x (o aviso do plano sem obstáculo reusa a proveniência do WOOP no catálogo; aviso que é regra do app devolve nil, sem origem inventada); 5 testes travam a regra "nenhum aviso alega eficácia", provados contra o texto antigo; suíte 724/126 em 06/09/2026 | A proveniência é texto do catálogo, não verificação; a bibliografia dos 21 vive só em `Metodos.json`; não viaja no prompt da sábia; o autor não é avisado quando um método some (só vê ao abrir a Lente); sem edição pela tela; a correção da UI do botão de encadeamento sem destino (ele não devia sequer acender) é volta própria; seis desvios de roteamento pré-existentes seguem abertos — `(?m)^quero` do WOOP engole Pré-mortem, Feynman, Primeiros princípios e Prática deliberada, `ideia` da Nota permanente engole Destilar, e `\bo dia de hoje\b` do Dia engole `\bolhando o dia de hoje\b` do Exame da noite; o roteamento na TELA é do degrau de bordo, que decide antes da regex e pode discordar dela (o esquema dele já nasce de `Catalogo.todos`); os três métodos da M4 e os pedidos de app que a forma livre levantou (campo repetível, compromisso recorrente, campo emparelhado, Classe de referência lendo o corpus) não entraram. Da A2 (ADR 06g): o `@Generable enum GestoDeBordo` com dez casos e o `instrucoes` com dez definições, mortos desde a 04l mas ainda no arquivo, foram apagados — o esquema e o prompt já nasciam de `Catalogo.todos`, e o código morto fez três leitores concluírem que a análise de bordo conhecia dez formas; o custo do catálogo inteiro no esquema foi MEDIDO (0,77 s com dez ids contra 0,79 s com 21, +3,3%, 7 frases no iPhone 17 Pro de teste) e não é proibitivo — com dez, as cinco frases que convocavam método fora da lista não tinham como acertar. Da A1: a proveniência do aviso EXISTE no motor e ainda NÃO aparece na tela — `CartaoAnaliseView` está aberta na volta 12 e não foi tocada; falta a linha "De onde vem" no cartão de Aviso e a captura em AX5 (no tamanho AX o botão Analisar não é alcançável pelo maestro, defeito de view da volta 12); o gatilho da afirmação vazia segue estreito |
| Domínios amplos de realização | Primeira jornada local proposta | Casos distintos criação/aprendizagem/organização e dependências externas honestas |
| Fora do app (widgets, tela bloqueada, Ilha, Siri, URL) | ADR05u: um snapshot versionado no App Group escrito só pelo app (`Superficie`), catálogo em `Traco/App/Intents/` com `Compartilhado/` no widget, dois `LiveActivityIntent` com identidade e confirmação real (feito sem toggle, soneca pelo orçamento), entidades mínimas com selo na consulta e no `perform()`, reconciliação de atividades no arranque/retorno/comando, `isStale` neutraliza, tipografia por `Tema` (zero tamanhos fixos), D6 fechado; correções do G3 (06/09): causa real do reload recusado achada no chronod (nome do produto "Traço" em NFD no disco × NFC no Info.plist; produto vira `Traco`, exibido segue "Traço") e volta à cena repete o que não foi confirmado (par revisão publicada/recarregada), suíte isolada da superfície real num ponto só do arranque, Destaque inteiro no widget pequeno, hora absoluta nos dois widgets, VoiceOver do botão vivo diz a linha, `#Preview` por família e estado, um tipo só para o próximo; 20 testes em `ForaDoAppTests` em 06/09/2026; ADR05w (F3, 06/09): controle "Anotar" (`ControlWidget`) na Central de Controle, na tela bloqueada e no botão de Ação → `CapturarIntent` compartilhado (só o app executa, contexto demonstrado no log) → `Rota.captura(ditado:)` tipada, guardada e consumida UMA vez pela cena pronta (`Rota.consumir`, arranque frio incluso) → Página em branco com o teclado pronto e o microfone a um toque; 22 testes; **ADR06c (F3b, 06/09): o áudio antes da letra** — o controle (agora "Ditar", com ícone de microfone) abre GRAVANDO; `AVAudioRecorder` escreve o m4a direto no destino final do anexo, a nota entra no disco SEM UMA LETRA e só então `SFSpeechURLRecognitionRequest` (`requiresOnDeviceRecognition`) é pedido; falha, silêncio ou app morto **depois do depósito** deixam a nota com o áudio tocável dentro (`[audio:…](traco://audio/<id>)`, cofre de anexos, zero campo novo no modelo) e a linha honesta — morto **durante a gravação** não vira nota, e o m4a sem átomo final é descartado pela varredura de órfãos (limite declarado, não perseguido: só o `stop()` fecha o arquivo, e sair do app já deposita); cada ditado carrega a SUA nota (`Sessao.armarDitado`), então dois sobrepostos não viram nota órfã + duplicata; o disco que recusa o depósito tem estado PRÓPRIO ("O áudio ficou no aparelho.", com "Tentar de novo"), nunca "Sem microfone"; "Guardado nas Notas." leva à nota; háptico nas duas trocas de resultado; a casca de confirmação é uma só (`FolhaDeConfirmacao`, usada pelo ditado e pela `ConfirmacaoView`); microfone negado não grava e diz por quê, com saída para a 05w; 12 testes em `DitadoProprioTests`, suíte 727/126, `maestro/ditado-proprio.yaml` pelos estados; ADR 06g (A2): o filtro "Só a forma" dos Atalhos era um `AppEnum` de NOVE casos escritos à mão — 9 das 21 formas alcançáveis por voz, 32% das 28 depois da colagem — e virou `FormaEntity`/`FormaQuery` nascida de `Catalogo.todos`, o método do autor incluído; `AppEnum` exige `caseDisplayRepresentations` estático e por isso não podia nascer de arquivo; **F4-C (correção do re-G3, 06/09)**: o estado honesto deixa de ser o último `else if` de uma view — com Destaque posto e o horizonte vencido o widget do Traço largava a agenda e ficava CALADO (R1), então a decisão vira lei com teste (`EstadoNaFace` em `Relogio.swift`: passada a validade toda face diz, só o LUGAR muda — miolo sem conteúdo, rodapé com conteúdo) e as duas famílias da tela bloqueada dizem pela etiqueta e pela linha; o médio vazio vira quadro de ofertas com três ações no CORPO em vez de 70% de área morta com os atalhos escondidos em AX5 (M1 e a segunda metade do A11); `Amostra.velhoComDestaque` nos previews das quatro famílias; 4 testes em `EstadoNaFaceTests`, suíte 732/128 (764/131 depois do merge com a main); **F4-D (a última milha, 06/09)**: o corte em AX5 que sobrou não era troca, era propriedade esquecida — a frase do Destaque terminava em `capítul…` (0,85 de escala) e `Desatualizado.` quebrava com HÍFEN no meio (`Desatualiza-/do.`), a mesma família do `PRÓXI-/MO` que derrubou a volta; a frase ganha `minimumScaleFactor(0.6)` + `allowsTightening` nos DOIS lugares onde é desenhada (o do Traço e o "vazio traz o Destaque" do Próximo, que já cortava com a superfície fresca) e o teto de linhas do estado sai da view e vira `LinhasDoEstado` em `Relogio.swift` — palavra sem espaço recebe UMA linha e encolhe, porque com teto maior o SwiftUI prefere hifenizar a encolher; 3 testes em `LinhasDoEstadoTests`, suíte 767/132, as quatro famílias em AX5 nos dois temas sem reticências e sem hífen (`f4d-ax5-*.png`); **F4-E (a correção do G4, 06/09)**: a face parou de fechar um número que não sabe — com CINCO compromissos o pequeno imprimia "+2 depois", contagem exata sobre uma lista que ele mesmo sabia cortada, e agora o instantâneo carrega quantos ficaram de fora (`Superficie.alemDaLista`, opcional: documento antigo decodifica como "não sei" e a face diz "mais depois", sem número — `Restantes` em `Relogio.swift`, com suíte), o corte mudou para `publicar` (quem corta CONTA) e a guarda do idêntico passou a ver a contagem, senão o sexto compromisso do dia não acordava escrita nenhuma; os DOIS kinds recarregam sempre, porque desde a F4 as duas faces leem o documento inteiro e o mapa da F2 deixava a agenda de ontem embaixo do Destaque de hoje; quem decide quantas linhas cabem virou o layout (`AgendaQueCabe`/`ViewThatFits`, cada candidata levando a própria conta) e a agenda deixou de sumir em AX5; `layoutPriority` na linha do Destaque, porque o VStack repartia a altura em fatias iguais e a frase saía com reticências pela REPARTIÇÃO e não pela propriedade; varredura de `lineLimit`/`minimumScaleFactor` face por face (24 e 8 conferidos, seis divergências, incluindo o `PRÓXIMO` da tela bloqueada sem teto de linha nenhum); `QuadroVazio` compartilhado, agora também no médio vazio do Próximo, com alvo de 44 pt preenchendo a altura (duas ofertas, uma em AX5: com 44 pt três não cabem, e foi medido); o risco do feito aparece na tela (atributo de run, porque o modificador não sobrevive ao `minimumScaleFactor` — e a linha por fazer continua texto simples, porque `Text(AttributedString)` não encolhe), `.invalidatableContent()` nos dois botões e a hora com `.contentTransition(.numericText())`; 6 testes novos, suíte 773/133, capturas `f4e-*.png`. **F5b (ADR 2026-09-08v): a Ilha do compromisso nos estados que ninguém tinha visto.** Com o Destaque e o compromisso vivos o iOS mostra UMA atividade na Ilha e empilha a outra na bloqueada — era o Destaque escondendo o compromisso a 40 min (D9 da F1): o compromisso passa a vencer por `relevanceScore` (1 contra 0), na Ilha e na ordem da pilha; a MÍNIMA só aparece com atividade de OUTRO app e foi plantada com um app descartável (`ferramentas/orca/f5b-outra/`), fotografada para o Destaque e para o compromisso; a expandida cortava o último dígito da contagem (teto de 76 pt sobre a caixa reservada do `.timer`) e o teto saiu; a Ilha não escala com Dynamic Type (compacta idêntica em large e AX5) e o "t" cortado da F4 não se reproduz; o FIM do compromisso está fotografado na compacta, na expandida e na bloqueada; vídeos `f5b-ilha-movimento.mp4` e `-reduzido.mp4` (entrada, troca, saída). Capturas `f5b-*.png` em 08/09/2026 no `6033B043`. **F5b-B (09/09, revisão G3 da 08v): a prova reprodutível** — o `ActivityContent` de request/update/recado nasce de um construtor por atividade e o teste lê o `relevanceScore` dele (vermelho com 5 falhas ao apagar o argumento, verde com ele); a atividade já viva ganha a relevância no `update`; `f5b-semear.sh` publica pela rota real (`TRACO_REPUBLICAR_CALENDARIO` em DEBUG → `ProximoCompromisso.publicar(eventos, cal:)`) e grava o `liveactivitiesd`; pares `f5bb-large-*`/`f5bb-ax5-*` com `f5bb-log-*.log` do mesmo instante, e o controle natural `f5bb-controle-sem-relevancia-ilha-compacta.png` (binário sem a 08v, mesmo estado: a Ilha volta ao Destaque) | Siri/Atalhos executando de fora (D1: build de simulador sem team-identifier; prova pendente no aparelho), Ilha no aparelho do dono (compacta, expandida e mínima estão provadas no simulador com Ilha — F5b, ADR 2026-09-08v — mas a permanência do "acabou" e o toque real ficam para o iPhone; o corte do relógio do cartão em AX5 ("39 minut…") fechou na F5b-C: `f5bc-ax5-bloqueada.png`), StandBy e tela bloqueada trancada com controles (o simulador não renderiza), estado "transcrito" do ditado só no simulador por instrumento de Debug (não há modelo de fala no aparelho simulado) e a transcrição real do Português por aparelho continua sem prova; áudio ainda não se ouve de dentro do Recordar; Spotlight com destino (F9), recado da soneca que sobreviva à republicação (F5) |
| Fora do app (widgets, tela bloqueada, Ilha, Siri, URL) | ADR05u: um snapshot versionado no App Group escrito só pelo app (`Superficie`), catálogo em `Traco/App/Intents/` com `Compartilhado/` no widget, dois `LiveActivityIntent` com identidade e confirmação real (feito sem toggle, soneca pelo orçamento), entidades mínimas com selo na consulta e no `perform()`, reconciliação de atividades no arranque/retorno/comando, `isStale` neutraliza, tipografia por `Tema` (zero tamanhos fixos), D6 fechado; correções do G3 (06/09): causa real do reload recusado achada no chronod (nome do produto "Traço" em NFD no disco × NFC no Info.plist; produto vira `Traco`, exibido segue "Traço") e volta à cena repete o que não foi confirmado (par revisão publicada/recarregada), suíte isolada da superfície real num ponto só do arranque, Destaque inteiro no widget pequeno, hora absoluta nos dois widgets, VoiceOver do botão vivo diz a linha, `#Preview` por família e estado, um tipo só para o próximo; 20 testes em `ForaDoAppTests` em 06/09/2026; ADR05w (F3, 06/09): controle "Anotar" (`ControlWidget`) na Central de Controle, na tela bloqueada e no botão de Ação → `CapturarIntent` compartilhado (só o app executa, contexto demonstrado no log) → `Rota.captura(ditado:)` tipada, guardada e consumida UMA vez pela cena pronta (`Rota.consumir`, arranque frio incluso) → Página em branco com o teclado pronto e o microfone a um toque; 22 testes; **ADR06c (F3b, 06/09): o áudio antes da letra** — o controle (agora "Ditar", com ícone de microfone) abre GRAVANDO; `AVAudioRecorder` escreve o m4a direto no destino final do anexo, a nota entra no disco SEM UMA LETRA e só então `SFSpeechURLRecognitionRequest` (`requiresOnDeviceRecognition`) é pedido; falha, silêncio ou app morto **depois do depósito** deixam a nota com o áudio tocável dentro (`[audio:…](traco://audio/<id>)`, cofre de anexos, zero campo novo no modelo) e a linha honesta — morto **durante a gravação** não vira nota, e o m4a sem átomo final é descartado pela varredura de órfãos (limite declarado, não perseguido: só o `stop()` fecha o arquivo, e sair do app já deposita); cada ditado carrega a SUA nota (`Sessao.armarDitado`), então dois sobrepostos não viram nota órfã + duplicata; o disco que recusa o depósito tem estado PRÓPRIO ("O áudio ficou no aparelho.", com "Tentar de novo"), nunca "Sem microfone"; "Guardado nas Notas." leva à nota; háptico nas duas trocas de resultado; a casca de confirmação é uma só (`FolhaDeConfirmacao`, usada pelo ditado e pela `ConfirmacaoView`); microfone negado não grava e diz por quê, com saída para a 05w; 12 testes em `DitadoProprioTests`, suíte 727/126, `maestro/ditado-proprio.yaml` pelos estados; ADR 06g (A2): o filtro "Só a forma" dos Atalhos era um `AppEnum` de NOVE casos escritos à mão — 9 das 21 formas alcançáveis por voz, 32% das 28 depois da colagem — e virou `FormaEntity`/`FormaQuery` nascida de `Catalogo.todos`, o método do autor incluído; `AppEnum` exige `caseDisplayRepresentations` estático e por isso não podia nascer de arquivo | Siri/Atalhos executando de fora (D1: build de simulador sem team-identifier; prova pendente no aparelho), Ilha no aparelho do dono (compacta, expandida e mínima estão provadas no simulador com Ilha — F5b, ADR 2026-09-08v — mas a permanência do "acabou" e o toque real ficam para o iPhone; o corte do relógio do cartão em AX5 ("39 minut…") fechou na F5b-C: `f5bc-ax5-bloqueada.png`), StandBy e tela bloqueada trancada com controles (o simulador não renderiza), estado "transcrito" do ditado só no simulador por instrumento de Debug (não há modelo de fala no aparelho simulado) e a transcrição real do Português por aparelho continua sem prova; áudio ainda não se ouve de dentro do Recordar; Spotlight com destino (F9), recado da soneca que sobreviva à republicação (F5) **ADR09r (F6, 09/09): os widgets da bloqueada vistos na bloqueada de verdade** — inline com o feito visível e vazio que oferece, retângulo em duas linhas e vazio que oferece; receita do editor por árvore de AX (`f6-plantar-bloqueada.sh`); limites medidos: não escalam com Dynamic Type nem têm claro/escuro; **lacuna aberta F6b: o botão de feito dos widgets abre o app na bloqueada** (o do cartão vivo roda). |
| Provedor por operação (aparelho × Grok) | ADR 07b (07/09/2026): `Politica` é a tabela única das 16 operações com regra e prova datada; só Grok onde o aparelho foi medido e não serviu (produzir, exercício, tentativa, revisar, conferir, ecos, calibragem, Padrões, pergunta do Recordar), Grok-depois-aparelho onde cabe na janela (responder, instigar, contrapor, Notas, vestir, classificar), só aparelho no domínio; sem provedor a tela DIZ nas três seções que calavam e o Perfil lista quem responde o quê; teto do aparelho medido em tokens (iOS 26.4) com reserva de resposta; 826/134 **Volta B2 (ADR 2026-09-09q): a frase existia e nenhuma tela a dizia.** Das 16 frases de `Politica.semProvedor` só SEIS tinham superfície; as outras dez eram ramo de `switch` que rota nenhuma alcançava, com `PoliticaTests` verde por cima. Na Lente, `instigar` e `contrapor` perguntavam a `Sabia.disponivel` (verdadeiro com conta ou Apple Intelligence) em vez de à tabela que as cortou por qualidade em 08q: o autor tocava, o laço girava e vinha uma VIBRAÇÃO — provado no `34CC3F94` com a árvore de AX idêntica 6 s depois do toque (`b2-02-instigar-calou.png`), e a explicação morava duas telas adiante, no Perfil. `Politica.aviso(_:)`, uma linha, passa a ser a resposta de toda rota; Lente, `perguntarASabia` (que dizia 'tente de novo' para o que nunca responde) e `vestirTudo` (mudo quando o refino remoto falhava) consertados, com `LinhaDeEstado` reusado e zero token novo. Portão `PortaoDaRotaQueCalaTests` congela quem mostra (10) e quem não mostra (6, com julgamento); vermelho medido acusando exatamente `instigar`, `responder` e `contrapor`. `responderNasNotas` fica como dívida nomeada da frente das Notas. Suíte 1005/0 em 162 suítes, 09/09/2026, no `34CC3F94`. | Medição com Grok em todas as operações (sem conta em simulador); `responder`/`instigar`/`contrapor` sem medição; reabrir o aparelho onde uma medição nova passar |
| gate-loop | Atualizado; 4 pares contrastantes revisados | Aplicação integrada de longo curso e invalidação de provas sob mudanças |
| curva-zero | Atualizada para prática/delegação contextual | Evidência de tarefas reais e participantes conforme necessidade |
| design-router | Atualizado para autoridade, fidelidade da captura e restauração do ambiente de teste; cenários revisados. V10: fases Construir e Mover com componentes em `Traco/Componentes` e movimento em `Tema`; Julgar e Portão ficam com G3/G4. ADR 08e (volta P1): a fase Mover ganha PORTÃO em teste — `PortaoDoMovimentoTests` varre os 125 fontes de `Traco/` e `TracoWidget/` (`Tema.swift` isento) e conta curva ou duração LITERAL escrita fora de `Tema`. **Medido em 08/09: ZERO** — as 38 chamadas de `withAnimation(` do repositório passam todas por `Tema.*` ou `CalendarioTema.morph`, e a lista congelada nasce vazia. Passar a curva DENTRO de `Tema.movimento(…)` é a forma que a ADR manda escrever e não conta. Curva literal nova, em arquivo velho ou novo, fica vermelha e diz o que fazer; descer nunca é vermelho. Provado: verde na árvore de hoje, vermelho com plantas em arquivo novo fora do `pbxproj` e em arquivo existente, e a forma prescrita, o comentário e a string fora da conta | Ampliar evidência de estados e tarefas; defeitos AX continuam abertos mesmo após restaurar o tamanho normal; o movimento já está TODO roteado por `Tema` (zero curva literal fora dele, medido em 08/09) e o portão existe para que continue assim — ele não julga se a linha existente escolheu a classe certa, só impede que a próxima seja escrita na view |

Não há autorização inferida para publicar, gastar ou enviar mensagens externas. Produção local e validação prosseguem; dependências reais entram como pendências explícitas. O AGENTS.md deste repositório foi criado para orientar agentes pela visão vigente; AGENTS de outros projetos e skills de terceiros não foram alterados.

Atualização 11/09/2026 — ADR 2026-09-11a: ação do Trabalho com duração opcional (intervalo vs marco); a mesma seleção do próximo compromisso inclui ação elegível (widget, bloqueada, Ilha, Siri) e continua a excluir deixa e origem protegida; porta dos Trabalhos não some na busca. `Prova.vaza` olha o predicado e os números (Lisboa não vaza pelo 4-grama do enunciado; 15 vaza). Prática não recusa vocabulário do pedido. Porta da calibragem aceita um par; a folha dos Padrões usa a mesma porta para dizer a indisponibilidade (`paresDoJuizo`). A rota na Politica permanece cortada.

Atualização 18/09/2026 — ADR 2026-09-18a: tesoura do próximo passa a `inicio > agora`. A bloqueada, a Ilha, o widget e o Lembrar largam no começo, não no fim; janela viva 1 h; dia inteiro não tapa horário; Destaque não empilha na janela; sem cartão «acabou». Prova em `ForaDoAppTests` (os 9). A lacuna da 08v («acabou» na bloqueada até o app abrir) fecha nesta lei. Ruptura 2 e a instalação no iPhone do dono desta lei ficam pendentes.

Atualização 12/09/2026 — ADR 2026-09-12a: contrato do caderno único e da guarda de obra. Pedido de livro/autor/tese ausente das Fontes cai localmente («não está no caderno», oferece plantar, não chama o modelo). O gesto «Plantar esta obra» grava o nome que ela pediu e projeta no índice depois do commit; a próxima pergunta acha a nota. Nome puro recusa sem alegar ausência. Identidade é por fonte, frase contígua: o autor extra do pedido tem de estar na mesma fonte; título «Tratado»/«Caderno» não identifica o nome composto. Hamlet/Duna/Dom Casmurro e «Explique» entram; palavras dispersas e frase comum citada não. **Ruptura 2 parcial:** conteúdo extra na fonte não foi qualificado como tese. Politica alinhada à 11a (régua e porta atuais); a folha dos Padrões usa a porta de um par para dizer a indisponibilidade; `recordar` e `calibragem` continuam cortadas até remedição. Q6: a folha das Notas recusa resposta se a fonte mudou no await. Sustentação da Página recusa PDF/sumário inventado; a rota `responder` segue cortada. Pesquisa delegada fechada (`PesquisaDelegada.aberta = false`); `aceitar` grava `origem=pesquisa` só com a flag, e a nota fica fora do Retrato. Retrato colhe Destilar (`frase`) e `ResultadoObservado` vigente só depois de `AcessoTrabalho`. Timeout em revisar/prepararPratica deixa de se chamar revisão incompleta ou exercício inválido (`Erro.provedorCalou`). Hub do Trabalho continua sem wizard. Colheita de juízo mora na retomada do Trabalho (`linhasDaColheita`) e viaja no pedido e na preparação (`colheitaDeJuizos`). «Está difícil» é o nó plantado (`dificuldadePlantada`) ou a próxima estação (`ofertaDaJornada` / `proximaEstacao`), dita na retomada e na preparação — sem rótulo de pessoa. A retomada mostra o nó (`trabalho-dificuldade-retomada`) ou a oferta (`trabalho-oferta-retomada`), inclusive a jornada fechada («não invento um gargalo») sem botão. O gesto «Ir ao próximo passo» rola à âncora da estação em falta; «Ver a dificuldade» rola ao campo (`ResultadoObservadoTests.aOfertaApontaAEstacaoEmFaltaSemWizard`, `ResultadoObservadoTests.aRetomadaMostraONoPlantadoOuAJornadaFechada`). Jornada Markdown ponta a ponta fecha só com entrega causal vigente (`entregaCausalVigente`): intenção atual, versão do pedido, evidência ainda igual à observação atual. Nova intenção ou novo resultado não fecham com o ajuste antigo. `guardarVersaoHumana` valida antes de cancelar. Causa do gesto humano fica no rascunho. Preparando, falha, cancelado, pronto sem artefato e edição alheia não fecham. Sem Grok; HTML e wizard não entram. A primeira versão é campo na estação de produção, sem gaveta a revelar; a gaveta de edição e «Ir ao próximo passo» devolvem o cursor ao campo `versao` (a folha abre no pedido). Prova de documento: `ResultadoObservadoTests.aJornadaFechaComVersaoHumanaDepoisDoRelato`. Prova de toque no simulador coordenado (iPhone 17, 12/09, /tmp/traco-c9-ui5.log): `JornadaC9UITests` passou — intenção, artefato escrito, ato, relato, colheita, ajuste humano, «não invento um gargalo», nó plantado. Não é Q8 no aparelho do dono. Q8 no aparelho continua não verificado. Q4: portão `Q4CadernoUnicoTests` (domínio `.soBordo`, plantar infere da nota, sem Território). A busca da lista não mostra segredo selado (`SeloNasRotasDoContextoTests.buscaNaoMostraOSegredoSelado`). D4 espera remedição de recordar/calibragem. Não certifica aprovação semântica de Q1/Q2. Fumaça oficial `q2-fumaca.json` em 12/09 no simulador coordenado gravou `contaGrokLigada:false`. No aparelho do dono, o binário Debug já instalado (sem install) gravou `contaGrokLigada:true` e 12 modelos; a identidade da 11a no mesmo processo mostrou porta velha (`qn-calibragem-par-unico` com 1 item, `chamadasGrok:[]`, 0,3 ms, rota liberada). Sem remedição e sem reabrir. A sonda agora carimba porta e régua em toda linha da fumaça, inclusive o `inicio`. O teste do selo+guarda passa pelo executor com gerador que inventa e índice velho apontando a nota selada. A guarda também pega o autor antes do verbo («O que o Daniel Kahneman diz»), os verbos de atribuição «pensa» / «explica» / «argumenta» / «escreve», e as marcas «de acordo com» / «na opinião de» (`GuardaDeObraTests.autorAntesDoVerboEPedidoENaoInventa`, `pensaTambemEPedidoENaoInventa`, `verbosDeAtribuicaoCalamOModelo`, `deAcordoComENaOpiniaoDeCalamOModelo`). Abrir a forma e o contexto da Página consultam a Politica antes de `instigar`/`ecos` (`PortaoDaRotaQueCalaTests.aFormaAbertaNaoDisparaInstigarCortado`, `oContextoDoCadernoNaoDisparaEcosCortado`, `semEcosADivulgacaoNaoContaCandidatasNaoLidas`). O script `remedir-recordar-calibragem.sh` passa a alcançar o iPhone por `VIA=devicectl` (D explícito; lê a 11a no binário antes da conta; `SO=1` para na fumaça e não gasta a matriz; não instala; não apaga o caderno). Fumaça deste candidato no simulador coordenado (12/09 19:01Z, `prova/12-sim-fumaca-11a.jsonl`): porta e régua `true`, conta desligada, `chamadasGrok:[]`, exit 4 — sem remedição. `SO=1` no iPhone do dono (12/09 19:09Z, `prova/12-aparelho-fumaca-so.jsonl`): conta ligada, 12 modelos, carimbos da 11a ausentes, exit 5 — sem matriz. O Debug instalado continua o de 17:36Z (porta velha). Ainda não remediu. Fixture pareada em `prova/12-recordar-calibragem-casos.json`. Loop 89/0 em 9 suítes (`/tmp/traco-qloop7.log`). **12/09, ADR 2026-09-12b:** `responderNasNotas` passa a conferir a candidata no mesmo pacote efetivo (`Grok.responder`, modelo da rota, parser `{base,texto,trechoIDs}`). A rota das Notas não chama `recusarSeAusente` e não diz «não está no caderno» a partir da seleção; a pergunta original «Qual tese Duna defende e como uso isso na minha escolha?» chega à geração e à conferência. Nome puro continua recusa local. Obra omitida do orçamento não encerra a pergunta: o pacote efetivo segue. Uma geração + uma conferência; reparo sem terceira validação; falha/timeout/cancelamento não publicam a candidata. `RevisaoTresRupturasTests` e `RevisaoRetryAjusteTests` intactos. Qualidade semântica **não aprovada**; fumaça test4 com `contaGrokLigada=false`; script `ferramentas/orca/avaliar-sustentacao-notas.sh` e fixture `prova/sustentacao-notas-casos.json` prontos, não executados nesta entrega.

- **Fora do app: a frase do autor inteira, e o dia no médio** (08/09, ADR 2026-09-08g).
  O corte em reticências da linha do Destaque — quatro voltas em aberto — fecha
  pela causa certa: o teto de linhas, não a propriedade de encolhimento. As
  quatro faces que desenham a frase passam a usar a mesma view, sem teto de
  linhas e com piso de encolhimento próprio do papel do texto. O médio do Traço
  troca dois atalhos de cabeçalho por duas linhas de agenda (era nenhuma), e o
  quadro do estado vazio deixa a forma de lista de Ajustes por uma linha de
  ações com cápsula âmbar. Prova: capturas `simctl` do iPhone 17 Pro (teste 3)
  em `ferramentas/orca/f5-*.png`, casa em claro/escuro × normal/AX5, estado
  vazio antes e depois, desatualizado nos dois tamanhos, tela bloqueada e Ilha
  compacta e expandida. Lacuna aberta que continua: modo escuro do widget
  (é `Tema.swift`, volta L2) e Ilha mínima (limite do instrumento).
  **F4-H (08/09, ADR 2026-09-08i): o corte honesto.** A promessa "inteira,
  sempre" custava o corpo de leitura (~11 pt em AX5, ~7 pt com 247
  caracteres, e ainda reticências). Agora o publicador limita a projeção
  (140 grafemas, uma vez, para widget, tela bloqueada e Ilha) e declara se é
  trecho; a face mantém o corpo do papel e reduz a quantidade, o rótulo
  "Nova nota" cede antes de uma linha da frase, o toque no pequeno abre a
  nota do Destaque, e o VoiceOver distingue trecho de integral. Prova:
  `ferramentas/orca/f4h-*.png` no iPhone 17 Pro (teste 4), 103 e 247
  caracteres, normal/AX5, claro/escuro, com "Desatualizado.".
  **F4-I (08/09, emenda da 08i):** o que faltava em Componentes — a ADR diz
  as duas gramáticas do marcador (palavra no publicador, grafema na face) e
  ganha o critério medível de corte evitável ("cabe uma linha inteira do
  corpo do papel no espaço livre ao lado do marcador"), com a premissa na
  suíte; `FraseDoAutor` e `CapsulaViva` com preview por estado.

- **Perfil: a terceira linha — o que a medida reprovou, dito ao autor** (08/09, ADR 2026-09-08l). O cartão CONTA lê da tabela `Politica` uma terceira lista, "indisponível por qualidade", em dois grupos (sem substituto / em correção com conserto nomeado), uma linha por operação com o quê, por quê em uma oração de pessoa e a data; vazio é estado dito; nunca manda conectar conta, nunca promete prazo. A letra miúda do cartão passa a escalar com Dynamic Type (`@ScaledMetric` no lugar de três `maxWidth: 280`): em AX5 a linha de uma operação cai de 0,32–0,38 tela para 0,26. Provado no iPhone Air em `large` e AX5, claro e com o aparelho em escuro (o app força claro); 3 testes.
