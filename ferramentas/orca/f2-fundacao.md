# F2 — Fundação fora do app (ADR 2026-09-05u)

Worker: Fable 5.1 (trilha Fora do app), 05/09/2026, worktree `fora-2-fundacao` sobre main `e51550e`. Simulador de teste: iPhone 17e `C7341E64` (sem Ilha), ligado por esta volta. Instrumento: tudo por `com-trava.sh`; `orca emulator` para toques; sem maestro.

Linha da volta: ciclo MULTIPLICAR; intenção: o mesmo comando produz o mesmo estado confirmado em qualquer entrada (widget, Ilha, Siri/Atalhos, URL) sem expor nota protegida; obstáculo: 12 intents soltos, chaves soltas no App Group, dois LiveActivityIntents que confirmam sem persistir, reload por minuto, 39 tamanhos fixos (D3), widget serve apagado (D6); evidência: 18 testes sem UI + capturas + build dos dois alvos sem aviso.

## O que entrou (contrato do orquestrador, item a item)

1. **Catálogo** `Traco/App/Intents/`: `Intencoes.swift` (movido; 8 intents, `GestoEscolha`, `Rota`), `TracoAtalhos.swift`, `Entidades.swift`; `Compartilhado/` compilado nos dois alvos (`SuperficieFora.swift`, `Atividades.swift`, `DestaqueFeitoIntent.swift` com `DestaqueDesfazerIntent`, `LembrarDepoisIntent.swift`). `TRACO_APP` só no app: a extensão declara e `ForaDoAlvo.recusar()` no `perform()`. Nomes de tipos preservados; `Rota.ir(_:)` é a convergência; rota pendente consumida também no `onAppear` (arranque frio). Rotas novas: `.nota`, `.trabalho`, `.compromisso`.
2. **Snapshot** `Superficie` (versao, revisao, geradoEm, validoAte, destaque{id,dia,linha,feito}, proximos[≤3]{id,inicio,fim,titulo,aviso,lembrarEm,doSistema}) em `group.app.traco/superficie.json`, escrito atômico por `SuperficieDisco.publicar` (idêntico não regrava; reload só dos kinds cuja seção mudou; falha → `false`; ausência/corrupção/versão → `.indisponivel`). Timeline do widget = `Superficie.transicoes` (agora, fim de cada próximo, soneca, horizonte), política `.never`; `estadoDoProximo`: indisponível / desatualizado / vazio / próximo. "atualizado há…" pelo sistema (`Text(style: .relative)`). `ProvedorProximo` não pede mais reload por minuto. D6: `gravar(nil)` publica lista vazia e recarrega.
3. **Dois intents com identidade**: `DestaqueFeitoIntent(nota:dia:)` → `DestaqueDoDia.marcarFeito` (revalida dona+dia, feito=true, publica; recusa desfaz e não confirma) → `encerrarAtividades` aguardado; `DestaqueDesfazerIntent` explícito. `LembrarDepoisIntent(ocorrencia:)` → `ProximoCompromisso.lembrarDepois`: `revalidar` no disco (ou doSistema na projeção), `Revisoes.soneca` (permissão, `Avisos.livres`, `add` que pode falhar), revalida de novo depois do await, registra soneca, republica, só então `contar` na atividade. Reconciliação `DestaqueDoDia.reconciliar`/`ProximoCompromisso.reconciliar` no arranque (`TracoApp`), retorno (`RaizView`) e após comandos; `isStale` neutraliza texto e botões no cartão e na Ilha (compacta e expandida).
4. **Entidades**: `NotaEntity`/`TrabalhoEntity`/`CompromissoEntity` com queries (ids, sugestões ≤10, texto) que aplicam o selo antes de representar; `AbrirNota/Trabalho/CompromissoIntent` revalidam em `destino(_:)` no `perform()`.
5. **D3**: zero `.system(size:)` no widget; degraus `Tema.label/miudo/meta/chrome/corpo/acaoViva` (dois tokens novos, documentados como "só fora do app").
6. **Anotar**: `AnotarIntent.anotar` → `.vazio` / `.falhou` / `.anotado` com falas distintas.

Bônus: D18 (aviso pré-existente em `EditorBlocoView.swift:270`) fechado com `@MainActor` no parâmetro + `assumeIsolated` no `set` do Binding (sem `@Sendable`, sem crash do frontend). Build dos dois alvos: **0 avisos**.

## Prova

- `ForaDoAppTests` (18): snapshot versionado/idempotente; reload por kind; App Group indisponível não confirma; truncado/versão errada → indisponível; horizonte vencido → desatualizado, vazio dentro dele; D6; linha do tempo curta; feito repetido não inverte + desfazer; cartão velho (outra nota/outro dia, também pelo intent); superfície recusada desfaz o feito; soneca negada/lotada/falhada não anuncia (+ intent real com `notDetermined`); soneca aceita guarda/publica e repetir substitui; ocorrência velha não chama o centro; corrida com editor durante o await; selo nas entidades; compromisso só do autor; proteção depois da consulta; anotar honesto.
- Suíte integral: **660 testes, 0 falhas** (17e, 05/09 20:15; repetida após as correções da jornada real: `✔ Test run with 660 tests in 123 suites passed after 7.954 seconds.` às 21:2x, depois de reinstalar e reiniciar o 17e — duas tentativas antes deram "The test runner hung before establishing connection" com nenhum teste executado, instrumento, não código). Linhas: `✔ Test run with 660 tests in 123 suites passed after 7.178 seconds.` / `** TEST SUCCEEDED **` / build: `** BUILD SUCCEEDED **` com `grep warning:` vazio nos dois alvos.
- Capturas (17e, simctl): `fora2-bloqueada-compromisso-vivo.png` (cartão da bloqueada com a tipografia de Tema, compromisso marcado pela prosa do app → publicado em `superficie.json` → atividade), `fora2-bloqueada-lembrar-recusado-honesto.png` (toque em "Lembrar em 10 min" com avisos `notDetermined`: o intent com identidade rodou no app, revalidou no disco, o centro recusou e o cartão diz "avisos desligados no iPhone" sem prometer hora; `superficie.json` sem `lembrarEm`).

## D1, literalmente

Tentado: (a) `com.apple.developer.team-identifier` + `application-identifier` embutidos no build de simulador via `CODE_SIGN_ENTITLEMENTS[sdk=iphonesimulator*]` — o binário passou a carregar os entitlements (`strings` confirma), mas o `linkd` continua: `Unable to get teamId from app.traco PID` → ele lê o teamId da ASSINATURA do processo (adhoc, `TeamIdentifier=not set`), não do entitlement; alerta igual ao da F1 (`fora2-atalhos-team-identifier-alerta.png`). (b) `CODE_SIGN_IDENTITY[sdk=iphonesimulator*]: Apple Development` — o Xcode ignora e assina "Sign to Run Locally". (c) reassinatura manual com o certificado real (`TeamIdentifier=W28WF9A5A2`) — o SpringBoard recusa lançar (`SBMainWorkspace denied`). Conclusão: App Shortcuts/Siri não executam de fora em simulador; Atalhos/Intents ficam **n/c**; a prova é no aparelho do dono. Ajustes revertidos; nada disso ficou no repositório.

## Jornada real no iPhone Air (Ilha), depois de "Air livre"

Widgets colocados na casa por `cliclick` (long-press → Editar → Adicionar Widget); compromissos marcados pela prosa do app. Dois defeitos achados AQUI, não nos testes, e corrigidos:

- **Horizonte móvel:** `validoAte = agora + 14 d` mudava a cada chamada, então cada ação gravava o documento cinco vezes (revisão 8 → 13 num compromisso) e o WidgetKit passou a recusar o reload em rajada (`reloadTimelines(ofKind:) … ChronoCoreErrorDomain Code=27` no log do app; nenhum "Reload timeline: app.traco" no `chronod`). Agora o horizonte é o início do dia (estável) e republicar o mesmo estado não regrava (teste novo em `apagadoNaoVolta`).
- **`desatualizado` no instante exato:** a entrada da linha do tempo cai em `validoAte` e a comparação era `>`; virou `>=` (teste ajustado).

Capturas (simctl, Air): `fora2-inicio-widgets-vazio-claro.png` (dois widgets, "nada marcado", "atualizado há…"), `fora2-inicio-widgets-claro.png` (Dentista hoje às 21:45, sino), `fora2-inicio-widgets-escuro.png` (D11 continua: papel claro no escuro — G0 de F4/F5), `fora2-inicio-widgets-ax5.png` (accessibility XXXL: tudo escala pelos degraus de Tema), `fora2-ilha-compacta-compromisso.png` (contagem), `fora2-ilha-expandida-compromisso.png` (título + "Lembrar em 10 min"), `fora2-inicio-proximo-desatualizado.png` (horizonte vencido → "desatualizado · abra o Traço", "atualizado há 2 h"), `fora2-inicio-proximo-sem-dados.png` (arquivo ausente → "sem dados · abra o Traço").

## O que não foi possível

- Widget do Destaque com linha e botão de feito na casa: nenhum Destaque vestido no Air (a forma não se veste por URL); coberto por teste (`feitoNaoAlterna`, `cartaoVelho`) e pela captura da bloqueada da F1; pendente de captura real no re-G3.
- Accessory na bloqueada, StandBy, Ilha mínima: limites do simulador (F1 §7).
- O rodapé "atualizado há…" é renderizado pelo sistema (`Text(style: .relative)`) e mostra segundos ("2 min e 13 seg"); é honesto, mas ruidoso — decisão de design para o G4 (mostrar só a partir de minutos exige entrada de linha do tempo por minuto, que é o que a volta eliminou).

## Correções do G3 (06/09/2026, worker Fable 5.1, iPhone Air `64F7B8B4`)

Revisão em `revisao-f2-fundacao.md` (Design 7, Estado honesto 7, Fora do app 7, Componentes 8, Acessibilidade 8, Complexidade 8). Rebase sobre main `e6b5033` (main já tinha andado de `2229031` para `e6b5033`, "RUMO: retomada"): conflitos em `SPEC.md` (05t de main antes de 05u) e `EditorBlocoView.swift` (ficou a versão desta volta, `@MainActor` no parâmetro + `assumeIsolated`, e a frase de main "só `@MainActor` também derruba o compilador" caiu porque compila limpa aqui: build 0 avisos); `Tema.swift`, `PaginaView.swift`, `EVOLUCAO.md` e o pbxproj mesclaram sozinhos; `xcodegen generate` sem diferença após o rebase.

**A1 — a causa real não era rajada.** Repetindo o cenário do revisor no Air (superfície publicada, app em primeiro plano) o pedido inicial E a repetição 2 s depois foram recusados com o mesmo `ChronoCoreErrorDomain Code=27`. No log do `chronod`, no milissegundo do pedido: `[com.apple.BaseBoard:Common] Resolved bundle path …/Traço.app does not match executable Traço`. Nos bytes: o executável no disco é `Trac` + U+0327 (NFD) e o `CFBundleExecutable` do Info.plist é `Traç` (NFC); o chronod compara os dois, não reconhece o processo como o app dono do widget e recusa TODO `reloadTimelines` — o que o revisor viu mudar nos widgets veio dos `Button(intent:)` (o WidgetKit recarrega sozinho depois do intent), nunca do app. Correção: `PRODUCT_NAME: Traco` (ASCII) em `project.yml`, com `TEST_HOST` e `maestro/varrer.sh` acompanhando; o nome exibido (`CFBundleDisplayName`) segue "Traço", o bundle id `app.traco` e o widget `app.traco.widget` não mudam. Depois disso o `chronod` regista `record reload: [app.traco::app.traco.widget:TracoProximo] = externalRequest([source: Traco:34091; valid: YES])` e nenhum Code 27. Rede, como o pedido do orquestrador: `SuperficieDisco.revisaoPublicada/revisaoRecarregada` + `recarregarPendente()` chamado 2 s após o `didBecomeActive` (no arranque nada está confirmado); teste `recargaNaVolta`. **Atenção ao mesclar:** no Air o `simctl install` do bundle com nome novo trocou o contêiner de dados (o do 17e/Air é descartável); no iPhone do dono confirmar, antes de qualquer outra volta, que instalar por cima preserva as notas (o contêiner é do bundle id, mas é o dono que decide correr esse risco).

**A2 — suíte isolada num ponto só.** `SuperficieDisco.isolarParaTestes()` chamado em `TracoApp.init` quando `XCTestConfigurationFilePath` existe (o mesmo portão do `DiscoTraco`): `url` numa pasta temporária, `recarregar` mudo, `defaults` numa suíte própria (`app.traco.testes`, e `DestaqueDoDia`/`ProximoCompromisso` passam a ler `SuperficieDisco.defaults`), `atividades` falso (portão único para `Activity.request`). Teste `suiteIsolada` prova que gravar Destaque, próximo e soneca não muda o arquivo nem as chaves do App Group real. No Air: `superficie.json` real ficou com a revisão 41 e a hora de 05/09 21:37 durante a suíte inteira.

**A3/A5/B1 — widget.** No pequeno, Destaque (ou "sem dados") toma o lugar de "Recordar" e a linha cabe em duas (`f2c-casa-claro.png`: "Correr antes / do café"); em tamanho de acessibilidade o pequeno mostra só a linha, em até três (`f2c-casa-ax5.png`). Os dois widgets dizem "atualizado às HH:mm" (`RodapeAtualizado`, `Superficie.horaCurta`), sem segundos. **A4:** o botão vivo diz "Marcar como feito: <linha>" no cartão e na Ilha. **B3:** a colheita das candidatas usa o mesmo `fimDoHorizonte` do `validoAte`. **B4:** `Fatia` é `typealias` de `Superficie.Proximo` (um tipo; init com padrões e `comLembrete` no tipo), sem `projecao`, sem repasses `horaCurta/diaEmPalavras/diaISO`, `DestaqueAtividade.id` sem padrão; `Superficie.quando` é o único "hoje às 21:45" (widget e `CompromissoEntity`). **B5:** oito `#Preview` (pequeno, médio, bloqueada e AX5 para cada widget, um estado por entrada: com dado, feito/soneca, vazio, desatualizado, sem dados).

Prova: build dos dois alvos 0 avisos (o alvo de TESTES traz de main dois avisos em `ConferenciaTrabalhoTests.swift:381`, "variable never mutated", que são falsos: trocar por `let` faz o compilador recusar as chamadas mutantes das linhas 401 e 411; ficou como em main); suíte integral no Air `✔ Test run with 665 tests in 124 suites passed after 7.216 seconds.` / `** TEST SUCCEEDED **` (665 = 660 da F2 + 3 de main + 2 novos); `git merge-tree` limpo contra main. Capturas (Air, simctl): `f2c-casa-claro.png` (Destaque inteiro no pequeno, Dentista hoje às 03:16 com sino, rodapé 03:14), `f2c-casa-escuro.png` (D11 segue), `f2c-casa-ax5.png` (só a linha, três linhas), `f2c-casa-desatualizado.png` (horizonte vencido às 03:18:34: "desatualizado · abra o Traço" com "atualizado às 03:14"), `f2c-casa-restaurada.png` (app reaberto: "nada marcado", "atualizado às 03:18" — A1 cumprido). Jornada: Destaque semeado nas chaves do App Group e três compromissos curtos no `calendario.json` do Air; a publicação, o reload e a recarga foram do app. Air restaurado (claro, `medium`, calendário e chaves de antes) e desligado.
