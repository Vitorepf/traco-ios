# Revisão G3 — V10 fundação de design (ADR 2026-09-05v)

Fable revisor, 06/09/2026, worktree `volta-10-fundacao` (topo 2e56518 = 2229031 + A 32bcf78 + B 2e56518; main e6b5033 difere de 2229031 só em `RUMO.md`). Simulador do revisor: iPhone 17 Pro C2416CBC (encontrado desligado, `large`, claro, RM 0; ligado por mim; ao fim restaurado a `large`, env `TRACO_SEM_MODELO` removido, desligado). Nenhum arquivo do branch editado; `busca-celular.png` e `recordar-lado-a-lado.png`, que o maestro reescreve, foram restaurados por `git checkout`. Todo build, teste e maestro por `com-trava.sh`.

## Veredito: CORRIGIR ANTES (três dimensões abaixo de 9), depois segue ao G4

Mínimo para mesclar, em ordem de custo:

1. **Componentes / Complexidade — motor sem superfície.** `BotaoCompacto`/`.compacto` (Botao.swift:33-45, 60-62) não tem chamador e duplica `CompactoStyle` de `CartaoAnaliseView.swift:328`, que continua lá: a volta declarou "sete estilos → três" e o repositório tem OITO (`grep ": ButtonStyle"`: PressaoDiscreta, PressaoClara, BotaoPrimario, BotaoCompacto, CompactoStyle, CartaoBotaoStyle, BarraBotaoStyle, AcaoTrabalhoStyle). Apagar `BotaoCompacto` ou migrar `CompactoStyle` para ele (zero pixel a provar). `Tema.confirmacaoEntra` (Tema.swift:153-155) é ponte para um `Toast` que nunca a citou: apagar. `Toast`/`.toast()` (72 linhas) e `LinhaQueAbre.abaixo` não têm chamador; ou o calendário passa a usar `Toast` nesta volta (o `CalendarioToast` já cita os mesmos tokens; zero pixel provável) ou a ADR nomeia os dois como dívida com dona (V15 calendário) em vez de "componente entregue".
2. **Contrato — inflado em três frases.** SPEC ADR 05v "três estilos de botão" (ver acima); EVOLUCAO linha 15 "diff de pixels 0 % em `large` e AX5" — o próprio relatório de B mede 0,07 % (ficha AX5), 0,08 % (escrever) e 3,7 % (escrever AX5), e a minha recontagem das capturas de B acha 218–636 px de anti-aliasing de texto onde B escreveu 0 (tabela abaixo); relatório de A: "`confirmacaoEntra` fica como ponte enquanto `Componentes/Toast.swift` (B) o cita" — Toast não cita. Corrigir as três frases; declarar na ADR o Δ sob Reduzir Movimento da queima (abaixo) e o mola da célula nova como custo assumido também sob RM.
3. **Relato/G5 — evidência acima do teto do RUMO (linha 35: PNG ≤ 400 KB).** `v10a-diff-ax5.png` tem 491 543 bytes; `sips -Z 1000` resolve. Os demais 56 arquivos estão dentro (3,2 MB no total).

Nada disso muda pixel; o G4 pode correr em paralelo com a correção se o orquestrador preferir.

## Scorecard (14 × nota × evidência)

| dimensão | nota | evidência |
|---|---|---|
| Visão | 9 | Linha G0 da V10 no RUMO (linha 62): "toda tela nasce dos mesmos tokens, componentes e movimentos"; EVOLUCAO linha "Direção visual e uso simples" ganhou ADR05v com o que existe e o que falta; ciclo multiplicar + eixo 4. O critério "linhas líquidas ≤ 0" da mesma linha G0 NÃO foi cumprido (ver Complexidade). |
| Contrato | **8** | ADR 05v em 45 linhas, coerente no essencial (duas metades, custo assumido: Δ ≤ 0,08, célula nova, Pilula seis formas, +799, lista do que fica por tela). Inflações: "sete ButtonStyle → três" vs oito no código; EVOLUCAO "0 % em large e AX5" vs 0,07–3,7 % medidos por B; A diz que Toast cita `confirmacaoEntra` (não cita). Não declarado: sob RM a queima da expressiva passou de 3,0 s (sem tratamento) a fade de 0,15 s seguido de 3,15 s de espera parada (`FechoExpressivaView.swift:134-135`, classe `.deslocamento`). O menu do domínio unificado (ícones nas Notas, marca na ficha) está declarado na ADR como custo: aceitável, é convergência de um menu só, sem passo novo. |
| Correção | 9 | `com-trava.sh xcodebuild test` no C2416CBC: **650 testes em 123 suítes, 0 falhas, 8,0 s** (`✔ Test run with 650 tests in 123 suites passed`). Avisos do build: só os dois pré-existentes (`EditorBlocoView.swift:272` Sendable; `ConferenciaTrabalhoTests.swift:381` var→let). Os 10 arquivos de `Componentes/` estão no alvo (pbxproj) e os 32 `#Preview` compilam no build Debug do alvo. Maestro no meu simulador com `clearState`, `TRACO_SEM_MODELO=1`, UDID explícito (o `varrer.sh` recusa porque o dono tem dois simuladores ligados; roteiro replicado em `maestro-meu.sh` no scratch): busca, busca-filtro, recordar, recordar-revelar, recordar-completo, recordar-adiar-e-pular, calendario — **FALHAS: nenhuma** (107 passos COMPLETED, 0 FAILED). Testes novos: 8 em `TemaTests` (lei por classe, pressão, corte, `nenhumLiteralDeDuracaoOuMolaNosArquivosDaV10A`, que lê 16 arquivos). B não tem teste novo: componente é view, a prova é o pixel. `merge-tree` contra e6b5033: árvore única, **zero conflito** (nem docs — main só mexeu no RUMO). Herdado, não regressão: `BotaoPrimario` põe `.animation` ANTES de `scaleEffect`/`opacity` (Botao.swift:22-29), exatamente como o `PrimarioStyle` antigo — a pressão do botão primário não anima (o estado salta); fica para a volta do Recordar. |
| Jornada real | 9 | Refiz os pares eu mesmo, mesmo roteiro maestro nos dois builds (`captura.yaml` no scratch: clearState → Notas vazio → 6 notas por `traco://anotar` → domínios pelo menu de contexto → ficha por prosa "Dentista sexta às 14:30" → mês → `traco://recordar` ler/escrever/revelar), build "antes" = `git archive e6b5033` no scratch. `v10-rev-{antes,depois}[-ax5]-<tela>.png` (capturas originais 1206×2622, diff feito nelas; versionadas reduzidas a 1000 px pelo teto do RUMO); diff fora dos 5 % do topo, limiar 16/255 — tabela abaixo. Conteúdo conferido a olho: mês com "Dentista" no dia 11, ficha topo e rodapé até "Apagar compromisso", lista com IDEIAS/SAÚDE, vazio com "escrever na página", Recordar nas três fases. Estados pensando/sem conta da sábia só em preview (declarado por B; o simulador tem modelo). |
| Design | 9 (provisório, G4 julga) | Tokens de `Tema` em tudo que a volta tocou; `CalendarioTema` virou alias de `Tema` (13 cores, 3 raios, 2 sombras, morph = `Mola.escala`). Dois vocabulários de raio convivem de propósito (`raio` 12 do caderno × `Raio.{10,14,18}`), declarado em Tema.swift:81-83. Grep: nenhum `duration:`/`spring(response:`/`interpolatingSpring(` fora de `Tema.swift` em todo `Traco/`; nenhum `withAnimation`/`.animation` fora da lei nos 14 arquivos de A (só `.transition(.opacity)` puras, permitidas). `TrabalhoView` segue animando sem RM (fora do escopo, V18). |
| Simplicidade | 9 | Zero toque, decisão ou tela a mais nas três telas: mesmos ids, mesmos rótulos, mesmo caminho comum (Notas: filtro 1 toque; ficha: Pronto; Recordar: escrever → Revelar). Menu do domínio: mesmos itens, ícone a mais, marca no atual (curva-zero igual). |
| Movimento | 9 | Vídeos de A (`v10a-*-quadros.png`, linha de cima antes / de baixo depois): Camadas 2 quadros de corte + 12 de subida iguais; morph 7→8 quadros na mesma progressão; sob RM crossfade de 4 quadros nos dois — conferido no strip. Vídeo de B (`v10b-recordar-fases-depois.mp4`): extraí quadros a 30 fps em t=1,0–1,24 s, 3,60–3,99 s, 4,50–4,74 s (`mont-recordar-video.png` no scratch): esconder apaga um objeto só e escrever amanhece sobre tela vazia — **nenhum quadro com dois textos na mesma linha** (§21 cumprido, defeito 4 da auditoria fechado). Célula nova (`EditorBlocoView:242`): mola 0,35/0,80 → 0,55/0,86 = assentamento ≈ 0,45 s → ≈ 0,70 s (+0,25 s), perceptível lado a lado, não em isolamento; ação rara (fileira de tabela); declarado por A como custo. Tentei filmar nos dois builds (`celula-*.mp4`): o toque ".*linha.*" do maestro focou a célula em vez de acrescentar a fileira — inconclusivo, limite meu. Sob RM: pressão vira estado (`pressaoAnim` nil), laço do "lendo…" para, timer corta — coberto por teste. Reservas: a queima sob RM (acima, contrato) e `.toast()` sem chamador. |
| Componentes | **8** | Um lugar (`Traco/Componentes`, 10 arquivos), nomes em pt, 32 `#Preview` (normal/selecionado/pressionado/desabilitado/vazio/carregando/falha/AX5 onde o estado existe), alvo 44 por `.alvo()` no Button/Menu (ADR 05f), labels/hints/ids preservados. Contra: `BotaoCompacto` e `Toast` sem chamador (motor sem superfície — não há tela que prove o pixel deles); `.compacto` DUPLICA `CompactoStyle` que continua em CartaoAnaliseView; estilos 7 → 8, não → 3. `Pilula` com seis formas: custo justo porque zero pixel era lei e as seis medidas são as das telas de hoje (filtro 12×8/34, menu 34, controle 36, acao 14/36, larga 44, etiqueta 6×2) — mas é dívida com nome na ADR, não sistema; a régua para a V13/V15 reduzir a `controle` + `etiqueta`. Resto sem duplicata: 24 rótulos em 12 arquivos ficam (contagem bate com B), `PressaoClara` de CalendarioTema segue viva mas já cita `Tema.pressaoLeve`. Pressão mudou de 0,96 (PressaoClara) para 0,94 (`.discreto`) em "Abrir no Calendário", ✕, Apagar e Repete da ficha: só no press, invisível em captura, dentro do sistema (§2.3 diz 0,94). |
| Acessibilidade | 9 | Labels, hints e ids iguais antes/depois (diff lido linha a linha: `ficha-fechar/pronto/dominio/aviso/apagar`, `chip-dominio`, `filtro-*`, `ordem-notas`, `limpar-busca`); `isSelected` continua no chamador; `LinhaDeEstado` sem laço; `ChipDominio` da ficha ganhou o mesmo label/hint das Notas. AX5: pares abaixo. Pré-existente e declarado: ficha em AX5 sai cortada à esquerda (fileira "Repete") igual nos dois builds. VoiceOver humano em aparelho real segue fora (EVOLUCAO). |
| Performance | n/a | Migração 1:1 de views; nenhuma lista, editor ou parser mudou de algoritmo; `Cartao` acrescenta um `.shadow(.clear, radius 0)` nos estilos sem sombra — custo de rasterização desprezível, mas é a causa provável do anti-aliasing diferente no texto do campo de busca e do cartão da sábia (218–636 px nas capturas de B). |
| Privacidade e autoria | n/a | Diff não toca Modelo, Trabalho, Analise, Notas/Corpus nem rotas; `Sabia.porOndeEmPalavras` e as frases de falha seguem as mesmas; nada publica, gasta ou envia. |
| Estado honesto | 9 | `LinhaDeEstado` mantém "a sábia não respondeu." / "a pergunta foi interrompida." / "a sábia … Sem ela, a busca continua." com os mesmos ids (`sabia-falhou-notas`, `sem-conta-notas`); Recordar "O QUE NÃO VOLTOU" intacto; toast do calendário intacto (não migrado). |
| Complexidade | **7** | Swift do app: **+1149 −470 = +679** (`Traco/*.swift`); `Componentes/` +848 (278 linhas de preview a partir do primeiro `#Preview`, ~200 de comentário de contrato, ~370 de código); as 21 telas/arquivos restantes **−169** (+301 −470); testes +67; docs +172. G0 pedia ≤ 0: não cumprido, B declarou (+640 seu, autoavaliação 6). Julgamento pela ESTEIRA: a lacuna justifica o vocabulário (média 6,2 em Componentes, 32 rótulos, 7 estilos, 16 durações) e preview conta como evidência, não como produto — mas ~110 linhas não têm chamador (Toast 72, BotaoCompacto ~15, `LinhaQueAbre.abaixo`, `confirmacaoEntra`) e são crescimento sem lacuna fechada. Para 9: apagar ou ligar o que não tem chamador (item 1) E o orquestrador rebasear na ADR o critério da fundação ("telas ≤ 0; Componentes com teto e abatimento previsto por volta"), porque nenhuma fundação fecha ≤ 0 no dia em que nasce. |
| Fora do app | n/a | Nenhuma superfície fora do app (widget, Ilha, StandBy, Atalhos) tocada. |
| Relato | 9 | Os dois relatórios são legíveis sem terminal, com tabela por chamada (A) e por tela (B), Δ nomeados, limites declarados (cliclick engolindo teclas, cadência do simctl, pergunta da sábia que muda). Deslizes: a frase falsa sobre a ponte (A) e o PNG acima do teto (item 3). |

## Zero pixel — pares refeitos por mim (build antes = main e6b5033; depois = 2e56518)

| tela / estado | large | AX5 |
|---|---|---|
| Notas vazio | 0 px | 0 px |
| Notas lista (6 notas, IDEIAS/SAÚDE) | 36 035 px = 1,2 % — **só a ORDEM** de duas notas criadas no mesmo segundo ("Marcar dentista" × "Ler o capítulo") trocou entre as rodadas; recorte em `v10-rev-notas-lista-diff.png`: cada linha é idêntica, deslocada. Desenho: 0 | 154 331 px = 5,1 % — de novo só a ORDEM de notas do mesmo segundo (recorte conferido: "Organizar"/"Comprar" trocadas); sem domínios em AX5 (o menu de contexto não é achado pelo maestro em AX5). Desenho: 0 |
| Ficha do compromisso, topo | 235 px = 0,008 % (caret do título) | 0 px |
| Ficha, rodapé (Aviso, Domínio, Notas, Apagar) | 235 px (idem) | 0 px-baixo |
| Calendário mês | 0 px | 0 px |
| Recordar ler (rota, captura imediata) | 0 px | 0 px |
| Recordar escrever (teclado aberto) | 0 px | 0 px |
| Recordar revelar (lado a lado) | 0 px | 0 px |

Recontagem das capturas de B (PNG de 256 cores, mesmo método): notas lista/vazio/filtro 218 px, busca 636, lista AX5 586 — todas na faixa do campo de busca (placeholder "Buscar ou perguntar" e borda do cartão) e no chip "Saúde" da ficha AX5 (1 624 px): anti-aliasing de texto, coerente com o `.shadow` de raio 0 do `Cartao` e com o `Menu` label passando por `.buttonStyle(.discreto)`. Não é redesenho; é o que "0 %" deveria ter dito.

## Achados por severidade

**Médio**
- M1. `BotaoCompacto` sem chamador e duplicando `CompactoStyle`; estilos 7 → 8 contra "→ 3" da ADR (Botao.swift:33-45; CartaoAnaliseView.swift:328).
- M2. `Toast`/`.toast()` e `LinhaQueAbre.abaixo(aberta:)` sem tela que os prove; ADR os lista como entregues.
- M3. Queima da expressiva sob Reduzir Movimento: classe `.deslocamento` reduz a cena de 3,0 s a 0,15 s e o app espera 3,15 s parado (FechoExpressivaView.swift:134-135); não declarado, não filmado. Se a intenção é essa, a ADR diz; se não, classe `.opacidade` (mantém 3 s) ou `.corte`.
- M4. EVOLUCAO "0 % em large e AX5" e relatório de A sobre a ponte: frases a corrigir.

**Baixo**
- B1. `Tema.confirmacaoEntra` órfã (Tema.swift:153-155).
- B2. `BotaoPrimario`: `.animation` antes de `scaleEffect` — herdado do `PrimarioStyle`; a pressão não anima. Volta do Recordar.
- B3. `v10a-diff-ax5.png` 491 KB > 400 KB (RUMO linha 35).
- B4. Célula nova +0,25 s de assentamento (declarado); sem filme comparativo.
- B5. `CabecalhoDeFolha` grava `accessibilityIdentifier("")` quando não há prefixo (Recordar): inócuo, mas é ruído na árvore.

## Instrumento e limites

`xcodebuild test` 650/0 no C2416CBC; build "antes" em `$scratch/build-antes` (EXIT 0); maestro 7 fluxos por UDID explícito (o `varrer.sh` exige um só simulador ligado e o dono tem dois — não desliguei nenhum); capturas por `takeScreenshot` do maestro (PNG cheio), AX5 por `simctl ui content_size accessibility-extra-extra-extra-large`, restaurado a `large`. O filme da célula nova não provou nada (toque errado do fluxo). Não medi VoiceOver nem Instruments (n/a nesta volta). O maestro reescreve `busca-celular.png` e `recordar-lado-a-lado.png` na raiz: restaurados.
