# V12-H — a última frase falsa da 08f, e o G5 (topo antes: `0840424`)

Worktree `volta-v12b-pagina`, simulador iPhone 17 Pro (teste 2) `B91C8DEF`.
Trava segurada em todo `xcodebuild` (`com-trava.sh`; dono conferido no
`/tmp/traco-instrumento.lock/dono`). Sem maestro, sem mouse, sem tocar no
`C2416CBC` da volta Q.

## T1 — a frase

Em "O que esta seção não muda" a 08f dizia: *"ao abrir a folha o encaixe some
por corte e, ao voltar, reaparece por corte — a folha cobre a tela nos dois
instantes"*. Agora diz: *"ao abrir, o encaixe some por corte e o papel fica só,
nu, por ~6 quadros (45–90 ms, medido) até a folha subir; ao voltar, reaparece
por corte atrás da folha ainda de pé"* — a frase proposta pelo juiz no re-G4
segundo (`g4-v12-design-final.md`), sem alteração. Subseção curta "V12-H" no
fim da 08f regista a troca e a prova da árvore mesclada.

## G5 — `main` no branch

- `git merge main`: conflito só em `SPEC.md`, de lugar (as duas pontas apendem
  ADR no mesmo ponto). Resolvido em ordem cronológica: 08a, 08b, 08e, **08f**,
  08g, 08h, 08i, 08j, 08k. `EVOLUCAO.md` e `project.pbxproj` mesclaram sozinhos;
  `xcodegen generate` sobre a árvore mesclada não muda o pbxproj (diff vazio).
- `git log HEAD..main | wc -l` = **0**. Só uma `## ADR 2026-09-08f` no SPEC:
  não colide (main não tem 08l/08m no SPEC apesar do que a tarefa dizia).
- Prova da árvore mesclada (`ferramentas/orca/com-trava.sh xcodebuild test
  -project Traco.xcodeproj -scheme Traco -destination id=B91C8DEF-… 
  -parallel-testing-enabled NO`):
  - build: `grep -c "warning:"` = 0 no log.
  - `✔ Test run with 933 tests in 152 suites passed after 55.256 seconds.`
  - `➜ Suite CadernoHitchesTests skipped.` (o mesmo pulado de sempre)
  - `✔ Suite PortaoDoMovimentoTests passed after 1.016 seconds.`
  - `Test Suite 'All tests' passed at 2026-09-08 18:35:05.455.`
- A contagem subiu de 892 para 933 (+41). Não consertei nada; é tudo de
  `main`, por `@Test` novos entre `0840424` e `main`:
  `AjusteDoExercicioTests.swift` +18 (até a linha 689),
  `ForaDoAppTests.swift` +9 (683), `LinhaDoTempoWidgetTests.swift` +7 (384),
  `ConferenciaTrabalhoTests.swift` +3 (755), `PortaoDoMovimentoTests.swift` +2
  (157), `EscritaPessoalTests.swift` +1 (534), `GrokContratoTests.swift` +1 (32).
  Nenhum vermelho vindo de `main`.

## Evidência solta comitada (sem editar os relatórios do juiz e do revisor)

- 58 PNG (`g4f-*`, `g4g-*`, `g4h-*`): **34,5 MB → 10,3 MB** por
  `sips -Z 1000`; quatro tirinhas de quadros ainda passavam de 400 KB a 1000 px
  e foram a 800 (`g4h-*-quadros-30-53`: 361/354 KB) e 700
  (`g4g-abrir-*-quadros`: 369/375 KB). Nenhuma acima de 400 KB.
- 8 MP4 (1,4–2,9 MB cada, **15,8 MB → 0,6 MB**): re-encodados com
  `ffmpeg -fps_mode passthrough -vf scale=440:-2 -crf 30`; contagem de quadros
  conferida igual por `ffprobe -count_frames` antes e depois
  (72, 77, 67, 66, 74, 82, 90, 73). Os PNG tracked de outras voltas
  (`g4-f4-*`, `g4-l1-*`, 1,0–2,4 MB) não são meus e ficaram como estão.
- `Traco.xcodeproj/xcshareddata/xcschemes/TracoUITests.xcscheme` entra: o
  condutor da prova de tela depende dele.
- 9 `g4f-ax-*.txt`, 5 `g4g/g4h-*.txt` de medidas, `g4-v12-design-final.md` e
  a alteração em `revisao-v12b-pagina.md`, como estavam.

## Scorecard (meu; a nota é do revisor)

| dimensão | nota | por quê |
|---|---|---|
| Contrato | 9 | a 08f diz o que a tela faz nos dois instantes do corte, com o número do juiz; a subseção V12-H diz de onde vem |
| Correção | 9 | suíte 933/933 e portão do movimento verdes na árvore mesclada, linhas coladas |
| Jornada real | 9 | nada mudou na tela nesta volta; a prova é a do re-G4 segundo (PASSA, Design 9) |
| Design | 9 | nota do juiz no re-G4 segundo; esta volta não tocou pixel |
| Movimento | 9 | 0 pares do encaixe; a `UIMenu` do sistema é limite declarado, não desconta |
| Acessibilidade | 9 | 8 pt de ar em AX5 medidos pelo juiz |
| Estado honesto | 9 | a frase falsa caiu; os vídeos reduzidos têm a contagem de quadros provada igual |
| Complexidade | 9 | 0 linhas de código; uma frase e uma subseção no SPEC |
